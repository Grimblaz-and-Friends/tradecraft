{"artifact": "skills/adversarial-review/references/arbitration.md — clause (b)", "finding": "The fix and record arms' antecedents overlap on a real finding with a determined cheap non-prose remedy and no evidence anyone would act differently; the ordering is carried only by the weight clause's scope condition, which is not stated as a tie-break.", "promotion": "a terminal stage or consumer that mis-rules the overlap in a case the weight clause's scope condition does not reach, or that asks which arm governs."}
{"artifact": "skills/adversarial-review/references/dispatch.md — the record fetch", "finding": "The fetch says 'from wherever the repository keeps them' and names no fallback for an adopter keeping no such record, where the review index's parallel obligation does name one.", "promotion": "an adopter session that stalls, invents a file, or reports the fetch unperformable."}
{"artifact": "skills/adversarial-review/references/arbitration.md — the defense's price and clause (b)", "finding": "The defense states a price on every non-disproved finding; no sentence says which limb of clause (b) or the weight clause consumes which of its two terms.", "promotion": "a terminal stage that states it cannot place a defense's price, or a defense that stops stating one."}
{"artifact": "skills/adversarial-review/SKILL.md — the lens-retirement paragraph", "finding": "Nothing says that performing a retirement which deletes shipped governing prose is itself a clause-(b) decision rather than an act of the review; 'the report names any retirement' can be read as licensing the edit.", "promotion": "a review that performs a shipped-surface retirement without convergence, or a stage that asks whether it may."}
{"artifact": "AGENTS.md flow line and skills/engagement/SKILL.md — the reconcile step", "finding": "Both name only 'external reviewer comments'; under the narrowed definition a commissioned pass is not covered, and it must run earlier in the flow than the step it used to sit in.", "promotion": "a session that skips a commissioned pass, books one under 'external', or runs it out of order."}
{"artifact": "tools/lint.py - check_subprocess_streams's walk", "finding": "The check does not filter through .gitignore where two sibling checks do, so an ignored .py file under skills/ or tools/ is linted; the remedy is determined (route its candidates through _git_ignored) and costs one git check-ignore subprocess per lint run plus reduced reach in the git-less trees the suite builds.", "promotion": "a commonly-ignored .py path landing under tools/ or skills/, or a finding reported against an ignored file."}
{"artifact": "tools/lint.py - check_subprocess_streams's walk", "finding": "The walk covers SHIPPED_DIRS and REPO_ONLY_NAMES only, so a repository-root .py file is never read and its launches go unchecked; the remedy is determined (widen the walk) but diverges this check's walk alone for a file class this tree does not have.", "promotion": "a .py file landing at the repository root, here or in an adopting repository whose lint derives from this one."}
{"artifact": "skills/adversarial-review/SKILL.md - the evidence standards", "finding": "The both-polarity probe binds a guard under review but is nowhere stated to bind a finding's own proposed remedy; three remedies in PR #232's review (round one's M5, cycle one's P1 and P3) diagnosed correctly and prescribed a change that would have reddened lawful work, each caught only because the defense ran the counterfactual unprompted.", "promotion": "a remedy that lands unprobed in the lawful polarity, or a cycle where the defense does not run the counterfactual and a fix reddens a lawful call."}
{"artifact": "tools/lint.py - _redirected's crediting of check_output's implicit stdout", "finding": "The guard credits check_output's implicit stdout and also reads an explicit stdout= keyword on it, so check_output(cmd, stdin=DEVNULL, stdout=PIPE, stderr=DEVNULL) scores three covered streams and passes clean while the call raises ValueError('stdout argument not allowed') on every execution -- the spelling the shipped reference calls impossible. The remedy is determined (a launcher in _IMPLICIT that names the stream it owns is reported as an invalid call rather than as a partial redirect) but needs its own finding class, message and pin, against a failure that is deterministic and self-naming on first run.", "promotion": "a check_output call naming stdout reaching a branch, or a session reporting that lint passed a call that raises ValueError."}
{"artifact": "tools/roster.py — matches(), and the mutation pins on it in tools/tests/test_roster.py", "finding": "Two mutants of the predicate survive the whole suite and are left unpinned: normalising then .strip() on both sides, and normalising then .lower(). Each is behaviour-changing - an entry differing from its cell only in leading or trailing whitespace, or only in the case of one description word, fires under the shipped predicate and is forgiven under the mutant. Pinning every relaxation of a predicate nobody has proposed was judged unbounded work against a failure mode with no route into the tree, since the entry is generated from the cell and neither whitespace nor case drifts on its own.", "promotion": "a predicate normalising whitespace or case is proposed for matches() or written into a sibling normaliser this one is harmonised with, or an entry differing from its cell only in trailing whitespace or only in case reaches a tree."}
{"artifact": "skills/adversarial-review/references/after-the-fix.md — the post-fix look's scope", "finding": "The look on PR #231 ran under the pre-#230 scope and prosecuted the review's own bookkeeping. One of its seven findings was purely that: the PR body's mutation-matrix control row, which the defense then disproved - the published figure was correct, and fixing it would have replaced it with one no deterministic run produces. It cost a look, a defense verdict and a ruling to establish that nothing was wrong. A second finding counted revisions in two self-referential sentences and reads as bookkeeping, but its sites were in tracked files and it was fixed, so it is not an instance. #230 retired that scope after this review closed; this is one measured instance of what the retired clause produced, recorded rather than acted on because the clause is already gone.", "promotion": "a later post-fix look returns apparatus-facing findings again despite the retired scope, or the retirement is argued to have been reversed."}
{"artifact": "commit c830f51's message, PR #246", "finding": "States a derived comparison -- 'The surface is net smaller than at the base despite the grown authoring description' -- with no command and no tree. A commit message is named in both the figure rule's list and the behaviour rule's, so the falsifier (which command, on which tree?) applies and is unanswered. Sustained but not fixed: the only remedy is a history rewrite, and be0c943 is cited by the experience-session note, all five seat reports, the assignment, both defenses and both rulings, none of which can be repaired once orphaned. The one remedy on the docket that would destroy the review's own evidence base.", "promotion": "a correction path for a landed commit message that does not require rewriting history, or a second instance where the unverifiable figure actually misleads someone."}
{"artifact": "docs/architecture/decisions/README.md -- the claim-correcting repair's bounds", "finding": "Silent on whether the frozen archive D-53-D-69 is inside the permission, where the repoint permission answers the same question explicitly one paragraph above ('D-53-D-69 are inside this permission'). The conspicuous asymmetry reads as deliberate exclusion. Not ruled the owner's: undoing a wrong pick costs an edit to a repo-only README, not habit or what others saw, so it fails the fork test. Not fixed: the only remedy is added prose on a surface this change already grew, against a session meeting a falsified sentence in entries D-135 itself says 'nothing sends a reader there to act'.", "promotion": "a session actually meeting a falsified sentence in D-53-D-69 in the course of other work, or escalating over the silence."}
{"artifact": "tools/lint.py -- the decision-entry guards", "finding": "No check couples the [corrected] marker to its Erratum block. Probed on a git archive copy by two independent stages with in-class negative controls: a silent claim rewrite of a landed entry, a marker with no block, a block with no marker (the footer-only shape the owner ruled against), and an index-row summary replaced wholesale all pass lint clean, while an unresolvable reference inside the block and a deleted index row both fire. The lawfulness half is unreachable by any guard -- whether cited evidence truly falsifies a sentence is judgment -- and that half is not what this records; the marker-block biconditional is mechanical and uncovered. Not fixed: zero instances exist in 48 entries, the change itself records that the clause landed with no live customer, and the remedy would be the first guard to read entry body structure, with a false-positive class (any bracketed 'corrected' written for another reason) that would red lawful work.", "promotion": "the first erratum landing footer-only or marker-only in a real entry."}
{"artifact": "skills/adversarial-review/SKILL.md:41 -- the load-bearing-claim standard, as applied to universals", "finding": "Two seats independently sustained the same unchecked universal in one review -- 'the ## References section every entry ends with' / 'every entry carries', true 1 time in 48, and the one entry carrying it was the artifact under review. A third seat miscounted the same file's skills/ citations as thirteen against an actual fifteen. Each was caught downstream and none changed a disposition. The lens is already worded at :41 ('Load hides in absence claims, universals, superlatives, and counts -- check those first'), so this is under-application rather than a gap, and a promoted lens restating it would be the armor authoring bars.", "promotion": "a second review where a seat files on an unchecked universal or count."}
{"artifact": "skills/adversarial-review/SKILL.md -- the session note's rank over behaviour", "finding": "A seat cited the experience-session note as independently corroborating its finding that one README sentence gives two opposite answers about whether a correction changes a claim. The note reports no such friction: its reported difficulty is bound three's description-versus-judgment line, a different sentence carried by a different finding. The standards give a note rank 'on behavior and on nothing else', so citing it for a claim it does not report carries that rank onto an unprobed claim -- and the claim it was lent to was then disproved by the defense and dropped by the judge.", "promotion": "a second instance of a session note cited for a claim it does not report."}
{"artifact": "skills/adversarial-review/SKILL.md:41 -- the load-bearing-claim standard, as applied to counts", "finding": "A fourth instance of a stated count reaching a stage unchecked, in the same review. The post-fix look wrote '17 of 48 entries carry a pin; 28 pinned references against 14 unpinned' in a declined examination. Re-derived at 4e2bc92 by the terminal stage: 48 entries, 12 carrying a pinned reference under lint._entry_refs, 28 path-form pinned against 199 path-form unpinned, 29 against 299 overall -- 14 reproduces under no basis, being figures.py's unresolvable-reference census, a different quantity. The class is covered twice over, since authoring's figure rule independently binds a derived figure on a seat report to its command, and is still skipped -- so the failure is not that a rule is missing or unclear. Round one's own promoter was not met on its terms: it named a second review where a seat files, and this is the same review's second cycle, in a declined examination on which no disposition turned.", "promotion": "a stated count a later stage cannot reproduce where a review's ruling actually turned on it, or a mechanism proposed that could check a count over a corpus the change itself writes into."}
{"artifact": "tools/lint.py -- the repository-wide walks: _iter_files, _python_files, and _prose_files", "finding": "_prose_files is a third rglob of the whole repository, differing from _iter_files only by a .git clause, and its docstring described its provenance as check_emitted_ascii's walk, which is narrower (*.py rather than *). The remedy is determined -- one shared walk, or git ls-files -co --exclude-standard, which returns tracked plus untracked-not-ignored in a single call and removes the check-ignore round trip too -- but the walk was measured at 4ms of a 0.88s run, so the cost case for acting is absent and the duplication is the whole of the harm. The docstring sentences were corrected; the third copy was left standing.", "promotion": "a fourth repository-wide walk is written, or a filter has to be applied across all of them at once, or a walk's cost is measured as a material share of a lint run."}
{"artifact": "tools/roster.py -- frontmatter()'s paragraph on the interior line endings it copies", "finding": "The sentence 'the committed bytes are the same wherever the generator last ran' is stated without the bound in_step states two functions down: git's text=auto refuses to normalise a file holding a lone carriage return, so a cell carrying one inside its frontmatter propagates it into the entry verbatim. Probed: such a cell and its entry both commit as i/-text. The only remedy adds a clause to the paragraph an experience session established, from real use, is the one a cold consumer reads and is turned by -- the consumer arrived with the wrong thesis and that paragraph corrected it -- and the bound is already stated in full, in the function whose job it is. Weight against a reader nobody has observed failing.", "promotion": "a session reuses frontmatter() outside the roster generator, or a report that a reader acted wrongly on the unqualified sentence, or the bound stops being stated on in_step."}
{"artifact": "tools/lint.py -- the message class for findings about the lint itself, being check-raised and check_emitted_ascii's unparseable-file branch", "finding": "check-raised states the situation and names no next step, so a session holding a red on the flow's mandated pre-commit gate is not told whether to file, re-run, or proceed. The claim that it is the only such message in the file was disproved: emitted-ascii's unparseable branch does the same, and is the same kind of message -- a report that the lint could not do its job rather than a report about the tree -- so check-raised follows a house pattern rather than breaking one. The remedy adds prose to a message an experience session established is untested by use, and if bought it covers both messages or neither.", "promotion": "a session reports being unable to act on a check-raised or unparseable-file finding, or a third message of this class is written, or either message is observed in use."}
{"artifact": "tools/lint.py -- check_committed_carriage_return's binary test", "finding": "The check skips binary content on a NUL in the first 8000 bytes, matched to git's own detection window so the two cannot disagree inside it. A binary whose first NUL falls past 8000 still classifies as i/-text, still has its bytes read, and still draws a finding whose remedy a reader cannot safely apply. The class is unclosable at this layer: git's own classification cannot separate such a file from a lone-carriage-return text file, which is the thing the check exists for. The remedy clause was softened to stop instructing a rewrite unconditionally; the misfire itself stands.", "promotion": "the first binary is tracked in this repository, or a finding is reported against a file whose text-ness the check cannot establish."}
{"artifact": "tools/lint.py -- _read_text's None, and what a check does with a file it cannot read", "finding": "_read_text returns None for 'binary, deliberately out of scope' and for 'could not be read' alike, so a caller cannot tell them apart and the mandated command can exit 0 over content it never examined -- the shape check_emitted_ascii's own message condemns. Distinguishing them changes a helper's contract with four consumers, which is a design call, and the docstring names two siblings (roster.is_generated, _git_ignored) that answer silently by design. The raising half was fixed; this is the silent half.", "promotion": "a third caller needs to tell the two apart, or a run is reported clean over content it could not read."}
{"artifact": "tools/lint.py -- check_committed_carriage_return's message for the untracked population", "finding": "The finding class is named committed-carriage-return and its remedy says to stage the file, but the untracked population reaches files a session may never intend to commit: persist.py stages only named pathspecs, so an untracked non-ignored scratch file is not on its way to a commit. The remedy adds a conditional clause to a message that two consecutive experience sessions establish is untested by use, which is the reasoning round one used to record the sibling finding about check-raised. A gitignored scratch file is already excluded, so the class is narrow.", "promotion": "a session reports being told to stage a file it did not intend to commit, or any untracked-population finding is observed in use."}
{"artifact": "tools/lint.py -- the skip lists, FROZEN_ARCHIVE and LIVE_RECORDS, against AGENTS.md", "finding": "The lists restate what the doctrine names rather than deriving from it, so a frozen path added to AGENTS.md and omitted here passes every test. Deriving them from the doctrine is the real remedy and was priced as bigger than the defect it closes. The test that claimed to check the lists against the prose now pins what they hold instead, which is the narrower true thing.", "promotion": "a frozen path is added to AGENTS.md, or a third list of doctrine-named paths is written into a tool."}
{"artifact": "skills/substrate/SKILL.md -- bullet 5, 'A path resolved against the directory of the file naming it'", "finding": "Stated as working in a source repository and an installed plugin alike, which holds only for an interpreted substrate. A compiled binary has no source-file directory at run time and os.Executable() names the install location, not the repository. The cell says nothing about the bound. Reported by the Go seat of PR #252's experience session, which deviated deliberately and said so.", "promotion": "an adopter on a compiled substrate reports the rule as inapplicable or follows it wrongly, or a second cell rule is found substrate-dependent."}
{"artifact": "skills/substrate/references/text-mode.md and references/subprocess-streams.md -- their Python register", "finding": "Both are stated in one substrate's API vocabulary while the rules they carry are general, and nothing signposts which sentences bind a reader on another substrate. PR #252's Go seat named separating rule from illustration as a real cost and asked for 'a signpost saying: if your substrate does not have configurable stream encoding, here is what still binds'. The remedy is a restructure of a shipped reference, which #252 priced out. Naming the language again is counter-indicated: an A/B in #252's defense found the base arm, where the language was named, paid MORE of its effort on that same triage and additionally took 'stdlib-first' as binding on a Go tree.", "promotion": "a second adopter on another substrate reports the same triage cost, or a restructure of the cell is undertaken for another reason."}
{"artifact": "skills/substrate/SKILL.md -- bullet 1, 'One substrate, chosen once and hardened'", "finding": "States an absolute with no bound: a polyglot adopter cannot tell whether it means one substrate per repository, per zone, or per new-code decision. AGENTS.md carries a 'for new code' bound the shipped rule does not. Whether the shipped standard should carry a per-decision or per-repository bound is a design call, and landing a one-off bound would pre-empt the general strictness standard #252 deferred. The one cold seat that met the absolute read it correctly and without friction.", "promotion": "a polyglot adopter reads the standard as forbidding what they have, or the general strictness standard is picked up."}
{"artifact": "skills/substrate/SKILL.md -- the description's 'when choosing a language for new code' trigger", "finding": "The body's answer to that trigger became a six-word redirect to the reader's own doctrine when #252 removed the language pick. Removing a trigger is a behaviour change to an always-on surface and no evidence exists in either direction: the instrument that could produce it, an experience session, tests bodies and not the descriptions that fire them. The precedent the panel pinned (D-192's 'or a dependency' trigger) does not reproduce -- that phrase never entered the shipped description.", "promotion": "an experience run that loads the cell on the language trigger and reports the body's answer as not worth the load."}
{"artifact": "AGENTS.md -- 'one thing every new script can assume', and the dropped 'Stdlib-first'", "finding": "#252 removed the practice's only sentence expressing dependency restraint, on the owner's ruling that the general standard be minimal. The norm is still operative and now unwritten: PR #252's experience session recorded a seat re-inferring it from this AGENTS.md clause and declining an installed PyYAML. Writing it back down would undo the owner's ruling and re-admit a rule #192 cut with no incident behind it.", "promotion": "a session here adds an installed dependency to a tools/ script and nothing objects."}
{"artifact": "tools/lint.py -- check_stdio_wired's finding message on the module-attribute call spelling", "finding": "A module that imports winio both ways -- `import winio` and a redundant `from winio import utf8_stdio` -- and calls winio.utf8_stdio() as the first statement of main() is reported as 'defines main() and does not call it first', because the check requires first.value.func be an ast.Name. The redundant from-import is what makes the message wrong rather than merely loose: with `import winio` alone the guard reports 'neither imports nor calls it first', since it scans only ast.ImportFrom. Probed on both spellings; the external reviewer caught this entry naming the second case's spelling against the first case's message. The call is first; the diagnosis names the wrong defect and the remedy prescribes what is already done. Message-only, but it is a code change and #252's affirmed boundary excluded changes to this guard. Bound: the misdiagnosis needs a redundant from-import present; the pure module-import spelling gets a loose message with a working remedy.", "promotion": "a session follows the misdiagnosis and edits ordering that is already correct, or the guard is opened for another reason."}
{"artifact": "skills/substrate/references/text-mode.md -- rule 2's 'two properties'", "finding": "lib/winio.py sets three: encoding, newline suppression, and errors='replace', the last because a guard that dies encoding its own failure message converts a legible failure into a traceback. A reader discharging exactly the two the rule names gets rc=1, empty stdout and UnicodeEncodeError on a surrogate filename byte -- the failure the same file describes two sections down -- reachable on Linux CI via surrogateescape. Originated by #252's defense and reproduced by its judge. Not fixed there because the remedy has two shapes, a third property in the headline or a bound in the helper-less branch, and picking trades against the rule's new strength of being exactly two.", "promotion": "a script written under rule 2's helper-less branch dies encoding a name."}
{"artifact": "skills/substrate/references/text-mode.md -- rule 2's 'a version-stamped path that stops resolving at the next release'", "finding": "A plugin cache does not promptly stop resolving. Measured on one machine: of the two plugins that have ever been updated, both retained the older version directory (tradecraft across 98 minutes, agent-orchestra across four days), and the day's in-use sweep removed neither; 0.37.0 still resolved six days and seventeen releases on. The two claims that decide the clause are sound though: the decisive reason is on the shipped surface one clause earlier as the within-reach test, and the plugin-installed seat of PR #252's second experience session read this clause, called the reasoning visibly true, and wrote both properties itself rather than reaching for a cache path. Not fixed because replacing it means asserting something true about cache eviction, which is a design call, and deleting it removes something a reader reported as useful.", "promotion": "a reader reaches for a cache path, or a change makes this clause load-bearing -- in particular removal or weakening of the within-reach test that currently carries the branch."}
{"artifact": "skills/substrate/references/text-mode.md -- rule 2's helper-less branch, folded with the 'two properties' entry above", "finding": "The branch states the requirement and never the compliant call. grep over shipped prose returns no reconfigure/newline= form anywhere; lib/winio.py states it and does not ship as prose. PR #252's second experience session: the seat with the plugin on disk did not hit this because it could read winio.py, and the seat with prose alone confirmed the newline half by capturing bytes rather than by reading anything shipped. Blocked on the same design call as the errors='replace' entry: naming the call means settling whether the third property is part of it, which the two seats answered differently. Cheapest candidate that avoids the design call is a pointer at the shipped worked form rather than a restatement, which does not reach an adopter who copied skills/ without lib/.", "promotion": "a third reader pays the same cost, or the errors='replace' question is settled -- at which point the compliant form is one clause and this becomes a fix."}
{"artifact": "skills/substrate/references/text-mode.md -- rule 2's density, as a whole", "finding": "Evidence for the density thesis rather than an independent defect. The 'neither redefine nor shadow' clause is scoped by its sentence opener and so does not contradict the outside-this-tree branch, but a reader must carry that scope across a long sentence: PR #252's plugin-installed seat resolved it the way the rule wants and reported it cost a real minute. The bullet now carries seven distinct moves. Its only available remedy is another qualifier on the sentence whose qualifier count is the problem, which is why this is recorded rather than fixed. The item behind it is the restructure fork -- general standards in SKILL.md, this practice's own discharge behind a reference -- which D-252 priced and rejected as a restructure of a cell five days old, a reason that has since changed.", "promotion": "a second reader reports the same tension, or any reader resolves it the wrong way. At two readers the item is the restructure fork put to the owner, never a fourth edit of the sentence."}
{"artifact": "tools/lint.py -- check_stdio_wired's docstring", "finding": "It carries the same over-claim PR #252 corrected on the shipped surface: 'The import binding is checked too, which is what makes it was called mean the helper was called.' It does not -- the predicate never reads node.module and a shadowing definition after a correct import passes green. This is where the shipped phrasing came from. Not fixed in #252 because its affirmed boundary excluded changing check_stdio_wired, and a docstring is part of it.", "promotion": "issue #254 is worked -- the docstring moves with the guard, or the over-claim survives its own fix."}
