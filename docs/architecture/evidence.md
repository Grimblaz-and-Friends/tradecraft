# Evidence registry — provenance for transcribed claims

ADR-009 §4 mandates citations over copies, and the 2026-08-15 review (finding M28) showed the constitution transcribing its strongest quantitative claims with no attached record. This registry is the repair: every transcribed claim an ADR leans on, with its provenance graded honestly. Grades:

- **repo-verifiable** — checkable from a public repository today; link given.
- **issue-linked** — recorded in a predecessor issue; link given.
- **session-measured** — measured during a recorded working session; the query is given so it can be re-run, but the original run itself is not durably archived.
- **recollection-grade** — carried from a session record, in the predecessor or in this repo, with no independently checkable artifact. Claims at this grade must not be load-bearing for new mandates, only illustrative.

| Claim | Used by | Grade | Record |
| --- | --- | --- | --- |
| Predecessor first commit 2025-12-07; ~60 skills (57 measured) | ADR-001, ADR-009 | repo-verifiable | `git log --reverse` and `ls skills/` in [agent-orchestra](https://github.com/Grimblaz/agent-orchestra) |
| Workflow logic in code root-caused six times to the same never-wired-live failure — the tally is the predecessor's own judge ruling (PR #903: findings M1/M4/M5/M7/M9/M10 "were one root cause — correct, unit-tested lib code that the agent-body orchestration prose never wired into the live path"); earlier drafts of this row mis-linked the tally to #874 and then wrongly declared it nonexistent — the number was always real, the link was wrong. The class recurs beyond that PR | ADR-002 | issue-linked | [PR #903 judgment](https://github.com/Grimblaz/agent-orchestra/pull/903) (the six-finding tally); further instances: [#844](https://github.com/Grimblaz/agent-orchestra/issues/844) (workflow never parsed, never ran), [#761](https://github.com/Grimblaz/agent-orchestra/issues/761) (the #762-shipped emission produced zero blocks across three consecutive reviews), [#949](https://github.com/Grimblaz/agent-orchestra/issues/949) (ledger never invoked on the goal lane; named hook absent from registry; harness bypassed its own loop), [#953](https://github.com/Grimblaz/agent-orchestra/issues/953) (join key "shipped" but never wrote a row) |
| 15 of 15 sustained findings in newly generalized prose, none in carried-verbatim text | ADR-009 | issue-linked | [agent-orchestra#844](https://github.com/Grimblaz/agent-orchestra/issues/844) review record |
| 324 open / 395 closed issues; ~40% of open untouched 90+ days; open/close rate never net-negative | ADR-006 | session-measured | `gh issue list --repo Grimblaz/agent-orchestra --state open/closed` + age bucketing, measured 2026-08-15 during the backlog-sustainability discussion that produced the filing rule |
| A five-pass review panel sustained a false finding | ADR-006 | recollection-grade | predecessor session record, 2026-08; the panel sustained a "A never provides B to C" claim later disproven by a reference grep |
| Post-fix re-review reliably finds defects in fresh fixes, the worst usually fix-introduced (upgraded from the recollection-grade "~1-in-3": [#953](https://github.com/Grimblaz/agent-orchestra/issues/953) records "a fix introduced a new defect in 3 of 5 rounds on #912" verbatim, and [PR #926](https://github.com/Grimblaz/agent-orchestra/pull/926) — #912's fix PR — confirms fix-introduced defects on its own record, incl. a defense-originated critical; 9 further findings on [PR #930](https://github.com/Grimblaz/agent-orchestra/pull/930)'s fix diff; 2 criticals both defects of the first remediation on [PR #1026](https://github.com/Grimblaz/agent-orchestra/pull/1026); ≥4 of 21 novel externals fix-introduced on [PR #991](https://github.com/Grimblaz/agent-orchestra/pull/991) round 2) | ADR-006 | issue-linked | PR judgment comments on the four PRs cited; seat-level mining recorded in [tradecraft#1](https://github.com/Grimblaz-and-Friends/tradecraft/issues/1) (whose correction comment carries per-round comment ids for the seat-yield table, not for these four PRs) |
| Stray zero-byte file dropped by a concurrent agent, nearly committed via broad `git add` | persist-changes SKILL.md | recollection-grade | predecessor session record, 2026-08 (stage-1 parallel review dispatch) |
| `__pycache__` artifacts shipped via broad staging in tradecraft's skeleton commit | ADR exhibits, persist-changes SKILL.md | repo-verifiable | [30fb484](https://github.com/Grimblaz-and-Friends/tradecraft/commit/30fb48482448ded6f45ccd9a2eb6ddb413bdee10), removed in [b98b569](https://github.com/Grimblaz-and-Friends/tradecraft/commit/b98b569) |
| Predecessor shipped-zone relative references to repo-only paths (23 live `../../.github/` instances in 6 skill files) | lint M1 rationale | repo-verifiable | grep in agent-orchestra `skills/` |
| Persona-prompting research both directions | ADR-001 | repo-verifiable | the four papers linked in ADR-001 |
| The 2026-08-15 full review of this repo: 51 raw → 38 merged → 35 sustained (6 high) / 3 dismissed; redundant generalist prosecutors added zero unique sustained findings; the defense killed 2 false constitution highs and corrected 12 severities; the post-fix pass caught 2 defects in the fixes (cwd-vs-root resolution; double-fire on relative zone refs) | ADR-006 review default; [adr/README.md](adr/README.md) | repo-verifiable | commit [fa3345b](https://github.com/Grimblaz-and-Friends/tradecraft/commit/fa3345b) (totals, dismissals, both post-fix defects by name) and [1e95412](https://github.com/Grimblaz-and-Friends/tradecraft/commit/1e95412) (stage yields); the sustained rows carry `source: full-adversarial-review-2026-08-15` in [../ledger.jsonl](../ledger.jsonl). Re-run: `git show -s --format=%B fa3345b 1e95412`, and for the row count `python -c "import json,sys;print(sum(1 for l in open('docs/ledger.jsonl',encoding='utf-8') if l.strip() and json.loads(l)['source']=='full-adversarial-review-2026-08-15'))"` from the repo root |
| Two further stage details of that review: the defense strengthened 3 findings, and the judge confirmed defense on all but severity settling | illustrative only — no mandate leans on these two | recollection-grade | unlike the sub-claims above, these two appear in no commit message, and that review's in-session prosecution, defense, and judge ledgers were never persisted (re-run: `git log --all -i --grep=strengthen --grep='severity settl'` returns nothing). Its rows also carry `found_by: unrecorded` throughout, so per-review `source` values and `found_by` close this gap for reviews from PR #2 onward, not retroactively |
| Seat-level review yield in the predecessor: across the 6 rounds with per-seat attribution (PRs [#843](https://github.com/Grimblaz/agent-orchestra/pull/843), [#859](https://github.com/Grimblaz/agent-orchestra/pull/859), [#1006](https://github.com/Grimblaz/agent-orchestra/pull/1006), [#955](https://github.com/Grimblaz/agent-orchestra/pull/955), [#1044](https://github.com/Grimblaz/agent-orchestra/pull/1044), [#874 design](https://github.com/Grimblaz/agent-orchestra/issues/874) — 116 sustained findings), a single pass retains 12% and loses nearly every critical and high; the best single seat with hindsight retains 46% (and did catch the corpus's one critical, at #859); best-3 seats retain ~75–100% by round, the 4th seat's marginal yield is real (up to ~1/5 of a round, at times highs), the 5th's thin; no seat consistently strongest; defense corpus-wide killed 40+ and originated 8+ sustained findings incl. a critical; 4 cold-read sole-finds, three load-bearing (the mining comment counts four load-bearing; #874's own dispositions classify its CR-F6 `routine`, so this row corrects its source's count); the #878-F1 class escaped 15 seat-readings + 2 post-fix cycles and fell to a differently-positioned lens | ADR-006 amendment (panel default, `found_by`) | session-measured | mined 2026-08-15 from judge score tables and dispositions markers; per-round comment ids in [tradecraft#1](https://github.com/Grimblaz-and-Friends/tradecraft/issues/1)'s correction comment. Re-run: fetch each round's judge table / dispositions marker at those ids, take each sustained finding's primary `pass`, and compute retention = findings kept ÷ 116 (single-pass: 14; best-seat-per-round: 53; per-round best-3/best-4 sums from the same pass columns) |
| The ledger's phase fields were degenerate before the 2026-08-16 amendment: all 192 rows then on `main` carried `introduced: authoring`, `catchable: authoring-review`, `caught: adversarial-review` — three fields, one value each, zero variance | ADR-006 §5 amendment (position/stage split) | repo-verifiable | Re-run against the pre-amendment tree: `git show 78d8524:docs/ledger.jsonl \| python -c "import json,sys,collections;r=[json.loads(l) for l in sys.stdin if l.strip()];print(len(r),[dict(collections.Counter(x[f] for x in r)) for f in ('introduced','catchable','caught')])"` |
| `ref` resolution rate: 34 of those 192 rows reach a surface naming that row's own finding id — 6/35 (commit-targeted), 7/50, 13/57, 8/50 (PR-targeted). Re-aiming the PR-targeted rows at their squash-merge commits was drafted and reverted: it measured **worse** on every group (5 vs 7, 11 vs 13, 0 vs 8), because those commits read richer in prose while naming fewer finding ids | ADR-006 §5 amendment (`ref`'s enumerate-findings property, and the debt recorded against the historical corpus) | session-measured | Measured 2026-08-16. Re-run: for each row, word-boundary match its `id` against the text of its `ref` target — `gh pr view N --json comments` for PR targets, `git log -1 --format=%B SHA` for commit targets. **Two known biases, in opposite directions, which is why no bound is claimed.** Inflating: finding ids repeat across sources within one PR (`pr4-panel-review` and `pr4-delta-panel` share 20 ids: `H1`–`H4`, `L1`–`L6`, `M1`–`M10`), and the match cannot tell which row a mention credits; a passing mention (`"F5/F8 family"`) also counts as naming. Deflating: the stated recipe reads `--json comments`, which omits the PR body and review bodies — adding `--json body,reviews` raises PR #2 from 7/50 to 9/50 (ids `D1`, `X1`), for **36/192** on the fuller surface. The figure is therefore **not a bound in either direction**; both readings leave most rows unresolvable, which is the debt it supports |
| Review yield decays **by** roughly one-third per pass across successive review passes — the prior behind ADR-006 §3's next-pass ruling. **The mandate rests on the owner's ruling (risk appetite), never on this figure**: the stop default is set by the owner and revised by measurement, and the figure is one illustrative input to a continuation argument beside the measured curves in the row below, never a threshold | ADR-006 §3 (next-pass ruling; illustrative only) | recollection-grade | Owner testimony, 2026-08-18, during the conversation that produced [issue #48](https://github.com/Grimblaz-and-Friends/tradecraft/issues/48): the pattern recurred "many times" across prior review practice, reported as about a one-third drop per pass, and was never captured well — the capture failure the amendment fixes going forward |
| Measured per-pass yield curves. **#42** (design artifact, three passes): 9 sustained highs at a four-seat panel with a judge, then 4 sustained highs at a one-seat post-fix cycle terminating at defense, then 1 sustained high at a one-seat look with **defense waived** — consistent in direction for that artifact and steeper than the prior in this unit, but two distinct widths and three terminal filters, so the three numbers are not one measurement. **The unit matters and the record keeps them apart**: in items the same passes ran 39 → 21 → 15, which is *not* steeper than the prior at the last step. **Implementation reviews**: every panel→post-fix pair in the ledger is a curve, and the count is deliberately left as a query rather than a figure — it grows each time a review writes its own rows, and a literal here was wrong twice in one day for exactly that reason. Both decaying pairs (pr19 6→2, pr6 5→1, pr3 4→1) and non-decaying ones (pr30 0→0, pr13 0→1) are present, so decay is the tendency and not a law | ADR-006 §3 (next-pass ruling; illustrative only) | repo-verifiable | #42's per-pass counts are in that issue's review-record, post-fix-record and revision-5 comments ([issue #42](https://github.com/Grimblaz-and-Friends/tradecraft/issues/42)), its item counts in that issue's revision-5 erratum `QF12`; the repository is public, so both are checkable today. **Two instruments, and which covers which:** a review of tracked files writes ledger rows, so its curves are a group-by — `python -c "import json,collections;rows=[json.loads(l) for l in open('docs/ledger.jsonl',encoding='utf-8') if l.strip()];src=collections.Counter(r['source'] for r in rows);hi=collections.Counter(r['source'] for r in rows if r['severity']=='high');print(sorted((s,hi[s]) for s in src))"` — which lists **every** source with its high count, including the zero-high passes a highs-only filter would hide, ordered within a PR by the stage token in the source name (`panel`/`panel-review` before `postfix` before `postfix-prosecution`), since `ref` cannot order them: most pairs share one `ref` or carry no comment id. A review of an issue-hosted artifact writes no ledger rows ([#46](https://github.com/Grimblaz-and-Friends/tradecraft/issues/46)), so its curve is read from the terminal reports each pass now records |
| A requirement whose instrument cannot express the finding it tests for has blocked work here **more than once**, and the earlier instances were not captured as evidence when they happened. **The count is unenumerated and no instance beyond the one below is named**, because none is recoverable: the owner reports having seen others and no artifact was written at the time. **Corroboration only** — D-69 rests on the checkable instance, not on this row, per this registry's rule that nothing recollection-grade may be a mandate's sole support | D-69 (trial-road instrument and exception) | recollection-grade | Owner testimony given in conversation while affirming [#67](https://github.com/Grimblaz-and-Friends/tradecraft/issues/67) on 2026-08-18. **The utterance itself has no durable record** — that is what makes this recollection-grade. What the affirmation record at [issues/67#issuecomment-5336705518](https://github.com/Grimblaz-and-Friends/tradecraft/issues/67#issuecomment-5336705518) carries is a third-person paraphrase written the same day: *"the owner stated that they have seen other instances of this class before, and that it was not captured as evidence at the time."* Cited as the paraphrase's home, never as a transcript. **The checkable instance this row corroborates** is the failed admission on [#28](https://github.com/Grimblaz-and-Friends/tradecraft/issues/28) ([artifact comment](https://github.com/Grimblaz-and-Friends/tradecraft/issues/28#issuecomment-5336236201), `D0` and `D6`), which is issue-linked and stands on its own. The three prior instances of the wider class named in `ADR-002-material-lifecycle.md` — [PR #6](https://github.com/Grimblaz-and-Friends/tradecraft/pull/6), [PR #12](https://github.com/Grimblaz-and-Friends/tradecraft/pull/12), the lifecycle above both — are a **different** record and are not what this row carries. Upgrades in place if any instance later gains a checkable artifact |

Rules going forward: a new constitutional claim — a statute rule or a log entry — either links its record inline or adds a row here at the grade it honestly holds; a recollection-grade claim that later gains a record upgrades its row; a row whose record turns out not to reach its grade is **downgraded in place, splitting the row** so each grade covers only the claims it holds, and the mandates that leaned on the demoted half are named in the same commit; and nothing recollection-grade may be the sole support for a mandate — the rule this registry enforces, distilled from ADR-009's stance that predecessor practice carries no presumption of correctness.
