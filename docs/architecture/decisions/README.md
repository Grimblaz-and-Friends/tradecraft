# The decision log

One file per decision, named `D-<N>-YYYY-MM-DD-<slug>.md` where `<N>` is the pull request that landed it. An entry is written in the PR that lands a choice a future session would otherwise re-derive or unknowingly undo, and is frozen on landing but for a moved reference, below.

**Decisions inform, never bind.** An entry records what was known and rejected when the choice was made; it is superseded by being read, not obeyed, and is never a citation against change. A rule or skill line may cite its entry as `[D-N]`; follow the citation before changing what it governs, then supersede knowingly.

**Frozen, and the one edit that is not.** Reversal is by a new entry, never by editing an old one — the claim-changing edit stays barred. One edit that changes no claim is lawful [D-135]:

- **Repoint a reference whose target moved**, to its current location; nothing else moves. A sentence that quotes or characterises its target is not a bare locator, so **where repointing would leave that sentence untrue of the target at its new home, the reference stays as it is** — and where the sentence survives the move, repoint it.
- **The mover pays** — the repair rides with a change that moves the target, covering every reference to it whichever move stranded it, never as a change of its own. Moving a target and rewriting what an entry quotes is one change here: split, they leave a live path over a false quotation, worse than the dead path it replaces.
- **Nothing left to repoint** — the target retired, or the move would falsify the sentence — record the reference in `tools/lint.py` with its reason. The only one of these that may grow, and what keeps the check from reddening with no lawful answer.
- **D-53–D-69 are inside this permission**: a frozen archive is non-binding, not unrepairable. Nothing sends a reader there to act, which is why their dead links were left, not why they had to be.
- **Pin as `` `path` at `<sha>` ``**, at authoring time only — a pin added later adds text. Dropping the backticks to dodge the check is not an answer; it makes the entry vaguer about where things live, which is what the check protects.

`tools/lint.py` carries the lawful forms and fires the permission while it is still lawful. It reports a recorded reference that has come back to life, so the record cannot rot; that the record only ever *shrinks* is held by the suite, which pins its membership.

Entries D-53 through D-69 were written under the pre-reset statute's fixed skeleton and are part of the frozen archive ([D-74](D-74-2026-08-19-constitutional-reset.md)); decisions before the log existed are in the [frozen ADRs](../adr/README.md).

| Entry | Decision |
| --- | --- |
| [D-53](D-53-2026-08-18-log-and-statute.md) | Split the constitution into a decision log and a statute; freeze the nine ADRs |
| [D-59](D-59-2026-08-18-work-prose.md) | `artifact` gains `work-prose` in the ledger vocabulary |
| [D-61](D-61-2026-08-18-decision-surfacing-and-the-attended-seam.md) | Decisions put to the owner carry argued options; the opening seam's question becomes attended-only |
| [D-69](D-69-2026-08-18-trial-instrument-and-exception.md) | The trial road's falsifier and evidence stop being ledger-only |
| [D-74](D-74-2026-08-19-constitutional-reset.md) | **The constitutional reset**: three content homes, purpose-anchored review, records as exhaust; the statute and its machinery become a frozen archive |
| [D-77](D-77-2026-08-19-owner-approval-admission-path.md) | The admission road gains a second path: an agent-proposed rule is admitted by an incident from real work, or by the owner's specific approval of that rule |
| [D-78](D-78-2026-08-19-carry-the-reasons.md) | Four reasons restored to the rules they govern; migrations must carry reasons or name the drop; review dispatches carry the charter verbatim |
| [D-80](D-80-2026-08-19-spikes.md) | Spikes: test a premise before you assert it — one named premise, a throwaway that commits nothing, reported whether it held, fell, or was abandoned |
| [D-81](D-81-2026-08-19-doctrine-callout.md) | The doctrine callout is a CI label and comment, not `CODEOWNERS`, which cannot request a review from the PR's own author |
| [D-90](D-90-2026-08-20-dispatch-contract.md) | Every dispatch is a shared block with nothing before it, carrying the assignment — byte-identical across recipients dispatched against the same diff base — and every predecessor stage's output whole, both verbatim or by link, never the dispatcher's paraphrase; then a labeled additive note; last the recipient's own identity, output format, skill-worded lens brief, and working location. The terminal stage's docket is set by rule, not by the dispatcher |
| [D-96](D-96-2026-08-20-post-fix-terminus.md) | The post-fix cycle ends when a look sustains no high it will fix whose remedy changes the artifact; a finding whose remedy is not a change to the artifact is recorded and routed, never buying a further round; the mandatory floor names its exemption to match — every fix batch but the one a terminating ruling orders. Until then the cycle continues without asking, so round one's ruling names the cycle budget — and exceeding it puts the review to the owner in that cycle's ruling rather than ending it |
| [D-97](D-97-2026-08-21-dispatch-contract-restated.md) | The dispatch contract restated rather than amended a sixth time: the fidelity clause leads its pair and byte-identity stands alone, so `both` and `itself` are gone rather than replaced. Position zero is absolute but for a byte-identical title line; part one's closure is scoped to prose that aims attention rather than to arity; the fronted `in this skill's own words` stops at a sentence boundary so it cannot reach `output format`; the note bar binds seat and post-fix prosecution dispatches with a reason that fits them. The restatement held and three of the four gap closures bolted onto it did not — bundling decisions into one sentence is what produced five versions in the first place. The post-hoc guard is held, reopening on a dispatch breaking any clause of the paragraph |
| [D-99](D-99-2026-08-21-dispatch-prompt-caching.md) | The dispatch prompt is not cacheable at any size once one byte differs per seat: the harness's post-system breakpoint sits at the end of the whole request, so any per-seat discriminator pushed into the request collapses the hit — cross-seat sharing survives only under a byte-identical dispatch with a *pulled* discriminator, measured, rejected on price at ~0.3% of a review, and filed as #98 |
| [D-102](D-102-2026-08-21-merged-list-is-an-index.md) | The merged list indexes the seat reports and never replaces, edits or restates them: the terminal stage's docket carries anything in a seat's report that no merged finding carries — a remedy, a dissent, an examination that declined to file — with one ruling per merged finding and per uncarried seat entry, and content the merge adds that no seat wrote is labeled as the merge's own. The merge keeps the pen and loses the authority — the defect is a summary standing in for its source, not the author's interest, so a clerk would not retire it |
| [D-104](D-104-2026-08-22-engagement-cell.md) | A fourth cell, `engagement`, for how a session and the owner work together — it owns the pre-implementation artifact whole, on the precedent that `adversarial-review` owns its own final report. The doctrine's admission ladder gains its missing rung (a new cell where none fits) and the burden flips from creating to cramming. The plain brief — a `**In plain terms:**` blockquote atop every surface the owner enters, bounded by a disqualifier rather than a word count, accountable to what sits beneath it rather than authoritative over it. `authoring` is restored to governing documents; spikes moves with a recorded graduation condition |
| [D-107](D-107-2026-08-22-price-is-implementation-cost.md) | A remedy's price is the cost of implementing it — what the fix touches and what it obliges later sessions to do, with a removed **rule or guard** pricing negative — never the cost of standing prose existing, whose removal the narrowed clause no longer credits, the reading that priced remedies by word count through PR #102's review and, upstream of every ruling, through its affirmed acceptance criteria. The terminal stage gains a third disposition, **sustain and route**, for a finding real and worth doing in another change, discriminated by the test that a price-out holds in every future change while circumstance routes; the disposition reaches `:75`'s external pass and `:71`'s never-priced-away floor, and `:71`'s own overloaded `routed` is removed. Size keeps its two homes and gains no third axis; acceptance criteria state what must be true of the result, not what it may cost, and a cost estimate sits outside the numbered criteria |
| [D-113](D-113-2026-08-22-component-sign-and-purpose-statement.md) | The remedy price's sign goes back to being a property of one component rather than a predicate on the remedy: `:65` states the price **in two terms** and hangs the removal credit off the obligation term as an appositive, so a deletion with a large regression surface no longer reads as free. A restatement rather than the proposed qualifier, because an external reviewer produced the total-sign reading unprompted and `authoring`'s no-armor rule directs a reader who genuinely errs to a restatement; an appositive rather than a relative pronoun, because nearest-antecedent binding is what cost `:41` five versions. D-107's `{rule, guard}` narrowing is restated here, since the sentence carrying it is gone. Separately, `:14` defines **purpose statement** as the artifact's stated purpose, audience and success criteria — the sense `:16` already used and nothing stated — so `:41`'s assignment and `:79`'s report resolve from one definition and the repo's most-amended paragraph is not opened |
| [D-114](D-114-2026-08-22-convergence-is-interactive.md) | Convergence is interactive: the pre-implementation artifact is drafted and settled with the owner in conversation and only the settled version is posted — the record of what was agreed, never where he first reads it — and with the affirmation recorded he is next needed at PR review. Affirmed, the artifact is the handoff contract, which is also the test for whether it is finished: nothing load-bearing left in chat, so a fresh session or another runtime builds the affirmed thing from it alone. The doctrine carries the ordering; `engagement` carries the completeness bar and what "ready for his review" delivers |
| [D-119](D-119-2026-08-23-cost-estimate-outside-the-artifact.md) | A cost estimate belongs outside the pre-implementation artifact entirely, not merely outside its numbered criteria: the artifact reaches every stage of the review whole, so partitioning it by typographic position moved the figure without moving what reads it. The reason clause states delivery rather than placement, and every surviving home — PR body, decision entry, the review's report — is written against a tree that exists, so the forecast bar comes free and no second rule is added. Picked up with #115 and #117 as a group; a spike falsified the premise both rested on — zero word counts across four cold seats in both arms — so they were closed rather than built, and the defect the spike actually found, an obligation term carrying one sign where a remedy releases one obligation and creates another, was filed as #121 |
| [D-128](D-128-2026-08-23-filing-cell.md) | Filing gets a cell of its own, `skills/filing`, on `authoring`'s own independent-trigger test — filing fires mid-review and mid-implementation, never while a governing document is being written, and hosting it would turn `authoring`'s success criterion into a disjunction over two unlike jobs. A filing searches the board first, in **every state**, and takes one of three lawful outcomes: extend an **open** issue, file with named ties, or file standalone. Four tie relationships (six verb names, the two paired ones written in both directions), closed under the test that a tie name earns its place by changing what a ranking does with the pair; a fifth for closed siblings was rejected against it. No filing states that it searched — the ties are the search's artifact and a compliance sentence is the 0/8 shape #94 records — and for the same reason the "Minimal filing" announcement retires while provenance stays. Creation carries the want, its evidence, why it will be picked up, its ties and the deferred questions; framing, options, remedy design and the artifact are pickup's. The boundary is a kind rather than a quantity — record what happened, do not decide what to do — with a floor that refuses a bare title. An issue template was considered first per the admission ladder and rejected: `gh issue create --title/--body` applies none, so it never reaches the party it would bind |
| [D-127](D-127-2026-08-23-fork-test-and-argued-ask.md) | A decision is the owner's when it is a genuine fork — what turns on the pick is something he would care about, and undoing a wrong pick costs him something an edit does not undo — and everything else the session decides and reports afterward with its reason, asking where no fork exists being a fabricated gate rather than politeness. The argued form travels with the test out of `Review`, where it had been scoped to findings while needing to govern every ask, into `Authority` beside the plain brief it sits on top of. Both halves were drafted wrong and fixed under review: the cost half measured the edit, under which a command name he types daily is a one-line revert and falls to the session — the paradigm class the issue was filed about; and the first half said *the live options differ*, a plural predicate one live option cannot satisfy, which returned every forced single-route decision to the session. `skills/engagement` carries the calibration, the single-live-option case, the fabricated-gate tell keyed on nothing turning on his answer, and the report-with-reason shape, because the budget held the test and not the examples; it also loses a dangling reference a consumer could not resolve. The class `adversarial-review` invokes but cannot define is routed to #129 |
| [D-131](D-131-2026-08-23-experience-session.md) | The **experience session**: a chartered, time-boxed use of the real result, in three pieces — charter, time-box, session note — and nothing more, since a fourth piece is the counter-bureaucracy failure the design exists to refuse. One duty, discovery — the unknown-unknowns a review structurally cannot reach; arbitration was a second duty in the affirmed artifact and the owner deleted it after the review closed, because D-132 landed the same day and `skills/spikes` now covers testing a candidate wording with a better trigger, leaving one instrument each side of the assertion. Its own cell on `authoring`'s independent-trigger test — the trigger fires when a change to how a later session must work has just been built, with no artifact being written and no review running — with `engagement` rejected because its purpose is the surfaces the owner enters. Root doctrine's flow line names it, on the owner's ruling that an instrument left to fire on a skill description alone does not fire; the clause carries the decline path in the same breath, so it states an order of work rather than a condition of merge. Its own text was spiked and held — and the experience session run on the built cell then found seven things the spike had missed, two of them contradictions, which is the entry's sharpest evidence and cuts against the spike as much as for it |
| [D-132](D-132-2026-08-23-spikes-graduate.md) | Spikes graduate to `skills/spikes`, and the trigger stops being keyed to the pre-implementation artifact: it fires on any load-bearing premise — an artifact's claim, a review thesis still disputed after a round, a candidate rule wording, a question arising while building — with governing prose named as a mechanism whose executor is a reader. Its own cell rather than a broadened reference, because the lint forbids any path reference between skills, so a file under `engagement` is reachable from a review seat only by URL or by name and a cell's frontmatter description is the handle a routing session actually has. Four wording changes came from the run rather than the issue, and the first of them is a correction: `a mechanism nobody has executed` contradicted D-80:15's own recorded predicate, which keys it to what the session can consult. Cold-seat A/B is named as the house pattern with its four properties; an index is declined because a spike commits nothing and so could never append to one, and the report's `# Spike report —` opening carries the findability instead |
| [D-135](D-135-2026-08-23-repointed-reference.md) | The freeze admits one edit — repointing a reference whose target moved, by the change that moves it, where the surrounding sentence survives — and a lint check fires the permission on the mover's own PR |
| [D-136](D-136-2026-08-23-index-dispositions-and-staffing.md) | The review row carries what came of the findings — the terminal stage's own four dispositions, dismissals included because they are the only measure of noise — and the model and runtime that staffed the review; required of every row appended after the twenty that existed at landing, gated on position because a gate on a date the author types is one a typo opts out of |
| [D-141](D-141-2026-08-23-figures-are-derived.md) | A write-up's figures are derived, not recalled: the general engine (suite count, doc size vs budget, delta vs an explicit base, each figure printed with its basis and tree) ships in `skills/authoring` because the failure is practice-general, while `tools/figures.py` applies it here by importing the budget and the census resolution from the lint so figure and guard cannot drift apart. No basis is ever picked silently — the fixed ones are printed, the delta's base is a required argument. Discovery rides the skill's extended trigger at zero doctrine cost; a reproduce-or-fail CI gate and guard migration into the shipped zone were both raised at convergence and declined by the owner |
| [D-153](D-153-2026-08-24-consequence-field-and-the-batching-cut.md) | A finding names the concrete consequence of standing unfixed, and consequence shape — where that consequence lands, on the artifact or on the record of having reviewed it — is read from the site the finding cites. The report carries the split by that axis, which is the instrument #122's self-referential-supply failure mode asks for and nothing could read. The post-fix look is bounded to the fix diff, whatever depends on it, and each sustained high's territory, and the next-pass ruling gains a **relevance terminus** — a pass continues only while its expected findings are about the artifact — because the first full-pipeline field run found post-fix cycles producing real, correctly-arbitrated defects on a subject that had drifted to the record of reviewing. **The batching mechanism the change was built around was measured and cut before merge**: classing at the merge, one price and one ruling per class, a per-item exclusion and a severance duty. Five stages and a cold experience session each formed zero classes, because the exclusion held everything artifact-facing and severance's record limb reached the rest, this repo's records all freezing by doctrine; measured yield was zero rulings of twenty-six, or one under a rejected reading. The owner cut it rather than ship it dormant, so [D-102]'s per-finding ruling unit stands byte-unchanged and this entry supersedes nothing; width is untouched by his standing ruling that experience comes before narrowing |
| [D-155](D-155-2026-08-24-measured-figure-lawful-in-the-artifact.md) | The pre-implementation artifact's figure rule is re-cut against the tree: its reason clause narrows from `any figure` to **a forecast** while the prohibition widens from a cost estimate to any forecast — a figure about a tree that does not exist yet stays outside the artifact, while a figure measured on a tree that exists is lawful in it and travels with the query or derivation that produced it, so a seat verifies rather than inherits. D-119's own argument is the discriminator it did not write down — every surviving home *is written against a tree that exists*; the forecast the owner struck stays struck. Carrying evidence figures by link was argued at convergence and declined. Three fixes travel with it, all from one cold seat on one job: the artifact carries an **open fork** rather than closing it, with the silent close named as the mirror of the cell's existing tripwire against asking where no fork exists; the **draft** a session holds for nearly all of the work is named and given its bar; and report-versus-perform is settled by scoping to the surface this cell owns — the artifact carries what came back rather than the fact that you looked — rather than by restating `filing` or `spikes`, which is #130 §4's one-standard-one-owner shape. The diff-checkability sentence #144 owns is byte-identical to `main`, with the seat's independent reproduction of #144's thesis recorded on #152 instead |
| [D-156](D-156-2026-08-24-installable-plugin-and-shipped-charter.md) | The practice ships as an installable plugin: the binding half of the doctrine moves into a skill cell, `skills/charter/SKILL.md`, which a `SessionStart` hook emits into every session of a repository that installs the plugin and which a session can also pull by name where the hook does not reach — Codex on Windows, a declined hook, a missing `python`. One file, two doors; `hooks/` joins the shipped zone. A shipped calling contract names no harness token — and the reason is cross-runtime, not that the token fails: Claude Code substitutes `${CLAUDE_PLUGIN_ROOT}` and `${CLAUDE_SKILL_DIR}` into a skill body before the model reads it, Codex substitutes neither and instead ships the relative-resolution rule as system prompt, so a token-bearing contract binds in one runtime and is dead in the other. The change's original premise — that the old contract was simply broken — was false for Claude Code and was corrected under review. Delivery is guarded by executing the declared hook command and comparing its output to the charter byte for byte, because `hooks.json` names the emitter and the emitter names the charter, and a guard resolving each rung would still fail an emitter that lawfully computes its path. A Claude-only forced output style was refused on runtime-neutrality grounds, and `commands/` after it — Codex converts commands to skills at install and silently drops any render over 4,000 bytes, which the charter exceeds |
| [D-166](D-166-2026-08-25-criteria-name-consumer-behavior.md) | Acceptance criteria state **consumer behavior** — what whoever works under the built result can do, or no longer does wrongly — and a criterion about the artifact conforming is lawful only where it argues its own consumer-facing load. The diff-checkability sentence narrows rather than repeals: **each criterion names the check that would falsify it**, the diff for what the diff settles, a command for what a command runs, and for what only use shows a session that has none of the change's history, or the built result in use; *a criterion nobody can check is a wish* survives verbatim. A criterion a plausible wrong implementation would also pass measures nothing, and one held across an open fork must be falsifiable on every branch it survives — the second surface **PR #155's terminal ruling routed here as N8**, [D-155] having ceded only the diff-checkability sentence. Warranted by a cold-seat A/B run during drafting: the old sentence reproduced PR #132's fifteen-criteria conformance list from a standing start, and clause (a) against twelve of that PR's own findings dismissed 5 of 12 under it against 8 of 12 under the new one, the delta being three findings its Arm A conformance criteria 13 and 14 had ratified — **two record-accuracy and one, D23, against shipped prose**, so of five shipped-prose findings on the docket Arm A kept four and Arm B three, both dismissing D20. The executable class survived both arms; **the narrowing's measured cost is that one shipped-prose finding**, caught by this PR's own review panel rather than by the A/B, and the entry's Deferred watch is open on arrival because of it. **The review is byte-identical to `main`** — the filter gained teeth with no new machinery, which is the measurement's whole point. Naming a check does not order it run: the owner ruled that fork after the arm priced itself at eight cold dockets per change, leaving run duty with `experience-session` and the flow line. Count guidance declined — the shape rule cut counts on its own, 15→12 and 9→7 |
| [D-167](D-167-2026-08-25-behavior-claims-carry-a-probe.md) | A behavior claim — what a reader does under a wording, whether a mechanism fires, what using the result is like — is a hypothesis until a probe answers it, and the obligation binds the defense and the ruling rather than the finder alone: a defense contesting behavior commissions the probe, and no ruling settles such a claim on argument alone where a probe is cheap. It reaches **severity** wherever the harm claim is behavioral — the channel #138 measured as the one that discriminates — while the executable class is excluded because its highs arrive with their probe. A probe must show it answers the question it is credited with, by a negative control **drawn from the probe's own class** — a control outside that class can pass while the probe stays masked — because a probe that reports a result it never took is *trusted* where argument is not. A dispute **the terminal stage cannot itself arbitrate cheaply** is routed to a spike as a species of `routed` rather than a fifth disposition value, run by the dispatching session before it closes the review or recorded why not; the round-one wording deferred it past the final ruling and the review falsified that — routing is itself a sustained disposition, so no compliant ruling existed, and five of five cold judges routed rather than probed. `spikes` closes [D-132]'s coverage exception for a carrier that is posted rather than reviewed. A session note reaches the seats through the assignment before it is given standing, and that standing is on behavior and nothing else. **#121 is dispositioned by the owner: the sign is dropped**, the term standing as criterion 10 affirmed it — the appositive deleted, nothing in its place. Round one widened the noun instead and the review reverted it: the widened form stated no reach where the appositive carried `{rule, guard}`, and the criterion's own next sentence had already granted what the widening was reaching for. The removal credit weakens and the entry says so rather than netting it away. **The trigger rewrite the artifact's criterion 7 required was not shipped**: the cold-seat run found the premise fell — four of four seats, both arms, routed the mid-review dispute to a spike without once deciding it on the sentence under test — so D-132's routed seam is answered in the opposite direction from the one it recorded |
| [D-169](D-169-2026-08-25-charter-exemption-and-cell-standards.md) | The charter is exempt from self-containment **in both directions** — any cell may name it, it may name any cell, no cell may name another — because it is already always-on in every session, so a citation costs no loading and cannot drift the way a second copy can, and the exemption is one target at depth one rather than a mesh. Its inheritance of the rule was a side effect of D-156 moving the binding half into a cell, and it cost eight rules their pointer to the cell owning their depth; two reached for periphrasis and six pointed nowhere. The exemption is the **name form only** — a rooted `skills/` path is dead once installed, which a portability test caught after the first design exempted every form — and it is held by guards rather than prose: `check_sideways_deps` learns it and now reads the name form as coupling, which it had documented as unlinted, while `check_cell_references` fails a name no cell answers to. `authoring` gains what a cell owes the roster — the description as the whole always-loaded triggering surface with the frontmatter guard as its floor, depth leaving the body on a disqualifier, standards for the code a cell carries, one owner for a shared standard — and sheds its own depth to stay the size it was, the tree's first `references/` files. The rule was tested on the change itself: `engagement` stopped keeping a synced copy of the fork test and cites the charter instead, and two of the new bullets were caught half-owning the portability rule. **The description standard's cold-seat A/B did not resolve** — twelve seats, three arms including one with the trigger apparatus removed, all identical, so the probe failed its own negative control; the warrant is anticipatory and #89's roster-width condition is what would settle it |
| [D-173](D-173-2026-08-25-declared-assumption-is-a-statement.md) | **A declared assumption is a statement, never a promise** — the falsifier stays required because it keeps the assumption honest and any future test one dispatch away, and no one owes a return visit; reopening on an incident where work rested on a false declared assumption whose falsifier sat unused, on which a successor owning the booking is the shape to argue first. **The ruling ships no rule sentence**, and the reasoning is the recorded part: a session unsure whether it owes a return visit resolves that by not making one, so the default under uncertainty is already the ruled behaviour — the asymmetry being that a booking ruling would have made a sentence mandatory, since silence can discharge only the ruling that nothing is owed. A concrete sentence was drafted, put to the owner against the instrumental-falsifier reading and #152's absent-content precedent, and declined; the entry-alone option was the drafting session's own first recommendation and was corrected before affirmation, because the party who would unknowingly undo the ruling is looking at `skills/spikes/SKILL.md:28` and not at the log. So the entry is cited from that line, and **the citation's cost is accepted rather than argued away** — it is the cell's first and only marker, on a line whose wording this entry did not change, while D-80 and D-132 which wrote the cell go uncited, which inverts the signal; the backfill that would answer it is record bookkeeping. Riding with it, two corrections from the instruments' first runs, both one dispatcher on one run and both self-caught late: `experience-session` makes **the material as this change leaves it** checked rather than assumed, placed in the paragraph that owns that phrase rather than where isolation is described, because the failure is about what the consumer receives, not about the mechanism that delivered it; and `spikes` puts the docket's actor condition on the existing docket property rather than adding a fifth, since a docket no seat can enter through the rule's own door is not a realistic one. No fourth piece, no register of open assumptions, no cold-seat A/B on either wording — each states a check the dispatcher performs rather than a judgment a reader interprets |
| [D-178](D-178-2026-08-25-fix-batch-buys-a-second-run.md) | **A review's fix batch buys a second use of the material where it rewrote what that material instructs** — once, on the last tree the review's fixes produce, before the review closes; a batch that changed only the record of the change is the mechanical case the cell already carried and owes nothing, not even the decline line. The bound is the discriminator #138's cold-consumer run measured: fixes to an instruction a job can traverse transferred to behaviour, fixes to register, provenance phrasing and record accuracy reached the consumer not at all. Over the fix-batch commits of PRs #166, #167, #169 and #173 it fires on three and stays silent on #173, whose panel ordered zero edits to either shipped cell and thirteen to the frozen record. **A second occasion, not a relocation — the owner's fork:** moving the single run to the post-fix tree would cost nothing and still test what merges, and was rejected because it deletes the panel's only real-use input, the note #125 gave standing and #167 spent a fork keeping, and lands every real-use finding after the terminal ruling. The trigger goes in the **description** and, after this change's own experience session found two of two consumers reasoning from the doctrine instead, in `AGENTS.md`'s flow line — the boundary had declined that edit on a budget claim the tree falsified (5,659 of 8,000); `skills/adversarial-review` is byte-identical to `main`. **Aiming the job is lawful and saying so in the dispatch is not** — the steer goes in the note; docket authorship stays with the interested session, recorded rather than solved, which is #172's seam 1 from the other side. Riding with it, **#174**: the cell told a dispatcher to withhold that anything is under test and then named a worktree or a clone, both of which hand over the branch name and a commit log that by this moment states the sentences under test outright; one `git archive` procedure replaces both recommendations and the contradicting opening clause is deleted rather than qualified, probed in both shells on this branch. No docket, no seat count, no arms — the cell's own no-required-count rule settles staffing without a sentence |
| [D-179](D-179-2026-08-25-counting-convention-and-facing.md) | The review row states its counting convention once where its fields are defined — every count sums across every cycle, the unit is the finding as originated and dispositioned, a seat's `merged` counts that seat's findings surviving the merge, and a seat key may book an aggregate that names what it aggregates — closing the `merged` column D-136 dropped for want of a vehicle. The row gains **`facing`**, the split by consequence shape over the same rulings `dispositions` counts, with a lint cross-total that is sound where the seat-count one is not; it grandfathers at its own position constant so no row between the two boundaries is silently un-obliged. Instrument firing gets a **recorded query and a reopen condition and no mechanism**: of the twelve PRs numbered above the one that landed the instrument, eleven carry a session note or a decline line and the twelfth merged before it, so twelve of twelve are accounted for with no observed skip, and the only skip on the record is a spike seat rather than an incident in real work |
| [D-184](D-184-2026-08-25-outflow-and-the-always-on-total.md) | The always-on surface — `AGENTS.md`, `CLAUDE.md`, the charter's body, and every cell's name and description — becomes **one derived figure reported for two audiences**, because no invocation reported the sum and so both prior restructures moved prose between always-on artifacts and reported the file they emptied: 11,590 at `3175369` when filed, 15,075 at `b9bcdeb` after the charter split, 15,602 at the merge base. The two audiences are different sets — a plugin's root `AGENTS.md` and `CLAUDE.md` reach a consumer's cache inert and are never loaded, so an adopter reads 9,747 of this practice's material where this repository reads 15,505. **Outflow is owed whenever an always-on surface is edited rather than when a budget is reached** — the admission order read backwards, three moves, never dropping a rule, and measuring the whole surface rather than the file being edited. Running it moved one passage to its citation and **declined another on reading D-78**, which had put that clause in the doctrine deliberately: an audit that never declines is not applying the rule. Both ceilings ratchet to measured (`AGENTS.md` 8,000→6,000, charter 6,000→5,600), each margin argued at its constant. `authoring`'s body cap becomes a guard — it was #169's own evidence that depth-shedding is applicable and was enforced by nothing, living in a command string inside a frozen entry; it bit twice while this change was written. Cells absent from the budget map are unbudgeted on purpose, because a number chosen for `adversarial-review` would be a ruling on its size arriving as a constant (#177). #164 discharged; a roster aggregate ceiling and per-cell budgets for every cell both rejected |
| [D-185](D-185-2026-08-25-finders-of-each-sustained-high.md) | The final report names **each sustained high's finders** — every credited seat as the merge records them, primary first and a merge-added co-finder labeled as its own; or the stage or seat that originated it where no merge carried it; and where a merge carried it with no finder list to copy, that fact. The primary tag and the merge-own label are what let the new corpus be set against the ledger's primary-only one, which is the disagreement the recomputation exists to settle. The width claim at `:23`/`:26` still rests entirely on the predecessor's six rounds, and [#1](https://github.com/Grimblaz-and-Friends/tradecraft/issues/1) answered that objection with a mechanism — `found_by` on every ledger row, so *"seats here retire by evidence"* — which froze at the reset while `docs/reviews.jsonl` replaced it with per-seat counts. Run against the record, the claim answers only to within a bracket: of 92 distinct sustained highs across 27 reviews, 59 are round-one panel findings and **36 of those (61%) name no finder anywhere**; of the 23 that do, the best fixed pair retains 21, where the same fixed-policy computation on the ledger's primary-only attribution puts that pair at 32 of 64 — each end biased toward its own conclusion, and the 36 unrecoverable. Two routine-lane rows in the attributed set forced their own sole-finding and carry no width information; excluded, the two candidate pairs **tie at 19 of 21**, so the sample separates nothing and width stays undecided. `:64` already obliges the merge to credit every finder and the two merged lists ever published carry exactly that, so the datum is produced every review and thrown away, which is the predecessor's failure reproduced verbatim. Scoped to highs because the class below them is the instrument-intrinsic ~88% and new obligations belong off the emission side [#143]; placed in the report rather than `docs/reviews.jsonl` because rows freeze and PR #167 priced out an index limb on that ground — a different column, so only the ground transfers. `:86` gains one sentence naming what the existing `high` column counts, because the clause supplied a warrant for reading it all-finders and a cold consumer followed it there on first use; the column carries credits, never distinct highs. **Width is not decided** — #138 stays open as the vehicle for a recomputation after six reviews have published finder data, the trigger living on the issue because a rule obliging a return visit is governing prose bought for a one-time job [D-173], with `:26` carrying a [D-185] marker so the line's own reader finds the contest |
| [D-186](D-186-2026-08-25-windows-text-mode-defaults.md) | Python's Windows text-mode defaults are decided once, as four rulings. **The encoding half takes two complementary rules, not one** — machine-read output stays ASCII, *and* a stream is set to UTF-8 before anything is written to it, guarded respectively by a literal check and by a call-site check that reads the first statement of `main()`. The original ruling had only the first and rejected the second as unguardable; the review falsified the premise under that argument, because **the two protect disjoint things and were never alternatives** — the rule reaches source literals and the helper reaches the stream, which is the only place a path handed over by `git` or chosen by a consumer can be protected. Probed: a shipped file whose name carried an em dash put #147's own cp1252 byte `0x97` back on stdout from the change that claimed to close it, one with an acute accent put `0xE9` there, and one outside cp1252 did not garble but *raised*, killing the report before the offending path printed. The guardability objection is answered rather than accepted — *was the helper called on this path* is reachability and undecidable, but **the first statement of `main()` is a position, and a position is exact**, with ordering load-bearing because a call after `parse_args` is one `--help` has outrun. So `lib/` is created and the deferred zone question is answered. **A docstring is not exempt when argparse pipes it**: three scripts, one shipped, emitted the same byte on `--help`, and the construction is now banned outright. **The newline half stays prose with no mechanism**, consciously against #137, on a warrant stated as narrower than [#163](https://github.com/Grimblaz-and-Friends/tradecraft/issues/163) claimed — the validator flips only in conjunction with an unquoted `: ` in a value, of which the eight shipped frontmatters hold zero. **The recorded fact names the symptom rather than the reassuring command**: `git status` showing ` M` against an empty diff, which is the surprise #162's autopsy says consumed a step, not the empty diff itself. The guard's bound is stated rather than implied — it reads literals, so it flags a filename or regex source and cannot see a character built by `chr()`; eleven such constructions were probed and all eleven evaded, and **chasing them is priced out** as rebuilding the approximate check ruling 1 rejected. Found in its own review: the check walked the repository root where every sibling scopes to a zone, producing **1,941 findings across 276 files** in the owner's checkout and breaking a mandated flow step on the one surface where the flow is run, while CI stayed green |
| [D-192](D-192-2026-08-25-substrate-cell-and-the-authoring-trigger.md) | The substrate standards leave `authoring` for a **cell of their own, `substrate`**, taking `skills/substrate/references/text-mode.md` with them, because a `references/` load condition reachable only through a body that never loads for its reader is unreachable: `authoring`'s six triggers were all prose-shaped and none described *I am writing a script*, while 11 of 15 tracked `.py` files sit outside `skills/` entirely, which the ASCII guard parses in full and the stdio guard in part. The scope widens from *code a cell carries* to the code this practice ships and runs, named as a meaning change. `authoring`'s description is re-derived rather than trimmed — two subset triggers fold into the clause they belonged to and the code job becomes a non-trigger stated as a job rather than a cell — landing at 632 of 700 where the merge base had headroom 10, and the body at 6,469 of 7,359 where it had headroom 4. `substrate` is unbudgeted on purpose per D-184. The outflow produced one move and one refusal, and its price is stated rather than absorbed: a ninth name-and-description pair takes an adopter's always-on surface 9,747 to 10,253, paid because a rule nobody loads costs the same in bytes and delivers nothing. The 700-character ceiling is not re-argued in the change that resizes the one cell it binds |
| [D-193](D-193-2026-08-25-review-cell-splits-at-the-reader.md) | `adversarial-review` splits **at the reader, not the topic**: the cell's readers are not one population — a dispatching session drives the whole pipeline while each seat, the defense, the judge, and the two triage-only firings each use one stage's machinery, and on a panel cycle the dispatcher is one load of seven or more. What binds every role stays in `SKILL.md` (purpose header, charter, lane choice, evidence standards, lens promotion); each stage's machinery goes one hop behind a pointer carrying its load condition. Body **24,155 → 8,738**. **The cell got bigger and the entry says so** — total prose 27,965, a governing-prose delta of **+3,810**: what shrank is what any one reader loads, and a body-only figure would have certified this as a 64% reduction. Nothing was deleted though deletions were admitted; conservation was checked mechanically and the only meaning changes in the prose that moved are four cross-references — three that said *"below"* and one *"under *Ending the review*"* — which now name their file; the check that establishes it compares base to head and so cannot see what the change added, which is where its real meaning changes are. The split wrote the tree's first sibling pointers between depth files and `check_cell_references` **could not see them** — from inside `references/` the bare form resolves a directory too deep, and `_token_before` reads the `../` prefix as somebody else's tree, so renaming a target left lint and the suite green. The guard now resolves relative `.md` pointers landing inside the naming file's own cell, bounded there because a reference out of a cell is already two other guards' and reporting it twice prices one defect as two; **its first version filtered on a `.md` suffix and the negative arm caught it**, since `RELATIVE_REF`'s trailing class swallows the full stop ending a sentence. A body budget follows at **9,000** — about one bullet above the body it landed at, a ceiling above a measured body and not the measured body, holding the split's claim over a rewrite: [#54](https://github.com/Grimblaz-and-Friends/tradecraft/issues/54)'s rewrite took the body to 13,721 and it regrew **76.0%** to 24,155 because nothing failed when it did. A body cap is dodgeable one directory down, so `figure_cell_total` reports the cell's whole prose beside it, **deliberately unbudgeted** — a ceiling there would cap depth-shedding itself. A second rewrite, graduating the external pass to its own cell, a total ceiling, and no budget at all were all rejected |
| [D-194](D-194-2026-08-25-figures-re-derive-at-read-time.md) | **A derived figure lives only on a surface that re-derives it at read time**; one that freezes — a decision entry, a commit message, a PR body, a pre-implementation artifact, a comment beside code — states the command and the tree it runs on, never the output. #184's figure-defects split cleanly: every re-deriving surface stayed correct and every transcribing one went stale, three fix batches running, with no wrong derivation anywhere in the review. The falsifier is stated in the cell so a reviewer can name it — *which command, on which tree?* — and `derived` is the subject, so a primary observation is untouched and `adversarial-review`'s panel-width basis survives. **[D-155] item 1 is superseded knowingly**: the artifact freezes on affirmation, so a measured figure in it becomes the command that derives it. The implementing session recommended the narrower rule — state the figure, pin it to its tree, which covers every failure on the record — and **the owner ruled the ban**, on the ground that a conditional rule still permits the act that kept failing and that the condition is satisfiable sloppily, the stale PR-body block having been stamped. **The review row stops carrying arithmetic**: from a position constant it names each sustained high instead, and the three counting fields are forbidden rather than optional, since an optional field cannot catch its own omission and lets the retired shape drift back into a file nobody may edit. `staffing` survives — a model and a runtime are facts about who ran the review, not arithmetic about it. How many highs a review sustained is now the length of the list, derived at read time from the row. **The row half of #171 and #180 closes** — no row books a disposition its ruling did not produce, and none needs a shape for a ruling that reached no merge — while the half of each that lives in the report stays open; the change claimed both closed until its own review read the issues against the shipped text. Guards go only where a checkable subset exists: the row's schema is guarded and mutation-probed through the repository's own index in both directions, and the figure rule gets **no guard at all**, on [#187](https://github.com/Grimblaz-and-Friends/tradecraft/issues/187)'s own reason rather than its fate: it proposed a seat lens rather than a detector and named a guard as the wrong material, since deciding whether a sentence still describes its subject is a reader's judgement. #187 was closed on standing, which does not transfer to an owner-initiated change. This removes the supply instead. Retiring the index entirely, a PR-body exception for a stamped block, backfilling, and reaching filings ahead of #158 all rejected |
| [D-210](D-210-2026-08-26-project-roster-and-the-loaded-total.md) | The roster a session in this repository loads is **generated into `.claude/skills/`**, one entry per cell, carrying the cell's frontmatter byte for byte over a body naming the generator and pointing at the cell. Nothing installed this repository's own plugin, so no session here held any cell's name or description and every trigger routed to one -- D-141's derived-figures trigger, D-184's outflow condition, recorded there as that change's load-bearing correction -- reached every adopter and missed us: three of four cold consumers on PR #194 never reached the rule, two writing a violation into a commit message and a PR body. `figure_always_on` reported 16,241 here against **11,351 a Claude Code session actually read**, and each audience's roster is now read from the directory that audience loads it from, so the total is derived rather than asserted and deleting an entry moves it; what a Claude Code session here loads rose 11,351 -> 16,241, while a Codex session loads neither roster and stays at 11,351, the 4,890 the figure had been claiming. `check_project_roster` asks the generator for the expected bytes rather than recomputing them, on `_always_on`'s own recorded failure. The entry is a pointer rather than a copy -- one hop, exercised by a cold session that invoked `filing` from it, followed it, and applied the cell -- because copying bodies would put 78,931 characters in a second place -- every cell body at the landing tree, each file's decoded characters less its frontmatter block. Self-installing the plugin is rejected (sources cache per version, so a session here would read *published* descriptions, backwards for the sessions that edit them); relocating the cells is priced out at 120 `skills/` references inside frozen entries under mover-pays; a committed symlink is dead on the owner's machine; accepting the split was the third option at convergence and the owner picked the mechanism. No always-on artifact was edited, so no outflow was owed; no doctrine rule was added, per the admission order; `.claude` stays out of `REPO_ROOTS` with the reason narrowed rather than left standing |
| [D-225](D-225-2026-08-29-values-ranking-adopted.md) | The values ranking is **adopted and binding**, moving to `docs/values.md` out of a directory whose declared job is holding reference that binds nothing — a status line inside a file is missed exactly once, where the directory name is read every time the path is. `AGENTS.md` gains a `## Worth doing` section that **binds rather than points**: a worth-it judgment whose evidence has run out is argued against the ranking, by number. Admitted at the admission order's last-resort tier because a ranking function is the *must bind before any context loads* class that tier is reserved for, and because per-cell copies were never available — cells are self-contained and may not reference `docs/`, so the shape was one home plus one always-on line or nothing. The both-ways guard gains the recognition half #221's cold read found missing: its existing sentence reaches a **claim**, which carries its value because somebody is arguing worth as it is read, while an **open item** is read at rest by a sweep nobody is present to answer — and a values-funded item has no measurable payoff by construction, so it reads as weak and the guard never fires, which is the tilt operating on the board rather than in an argument. An item whose case rests on unmeasurable value names that value where its case is made, and **absence of a name means untagged, never *measurable***: a tag whose absence carries meaning must be applied reliably, and applied patchily a missing tag becomes a measurability claim that culls precisely the work the guard protects. The stronger shape — the obligation in the shipped `filing` cell — was declined on four grounds, decisively that the zone wall forbids a cell naming `docs/`, so an adopter who never loads `AGENTS.md` would receive an instruction to cite a numbered value against no ranking at all; it stays available on a sighting, which #221's cold read is not, the charter holding that a review finding about governing prose is no incident. The outflow took two moves and one refusal: the flow's fix-batch clause goes, `experience-session` carrying that rule in its body and its **always-loaded description** carrying the trigger, so the doctrine held a duplicate rather than a rule; the Release line's CODEOWNERS contrast compresses to the `[D-81]` already beside it, whose title is that contrast; and the CRLF clause is **refused**, because D-186 §5 records that placement as deliberate — the fact must be found mid-task, when a session notices CRLF and has no reason to open the decision log. Ranking adopted as written, amendment still the owner's alone. Charter export, allocation lanes, the worth-it check itself, and retro-tagging the open board all out of scope |
