# The decision log

One file per decision, named `D-<N>-YYYY-MM-DD-<slug>.md` where `<N>` is the pull request that landed it. An entry is written in the PR that lands a choice a future session would otherwise re-derive or unknowingly undo, and is frozen on landing but for the two narrow repairs below.

**Decisions inform, never bind.** An entry records what was known and rejected when the choice was made; it is superseded by being read, not obeyed, and is never a citation against change. A rule or skill line may cite its entry as `[D-N]`; follow the citation before changing what it governs, then supersede knowingly.

**Frozen, and the two edits that are not.** Reversal is by a new entry, never by editing an old one. Two narrow repairs are lawful — one that changes no claim, and one that repairs a claim the entry's own evidence disproves. Everything else stays barred.

**The repair that changes no claim** [D-135]:

- **Repoint a reference whose target moved**, to its current location; nothing else moves. A sentence that quotes or characterises its target is not a bare locator, so **where repointing would leave that sentence untrue of the target at its new home, the reference stays as it is** — and where the sentence survives the move, repoint it.
- **The mover pays** — the repair rides with a change that moves the target, covering every reference to it whichever move stranded it, never as a change of its own. Moving a target and rewriting what an entry quotes is one change here: split, they leave a live path over a false quotation, worse than the dead path it replaces.
- **Nothing left to repoint** — the target retired, or the move would falsify the sentence — record the reference in `tools/lint.py` with its reason. The only one of these that may grow, and what keeps the check from reddening with no lawful answer.
- **D-53–D-69 are inside this permission**: a frozen archive is non-binding, not unrepairable. Nothing sends a reader there to act, which is why their dead links were left, not why they had to be.
- **Pin as `` `path` at `<sha>` ``**, at authoring time only — a pin added later adds text. Dropping the backticks to dodge the check is not an answer; it makes the entry vaguer about where things live, which is what the check protects.

`tools/lint.py` carries the repoint forms and fires the permission while it is still lawful. It reports a recorded reference that has come back to life, so the record cannot rot; that the record only ever *shrinks* is held by the suite, which pins its membership.

**The repair that corrects a claim.** Where a sentence is falsified by evidence the entry itself cites — the test it names, the ruling it quotes, the file it points at — correcting it is lawful. This is the only claim-changing edit there is, and it is bounded four ways:

- **The falsifier must be the entry's own.** Evidence from elsewhere, or a tree that has simply moved on since, is supersession — a new entry, not a correction. The entry has to have been wrong when it landed, by what it already carried. **The pin is how that is established**: an entry's evidence is pinned to the commit it shipped at, so the cited test or file can be read at the pin and the sentence judged against the tree it was written on. Where nothing is pinned and the two cannot be told apart, it is supersession.
- **A marker where the error is, a block at the end.** The sentence keeps a short `[corrected — see Erratum]` flag, and a final `## Erratum` section, after everything else in the entry, names the sentence and the evidence that falsifies it. **It names the demonstrator rather than restating the behaviour** — the probe, test, ruling, or file a reader can consult — because a correction written as a fresh paraphrase is a fresh frozen copy, which is the defect this repair exists to end. The body is not rewritten: a reader has to be stopped at the error, and has to still be able to read the entry as what was written then. The marker is what makes this lawful under the same test the repoint uses — it changes no claim; the block beside it carries the change.
- **Judgment is out of reach.** What was decided, what was rejected, and why stay as they are. A correction repairs a description of what the tree does. It never re-decides, re-dispositions, or softens a ruling — that is reversal, and reversal is a new entry. Where that line is the question, `skills/authoring/references/frozen-documents.md` draws it at greater length under *What is irreducible stays prose*.
- **It rides with work, never alone.** The correction lands in a change that had another reason to be open. A pull request whose only content is errata is the tripwire.

**The index row moves with it**, in the same change, where the corrected sentence is what the row summarises — the row is the only route from a decision's number to its reasoning, so a row left wrong misroutes.

**Nothing here licenses a sweep.** The permission is for a falsified sentence found in the course of other work, never a mandate to go auditing the log for them.

Entries D-53 through D-69 were written under the pre-reset statute's fixed skeleton and are part of the frozen archive ([D-74](D-74-2026-08-19-constitutional-reset.md)); decisions before the log existed are in the [frozen ADRs](../adr/README.md).

| Entry | Decision |
| --- | --- |
| [D-53](D-53-2026-08-18-log-and-statute.md) | Split the constitution into a decision log and a statute; freeze the nine ADRs |
| [D-59](D-59-2026-08-18-work-prose.md) | `artifact` gains `work-prose` in the ledger vocabulary |
| [D-61](D-61-2026-08-18-decision-surfacing-and-the-attended-seam.md) | Decisions put to the owner carry argued options; the opening seam's question becomes attended-only |
| [D-69](D-69-2026-08-18-trial-instrument-and-exception.md) | The trial road's falsifier and evidence stop being ledger-only |
| [D-74](D-74-2026-08-19-constitutional-reset.md) | **The constitutional reset**: three content homes, purpose-anchored review, records as exhaust; the statute and its machinery become a frozen archive |
| [D-77](D-77-2026-08-19-owner-approval-admission-path.md) | The admission road gains a second path: an agent-proposed rule is admitted by an incident from real work, or by the owner's specific approval of that rule |
| [D-78](D-78-2026-08-19-carry-the-reasons.md) | Four reasons restored to the rules they govern; migrations must carry reasons or name the drop; review dispatches carry the charter verbatim |
| [D-80](D-80-2026-08-19-spikes.md) | Spikes: test a premise before you assert it — one named premise, a throwaway that commits nothing, reported whether it held, fell, or was abandoned |
| [D-81](D-81-2026-08-19-doctrine-callout.md) | The doctrine callout is a CI label and comment, not `CODEOWNERS`, which cannot request a review from the PR's own author |
| [D-90](D-90-2026-08-20-dispatch-contract.md) | Every dispatch is a shared block with nothing before it, carrying the assignment — byte-identical across recipients dispatched against the same diff base — and every predecessor stage's output whole, both verbatim or by link, never the dispatcher's paraphrase; then a labeled additive note; last the recipient's own identity, output format, skill-worded lens brief, and working location. The terminal stage's docket is set by rule, not by the dispatcher |
| [D-96](D-96-2026-08-20-post-fix-terminus.md) | The post-fix cycle ends when a look sustains no high it will fix whose remedy changes the artifact; a finding whose remedy is not a change to the artifact is recorded and routed, never buying a further round; the mandatory floor names its exemption to match — every fix batch but the one a terminating ruling orders. Until then the cycle continues without asking, so round one's ruling names the cycle budget — and exceeding it puts the review to the owner in that cycle's ruling rather than ending it |
| [D-97](D-97-2026-08-21-dispatch-contract-restated.md) | The dispatch contract restated rather than amended a sixth time: the fidelity clause leads its pair and byte-identity stands alone, so `both` and `itself` are gone rather than replaced. Position zero is absolute but for a byte-identical title line; part one's closure is scoped to prose that aims attention rather than to arity; the fronted `in this skill's own words` stops at a sentence boundary so it cannot reach `output format`; the note bar binds seat and post-fix prosecution dispatches with a reason that fits them. The restatement held and three of the four gap closures bolted onto it did not — bundling decisions into one sentence is what produced five versions in the first place. The post-hoc guard is held, reopening on a dispatch breaking any clause of the paragraph |
| [D-99](D-99-2026-08-21-dispatch-prompt-caching.md) | The dispatch prompt is not cacheable at any size once one byte differs per seat: the harness's post-system breakpoint sits at the end of the whole request, so any per-seat discriminator pushed into the request collapses the hit — cross-seat sharing survives only under a byte-identical dispatch with a *pulled* discriminator, measured, rejected on price at ~0.3% of a review, and filed as #98 |
| [D-102](D-102-2026-08-21-merged-list-is-an-index.md) | The merged list indexes the seat reports and never replaces, edits or restates them: the terminal stage's docket carries anything in a seat's report that no merged finding carries — a remedy, a dissent, an examination that declined to file — with one ruling per merged finding and per uncarried seat entry, and content the merge adds that no seat wrote is labeled as the merge's own. The merge keeps the pen and loses the authority — the defect is a summary standing in for its source, not the author's interest, so a clerk would not retire it |
| [D-104](D-104-2026-08-22-engagement-cell.md) | A fourth cell, `engagement`, for how a session and the owner work together — it owns the pre-implementation artifact whole, on the precedent that `adversarial-review` owns its own final report. The doctrine's admission ladder gains its missing rung (a new cell where none fits) and the burden flips from creating to cramming. The plain brief — a `**In plain terms:**` blockquote atop every surface the owner enters, bounded by a disqualifier rather than a word count, accountable to what sits beneath it rather than authoritative over it. `authoring` is restored to governing documents; spikes moves with a recorded graduation condition |
| [D-107](D-107-2026-08-22-price-is-implementation-cost.md) | A remedy's price is the cost of implementing it — what the fix touches and what it obliges later sessions to do, with a removed **rule or guard** pricing negative — never the cost of standing prose existing, whose removal the narrowed clause no longer credits, the reading that priced remedies by word count through PR #102's review and, upstream of every ruling, through its affirmed acceptance criteria. The terminal stage gains a third disposition, **sustain and route**, for a finding real and worth doing in another change, discriminated by the test that a price-out holds in every future change while circumstance routes; the disposition reaches `:75`'s external pass and `:71`'s never-priced-away floor, and `:71`'s own overloaded `routed` is removed. Size keeps its two homes and gains no third axis; acceptance criteria state what must be true of the result, not what it may cost, and a cost estimate sits outside the numbered criteria |
| [D-113](D-113-2026-08-22-component-sign-and-purpose-statement.md) | The remedy price's sign goes back to being a property of one component rather than a predicate on the remedy: `:65` states the price **in two terms** and hangs the removal credit off the obligation term as an appositive, so a deletion with a large regression surface no longer reads as free. A restatement rather than the proposed qualifier, because an external reviewer produced the total-sign reading unprompted and `authoring`'s no-armor rule directs a reader who genuinely errs to a restatement; an appositive rather than a relative pronoun, because nearest-antecedent binding is what cost `:41` five versions. D-107's `{rule, guard}` narrowing is restated here, since the sentence carrying it is gone. Separately, `:14` defines **purpose statement** as the artifact's stated purpose, audience and success criteria — the sense `:16` already used and nothing stated — so `:41`'s assignment and `:79`'s report resolve from one definition and the repo's most-amended paragraph is not opened |
| [D-114](D-114-2026-08-22-convergence-is-interactive.md) | Convergence is interactive: the pre-implementation artifact is drafted and settled with the owner in conversation and only the settled version is posted — the record of what was agreed, never where he first reads it — and with the affirmation recorded he is next needed at PR review. Affirmed, the artifact is the handoff contract, which is also the test for whether it is finished: nothing load-bearing left in chat, so a fresh session or another runtime builds the affirmed thing from it alone. The doctrine carries the ordering; `engagement` carries the completeness bar and what "ready for his review" delivers |
| [D-119](D-119-2026-08-23-cost-estimate-outside-the-artifact.md) | A cost estimate belongs outside the pre-implementation artifact entirely, not merely outside its numbered criteria: the artifact reaches every stage of the review whole, so partitioning it by typographic position moved the figure without moving what reads it. The reason clause states delivery rather than placement, and every surviving home — PR body, decision entry, the review's report — is written against a tree that exists, so the forecast bar comes free and no second rule is added. Picked up with #115 and #117 as a group; a spike falsified the premise both rested on — zero word counts across four cold seats in both arms — so they were closed rather than built, and the defect the spike actually found, an obligation term carrying one sign where a remedy releases one obligation and creates another, was filed as #121 |
| [D-128](D-128-2026-08-23-filing-cell.md) | Filing gets a cell of its own, `skills/filing`, on `authoring`'s own independent-trigger test — filing fires mid-review and mid-implementation, never while a governing document is being written, and hosting it would turn `authoring`'s success criterion into a disjunction over two unlike jobs. A filing searches the board first, in **every state**, and takes one of three lawful outcomes: extend an **open** issue, file with named ties, or file standalone. Four tie relationships (six verb names, the two paired ones written in both directions), closed under the test that a tie name earns its place by changing what a ranking does with the pair; a fifth for closed siblings was rejected against it. No filing states that it searched — the ties are the search's artifact and a compliance sentence is the 0/8 shape #94 records — and for the same reason the "Minimal filing" announcement retires while provenance stays. Creation carries the want, its evidence, why it will be picked up, its ties and the deferred questions; framing, options, remedy design and the artifact are pickup's. The boundary is a kind rather than a quantity — record what happened, do not decide what to do — with a floor that refuses a bare title. An issue template was considered first per the admission ladder and rejected: `gh issue create --title/--body` applies none, so it never reaches the party it would bind |
| [D-127](D-127-2026-08-23-fork-test-and-argued-ask.md) | A decision is the owner's when it is a genuine fork — what turns on the pick is something he would care about, and undoing a wrong pick costs him something an edit does not undo — and everything else the session decides and reports afterward with its reason, asking where no fork exists being a fabricated gate rather than politeness. The argued form travels with the test out of `Review`, where it had been scoped to findings while needing to govern every ask, into `Authority` beside the plain brief it sits on top of. Both halves were drafted wrong and fixed under review: the cost half measured the edit, under which a command name he types daily is a one-line revert and falls to the session — the paradigm class the issue was filed about; and the first half said *the live options differ*, a plural predicate one live option cannot satisfy, which returned every forced single-route decision to the session. `skills/engagement` carries the calibration, the single-live-option case, the fabricated-gate tell keyed on nothing turning on his answer, and the report-with-reason shape, because the budget held the test and not the examples; it also loses a dangling reference a consumer could not resolve. The class `adversarial-review` invokes but cannot define is routed to #129 |
| [D-131](D-131-2026-08-23-experience-session.md) | The **experience session**: a chartered, time-boxed use of the real result, in three pieces — charter, time-box, session note — and nothing more, since a fourth piece is the counter-bureaucracy failure the design exists to refuse. One duty, discovery — the unknown-unknowns a review structurally cannot reach; arbitration was a second duty in the affirmed artifact and the owner deleted it after the review closed, because D-132 landed the same day and `skills/spikes` now covers testing a candidate wording with a better trigger, leaving one instrument each side of the assertion. Its own cell on `authoring`'s independent-trigger test — the trigger fires when a change to how a later session must work has just been built, with no artifact being written and no review running — with `engagement` rejected because its purpose is the surfaces the owner enters. Root doctrine's flow line names it, on the owner's ruling that an instrument left to fire on a skill description alone does not fire; the clause carries the decline path in the same breath, so it states an order of work rather than a condition of merge. Its own text was spiked and held — and the experience session run on the built cell then found seven things the spike had missed, two of them contradictions, which is the entry's sharpest evidence and cuts against the spike as much as for it |
| [D-132](D-132-2026-08-23-spikes-graduate.md) | Spikes graduate to `skills/spikes`, and the trigger stops being keyed to the pre-implementation artifact: it fires on any load-bearing premise — an artifact's claim, a review thesis still disputed after a round, a candidate rule wording, a question arising while building — with governing prose named as a mechanism whose executor is a reader. Its own cell rather than a broadened reference, because the lint forbids any path reference between skills, so a file under `engagement` is reachable from a review seat only by URL or by name and a cell's frontmatter description is the handle a routing session actually has. Four wording changes came from the run rather than the issue, and the first of them is a correction: `a mechanism nobody has executed` contradicted D-80:15's own recorded predicate, which keys it to what the session can consult. Cold-seat A/B is named as the house pattern with its four properties; an index is declined because a spike commits nothing and so could never append to one, and the report's `# Spike report —` opening carries the findability instead |
| [D-135](D-135-2026-08-23-repointed-reference.md) | The freeze admits one edit — repointing a reference whose target moved, by the change that moves it, where the surrounding sentence survives — and a lint check fires the permission on the mover's own PR |
| [D-136](D-136-2026-08-23-index-dispositions-and-staffing.md) | The review row carries what came of the findings — the terminal stage's own four dispositions, dismissals included because they are the only measure of noise — and the model and runtime that staffed the review; required of every row appended after the twenty that existed at landing, gated on position because a gate on a date the author types is one a typo opts out of |
| [D-141](D-141-2026-08-23-figures-are-derived.md) | A write-up's figures are derived, not recalled: the general engine (suite count, doc size vs budget, delta vs an explicit base, each figure printed with its basis and tree) ships in `skills/authoring` because the failure is practice-general, while `tools/figures.py` applies it here by importing the budget and the census resolution from the lint so figure and guard cannot drift apart. No basis is ever picked silently — the fixed ones are printed, the delta's base is a required argument. Discovery rides the skill's extended trigger at zero doctrine cost; a reproduce-or-fail CI gate and guard migration into the shipped zone were both raised at convergence and declined by the owner |
| [D-153](D-153-2026-08-24-consequence-field-and-the-batching-cut.md) | A finding names the concrete consequence of standing unfixed, and consequence shape — where that consequence lands, on the artifact or on the record of having reviewed it — is read from the site the finding cites. The report carries the split by that axis, which is the instrument #122's self-referential-supply failure mode asks for and nothing could read. The post-fix look is bounded to the fix diff, whatever depends on it, and each sustained high's territory, and the next-pass ruling gains a **relevance terminus** — a pass continues only while its expected findings are about the artifact — because the first full-pipeline field run found post-fix cycles producing real, correctly-arbitrated defects on a subject that had drifted to the record of reviewing. **The batching mechanism the change was built around was measured and cut before merge**: classing at the merge, one price and one ruling per class, a per-item exclusion and a severance duty. Five stages and a cold experience session each formed zero classes, because the exclusion held everything artifact-facing and severance's record limb reached the rest, this repo's records all freezing by doctrine; measured yield was zero rulings of twenty-six, or one under a rejected reading. The owner cut it rather than ship it dormant, so [D-102]'s per-finding ruling unit stands byte-unchanged and this entry supersedes nothing; width is untouched by his standing ruling that experience comes before narrowing |
| [D-155](D-155-2026-08-24-measured-figure-lawful-in-the-artifact.md) | The pre-implementation artifact's figure rule is re-cut against the tree: its reason clause narrows from `any figure` to **a forecast** while the prohibition widens from a cost estimate to any forecast — a figure about a tree that does not exist yet stays outside the artifact, while a figure measured on a tree that exists is lawful in it and travels with the query or derivation that produced it, so a seat verifies rather than inherits. D-119's own argument is the discriminator it did not write down — every surviving home *is written against a tree that exists*; the forecast the owner struck stays struck. Carrying evidence figures by link was argued at convergence and declined. Three fixes travel with it, all from one cold seat on one job: the artifact carries an **open fork** rather than closing it, with the silent close named as the mirror of the cell's existing tripwire against asking where no fork exists; the **draft** a session holds for nearly all of the work is named and given its bar; and report-versus-perform is settled by scoping to the surface this cell owns — the artifact carries what came back rather than the fact that you looked — rather than by restating `filing` or `spikes`, which is #130 §4's one-standard-one-owner shape. The diff-checkability sentence #144 owns is byte-identical to `main`, with the seat's independent reproduction of #144's thesis recorded on #152 instead |
| [D-156](D-156-2026-08-24-installable-plugin-and-shipped-charter.md) | The practice ships as an installable plugin: the binding half of the doctrine moves into a skill cell, `skills/charter/SKILL.md`, which a `SessionStart` hook emits into every session of a repository that installs the plugin and which a session can also pull by name where the hook does not reach — Codex on Windows, a declined hook, a missing `python`. One file, two doors; `hooks/` joins the shipped zone. A shipped calling contract names no harness token — and the reason is cross-runtime, not that the token fails: Claude Code substitutes `${CLAUDE_PLUGIN_ROOT}` and `${CLAUDE_SKILL_DIR}` into a skill body before the model reads it, Codex substitutes neither and instead ships the relative-resolution rule as system prompt, so a token-bearing contract binds in one runtime and is dead in the other. The change's original premise — that the old contract was simply broken — was false for Claude Code and was corrected under review. Delivery is guarded by executing the declared hook command and comparing its output to the charter byte for byte, because `hooks.json` names the emitter and the emitter names the charter, and a guard resolving each rung would still fail an emitter that lawfully computes its path. A Claude-only forced output style was refused on runtime-neutrality grounds, and `commands/` after it — Codex converts commands to skills at install and silently drops any render over 4,000 bytes, which the charter exceeds |
| [D-166](D-166-2026-08-25-criteria-name-consumer-behavior.md) | Acceptance criteria state **consumer behavior** — what whoever works under the built result can do, or no longer does wrongly — and a criterion about the artifact conforming is lawful only where it argues its own consumer-facing load. The diff-checkability sentence narrows rather than repeals: **each criterion names the check that would falsify it**, the diff for what the diff settles, a command for what a command runs, and for what only use shows a session that has none of the change's history, or the built result in use; *a criterion nobody can check is a wish* survives verbatim. A criterion a plausible wrong implementation would also pass measures nothing, and one held across an open fork must be falsifiable on every branch it survives — the second surface **PR #155's terminal ruling routed here as N8**, [D-155] having ceded only the diff-checkability sentence. Warranted by a cold-seat A/B run during drafting: the old sentence reproduced PR #132's fifteen-criteria conformance list from a standing start, and clause (a) against twelve of that PR's own findings dismissed 5 of 12 under it against 8 of 12 under the new one, the delta being three findings its Arm A conformance criteria 13 and 14 had ratified — **two record-accuracy and one, D23, against shipped prose**, so of five shipped-prose findings on the docket Arm A kept four and Arm B three, both dismissing D20. The executable class survived both arms; **the narrowing's measured cost is that one shipped-prose finding**, caught by this PR's own review panel rather than by the A/B, and the entry's Deferred watch is open on arrival because of it. **The review is byte-identical to `main`** — the filter gained teeth with no new machinery, which is the measurement's whole point. Naming a check does not order it run: the owner ruled that fork after the arm priced itself at eight cold dockets per change, leaving run duty with `experience-session` and the flow line. Count guidance declined — the shape rule cut counts on its own, 15→12 and 9→7 |
| [D-167](D-167-2026-08-25-behavior-claims-carry-a-probe.md) | A behavior claim — what a reader does under a wording, whether a mechanism fires, what using the result is like — is a hypothesis until a probe answers it, and the obligation binds the defense and the ruling rather than the finder alone: a defense contesting behavior commissions the probe, and no ruling settles such a claim on argument alone where a probe is cheap. It reaches **severity** wherever the harm claim is behavioral — the channel #138 measured as the one that discriminates — while the executable class is excluded because its highs arrive with their probe. A probe must show it answers the question it is credited with, by a negative control **drawn from the probe's own class** — a control outside that class can pass while the probe stays masked — because a probe that reports a result it never took is *trusted* where argument is not. A dispute **the terminal stage cannot itself arbitrate cheaply** is routed to a spike as a species of `routed` rather than a fifth disposition value, run by the dispatching session before it closes the review or recorded why not; the round-one wording deferred it past the final ruling and the review falsified that — routing is itself a sustained disposition, so no compliant ruling existed, and five of five cold judges routed rather than probed. `spikes` closes [D-132]'s coverage exception for a carrier that is posted rather than reviewed. A session note reaches the seats through the assignment before it is given standing, and that standing is on behavior and nothing else. **#121 is dispositioned by the owner: the sign is dropped**, the term standing as criterion 10 affirmed it — the appositive deleted, nothing in its place. Round one widened the noun instead and the review reverted it: the widened form stated no reach where the appositive carried `{rule, guard}`, and the criterion's own next sentence had already granted what the widening was reaching for. The removal credit weakens and the entry says so rather than netting it away. **The trigger rewrite the artifact's criterion 7 required was not shipped**: the cold-seat run found the premise fell — four of four seats, both arms, routed the mid-review dispute to a spike without once deciding it on the sentence under test — so D-132's routed seam is answered in the opposite direction from the one it recorded |
| [D-169](D-169-2026-08-25-charter-exemption-and-cell-standards.md) | The charter is exempt from self-containment **in both directions** — any cell may name it, it may name any cell, no cell may name another — because it is already always-on in every session, so a citation costs no loading and cannot drift the way a second copy can, and the exemption is one target at depth one rather than a mesh. Its inheritance of the rule was a side effect of D-156 moving the binding half into a cell, and it cost eight rules their pointer to the cell owning their depth; two reached for periphrasis and six pointed nowhere. The exemption is the **name form only** — a rooted `skills/` path is dead once installed, which a portability test caught after the first design exempted every form — and it is held by guards rather than prose: `check_sideways_deps` learns it and now reads the name form as coupling, which it had documented as unlinted, while `check_cell_references` fails a name no cell answers to. `authoring` gains what a cell owes the roster — the description as the whole always-loaded triggering surface with the frontmatter guard as its floor, depth leaving the body on a disqualifier, standards for the code a cell carries, one owner for a shared standard — and sheds its own depth to stay the size it was, the tree's first `references/` files. The rule was tested on the change itself: `engagement` stopped keeping a synced copy of the fork test and cites the charter instead, and two of the new bullets were caught half-owning the portability rule. **The description standard's cold-seat A/B did not resolve** — twelve seats, three arms including one with the trigger apparatus removed, all identical, so the probe failed its own negative control; the warrant is anticipatory and #89's roster-width condition is what would settle it |
| [D-173](D-173-2026-08-25-declared-assumption-is-a-statement.md) | **A declared assumption is a statement, never a promise** — the falsifier stays required because it keeps the assumption honest and any future test one dispatch away, and no one owes a return visit; reopening on an incident where work rested on a false declared assumption whose falsifier sat unused, on which a successor owning the booking is the shape to argue first. **The ruling ships no rule sentence**, and the reasoning is the recorded part: a session unsure whether it owes a return visit resolves that by not making one, so the default under uncertainty is already the ruled behaviour — the asymmetry being that a booking ruling would have made a sentence mandatory, since silence can discharge only the ruling that nothing is owed. A concrete sentence was drafted, put to the owner against the instrumental-falsifier reading and #152's absent-content precedent, and declined; the entry-alone option was the drafting session's own first recommendation and was corrected before affirmation, because the party who would unknowingly undo the ruling is looking at `skills/spikes/SKILL.md:28` and not at the log. So the entry is cited from that line, and **the citation's cost is accepted rather than argued away** — it is the cell's first and only marker, on a line whose wording this entry did not change, while D-80 and D-132 which wrote the cell go uncited, which inverts the signal; the backfill that would answer it is record bookkeeping. Riding with it, two corrections from the instruments' first runs, both one dispatcher on one run and both self-caught late: `experience-session` makes **the material as this change leaves it** checked rather than assumed, placed in the paragraph that owns that phrase rather than where isolation is described, because the failure is about what the consumer receives, not about the mechanism that delivered it; and `spikes` puts the docket's actor condition on the existing docket property rather than adding a fifth, since a docket no seat can enter through the rule's own door is not a realistic one. No fourth piece, no register of open assumptions, no cold-seat A/B on either wording — each states a check the dispatcher performs rather than a judgment a reader interprets |
| [D-178](D-178-2026-08-25-fix-batch-buys-a-second-run.md) | **A review's fix batch buys a second use of the material where it rewrote what that material instructs** — once, on the last tree the review's fixes produce, before the review closes; a batch that changed only the record of the change is the mechanical case the cell already carried and owes nothing, not even the decline line. The bound is the discriminator #138's cold-consumer run measured: fixes to an instruction a job can traverse transferred to behaviour, fixes to register, provenance phrasing and record accuracy reached the consumer not at all. Over the fix-batch commits of PRs #166, #167, #169 and #173 it fires on three and stays silent on #173, whose panel ordered zero edits to either shipped cell and thirteen to the frozen record. **A second occasion, not a relocation — the owner's fork:** moving the single run to the post-fix tree would cost nothing and still test what merges, and was rejected because it deletes the panel's only real-use input, the note #125 gave standing and #167 spent a fork keeping, and lands every real-use finding after the terminal ruling. The trigger goes in the **description** and, after this change's own experience session found two of two consumers reasoning from the doctrine instead, in `AGENTS.md`'s flow line — the boundary had declined that edit on a budget claim the tree falsified (5,659 of 8,000); `skills/adversarial-review` is byte-identical to `main`. **Aiming the job is lawful and saying so in the dispatch is not** — the steer goes in the note; docket authorship stays with the interested session, recorded rather than solved, which is #172's seam 1 from the other side. Riding with it, **#174**: the cell told a dispatcher to withhold that anything is under test and then named a worktree or a clone, both of which hand over the branch name and a commit log that by this moment states the sentences under test outright; one `git archive` procedure replaces both recommendations and the contradicting opening clause is deleted rather than qualified, probed in both shells on this branch. No docket, no seat count, no arms — the cell's own no-required-count rule settles staffing without a sentence |
| [D-179](D-179-2026-08-25-counting-convention-and-facing.md) | The review row states its counting convention once where its fields are defined — every count sums across every cycle, the unit is the finding as originated and dispositioned, a seat's `merged` counts that seat's findings surviving the merge, and a seat key may book an aggregate that names what it aggregates — closing the `merged` column D-136 dropped for want of a vehicle. The row gains **`facing`**, the split by consequence shape over the same rulings `dispositions` counts, with a lint cross-total that is sound where the seat-count one is not; it grandfathers at its own position constant so no row between the two boundaries is silently un-obliged. Instrument firing gets a **recorded query and a reopen condition and no mechanism**: of the twelve PRs numbered above the one that landed the instrument, eleven carry a session note or a decline line and the twelfth merged before it, so twelve of twelve are accounted for with no observed skip, and the only skip on the record is a spike seat rather than an incident in real work |
| [D-184](D-184-2026-08-25-outflow-and-the-always-on-total.md) | The always-on surface — `AGENTS.md`, `CLAUDE.md`, the charter's body, and every cell's name and description — becomes **one derived figure reported for two audiences**, because no invocation reported the sum and so both prior restructures moved prose between always-on artifacts and reported the file they emptied: 11,590 at `3175369` when filed, 15,075 at `b9bcdeb` after the charter split, 15,602 at the merge base. The two audiences are different sets — a plugin's root `AGENTS.md` and `CLAUDE.md` reach a consumer's cache inert and are never loaded, so an adopter reads 9,747 of this practice's material where this repository reads 15,505. **Outflow is owed whenever an always-on surface is edited rather than when a budget is reached** — the admission order read backwards, three moves, never dropping a rule, and measuring the whole surface rather than the file being edited. Running it moved one passage to its citation and **declined another on reading D-78**, which had put that clause in the doctrine deliberately: an audit that never declines is not applying the rule. Both ceilings ratchet to measured (`AGENTS.md` 8,000→6,000, charter 6,000→5,600), each margin argued at its constant. `authoring`'s body cap becomes a guard — it was #169's own evidence that depth-shedding is applicable and was enforced by nothing, living in a command string inside a frozen entry; it bit twice while this change was written. Cells absent from the budget map are unbudgeted on purpose, because a number chosen for `adversarial-review` would be a ruling on its size arriving as a constant (#177). #164 discharged; a roster aggregate ceiling and per-cell budgets for every cell both rejected |
| [D-185](D-185-2026-08-25-finders-of-each-sustained-high.md) | The final report names **each sustained high's finders** — every credited seat as the merge records them, primary first and a merge-added co-finder labeled as its own; or the stage or seat that originated it where no merge carried it; and where a merge carried it with no finder list to copy, that fact. The primary tag and the merge-own label are what let the new corpus be set against the ledger's primary-only one, which is the disagreement the recomputation exists to settle. The width claim at `:23`/`:26` still rests entirely on the predecessor's six rounds, and [#1](https://github.com/Grimblaz-and-Friends/tradecraft/issues/1) answered that objection with a mechanism — `found_by` on every ledger row, so *"seats here retire by evidence"* — which froze at the reset while `docs/reviews.jsonl` replaced it with per-seat counts. Run against the record, the claim answers only to within a bracket: of 92 distinct sustained highs across 27 reviews, 59 are round-one panel findings and **36 of those (61%) name no finder anywhere**; of the 23 that do, the best fixed pair retains 21, where the same fixed-policy computation on the ledger's primary-only attribution puts that pair at 32 of 64 — each end biased toward its own conclusion, and the 36 unrecoverable. Two routine-lane rows in the attributed set forced their own sole-finding and carry no width information; excluded, the two candidate pairs **tie at 19 of 21**, so the sample separates nothing and width stays undecided. `:64` already obliges the merge to credit every finder and the two merged lists ever published carry exactly that, so the datum is produced every review and thrown away, which is the predecessor's failure reproduced verbatim. Scoped to highs because the class below them is the instrument-intrinsic ~88% and new obligations belong off the emission side [#143]; placed in the report rather than `docs/reviews.jsonl` because rows freeze and PR #167 priced out an index limb on that ground — a different column, so only the ground transfers. `:86` gains one sentence naming what the existing `high` column counts, because the clause supplied a warrant for reading it all-finders and a cold consumer followed it there on first use; the column carries credits, never distinct highs. **Width is not decided** — #138 stays open as the vehicle for a recomputation after six reviews have published finder data, the trigger living on the issue because a rule obliging a return visit is governing prose bought for a one-time job [D-173], with `:26` carrying a [D-185] marker so the line's own reader finds the contest |
| [D-186](D-186-2026-08-25-windows-text-mode-defaults.md) | Python's Windows text-mode defaults are decided once, as four rulings. **The encoding half takes two complementary rules, not one** — machine-read output stays ASCII, *and* a stream is set to UTF-8 before anything is written to it, guarded respectively by a literal check and by a call-site check that reads the first statement of `main()`. The original ruling had only the first and rejected the second as unguardable; the review falsified the premise under that argument, because **the two protect disjoint things and were never alternatives** — the rule reaches source literals and the helper reaches the stream, which is the only place a path handed over by `git` or chosen by a consumer can be protected. Probed: a shipped file whose name carried an em dash put #147's own cp1252 byte `0x97` back on stdout from the change that claimed to close it, one with an acute accent put `0xE9` there, and one outside cp1252 did not garble but *raised*, killing the report before the offending path printed. The guardability objection is answered rather than accepted — *was the helper called on this path* is reachability and undecidable, but **the first statement of `main()` is a position, and a position is exact**, with ordering load-bearing because a call after `parse_args` is one `--help` has outrun. So `lib/` is created and the deferred zone question is answered. **A docstring is not exempt when argparse pipes it**: three scripts, one shipped, emitted the same byte on `--help`, and the construction is now banned outright. **The newline half stays prose with no mechanism**, consciously against #137, on a warrant stated as narrower than [#163](https://github.com/Grimblaz-and-Friends/tradecraft/issues/163) claimed — the validator flips only in conjunction with an unquoted `: ` in a value, of which the eight shipped frontmatters hold zero. **The recorded fact names the symptom rather than the reassuring command**: `git status` showing ` M` against an empty diff, which is the surprise #162's autopsy says consumed a step, not the empty diff itself. The guard's bound is stated rather than implied — it reads literals, so it flags a filename or regex source and cannot see a character built by `chr()`; eleven such constructions were probed and all eleven evaded, and **chasing them is priced out** as rebuilding the approximate check ruling 1 rejected. Found in its own review: the check walked the repository root where every sibling scopes to a zone, producing **1,941 findings across 276 files** in the owner's checkout and breaking a mandated flow step on the one surface where the flow is run, while CI stayed green |
| [D-192](D-192-2026-08-25-substrate-cell-and-the-authoring-trigger.md) | The substrate standards leave `authoring` for a **cell of their own, `substrate`**, taking `skills/substrate/references/text-mode.md` with them, because a `references/` load condition reachable only through a body that never loads for its reader is unreachable: `authoring`'s six triggers were all prose-shaped and none described *I am writing a script*, while 11 of 15 tracked `.py` files sit outside `skills/` entirely, which the ASCII guard parses in full and the stdio guard in part. The scope widens from *code a cell carries* to the code this practice ships and runs, named as a meaning change. `authoring`'s description is re-derived rather than trimmed — two subset triggers fold into the clause they belonged to and the code job becomes a non-trigger stated as a job rather than a cell — landing at 632 of 700 where the merge base had headroom 10, and the body at 6,469 of 7,359 where it had headroom 4. `substrate` is unbudgeted on purpose per D-184. The outflow produced one move and one refusal, and its price is stated rather than absorbed: a ninth name-and-description pair takes an adopter's always-on surface 9,747 to 10,253, paid because a rule nobody loads costs the same in bytes and delivers nothing. The 700-character ceiling is not re-argued in the change that resizes the one cell it binds |
| [D-193](D-193-2026-08-25-review-cell-splits-at-the-reader.md) | `adversarial-review` splits **at the reader, not the topic**: the cell's readers are not one population — a dispatching session drives the whole pipeline while each seat, the defense, the judge, and the two triage-only firings each use one stage's machinery, and on a panel cycle the dispatcher is one load of seven or more. What binds every role stays in `SKILL.md` (purpose header, charter, lane choice, evidence standards, lens promotion); each stage's machinery goes one hop behind a pointer carrying its load condition. Body **24,155 → 8,738**. **The cell got bigger and the entry says so** — total prose 27,965, a governing-prose delta of **+3,810**: what shrank is what any one reader loads, and a body-only figure would have certified this as a 64% reduction. Nothing was deleted though deletions were admitted; conservation was checked mechanically and the only meaning changes in the prose that moved are four cross-references — three that said *"below"* and one *"under *Ending the review*"* — which now name their file; the check that establishes it compares base to head and so cannot see what the change added, which is where its real meaning changes are. The split wrote the tree's first sibling pointers between depth files and `check_cell_references` **could not see them** — from inside `references/` the bare form resolves a directory too deep, and `_token_before` reads the `../` prefix as somebody else's tree, so renaming a target left lint and the suite green. The guard now resolves relative `.md` pointers landing inside the naming file's own cell, bounded there because a reference out of a cell is already two other guards' and reporting it twice prices one defect as two; **its first version filtered on a `.md` suffix and the negative arm caught it**, since `RELATIVE_REF`'s trailing class swallows the full stop ending a sentence. A body budget follows at **9,000** — about one bullet above the body it landed at, a ceiling above a measured body and not the measured body, holding the split's claim over a rewrite: [#54](https://github.com/Grimblaz-and-Friends/tradecraft/issues/54)'s rewrite took the body to 13,721 and it regrew **76.0%** to 24,155 because nothing failed when it did. A body cap is dodgeable one directory down, so `figure_cell_total` reports the cell's whole prose beside it, **deliberately unbudgeted** — a ceiling there would cap depth-shedding itself. A second rewrite, graduating the external pass to its own cell, a total ceiling, and no budget at all were all rejected |
| [D-194](D-194-2026-08-25-figures-re-derive-at-read-time.md) | **A derived figure lives only on a surface that re-derives it at read time**; one that freezes — a decision entry, a commit message, a PR body, a pre-implementation artifact, a comment beside code — states the command and the tree it runs on, never the output. #184's figure-defects split cleanly: every re-deriving surface stayed correct and every transcribing one went stale, three fix batches running, with no wrong derivation anywhere in the review. The falsifier is stated in the cell so a reviewer can name it — *which command, on which tree?* — and `derived` is the subject, so a primary observation is untouched and `adversarial-review`'s panel-width basis survives. **[D-155] item 1 is superseded knowingly**: the artifact freezes on affirmation, so a measured figure in it becomes the command that derives it. The implementing session recommended the narrower rule — state the figure, pin it to its tree, which covers every failure on the record — and **the owner ruled the ban**, on the ground that a conditional rule still permits the act that kept failing and that the condition is satisfiable sloppily, the stale PR-body block having been stamped. **The review row stops carrying arithmetic**: from a position constant it names each sustained high instead, and the three counting fields are forbidden rather than optional, since an optional field cannot catch its own omission and lets the retired shape drift back into a file nobody may edit. `staffing` survives — a model and a runtime are facts about who ran the review, not arithmetic about it. How many highs a review sustained is now the length of the list, derived at read time from the row. **The row half of #171 and #180 closes** — no row books a disposition its ruling did not produce, and none needs a shape for a ruling that reached no merge — while the half of each that lives in the report stays open; the change claimed both closed until its own review read the issues against the shipped text. Guards go only where a checkable subset exists: the row's schema is guarded and mutation-probed through the repository's own index in both directions, and the figure rule gets **no guard at all**, on [#187](https://github.com/Grimblaz-and-Friends/tradecraft/issues/187)'s own reason rather than its fate: it proposed a seat lens rather than a detector and named a guard as the wrong material, since deciding whether a sentence still describes its subject is a reader's judgement. #187 was closed on standing, which does not transfer to an owner-initiated change. This removes the supply instead. Retiring the index entirely, a PR-body exception for a stamped block, backfilling, and reaching filings ahead of #158 all rejected |
| [D-210](D-210-2026-08-26-project-roster-and-the-loaded-total.md) | The roster a session in this repository loads is **generated into `.claude/skills/`**, one entry per cell, carrying the cell's frontmatter byte for byte over a body naming the generator and pointing at the cell. Nothing installed this repository's own plugin, so no session here held any cell's name or description and every trigger routed to one -- D-141's derived-figures trigger, D-184's outflow condition, recorded there as that change's load-bearing correction -- reached every adopter and missed us: three of four cold consumers on PR #194 never reached the rule, two writing a violation into a commit message and a PR body. `figure_always_on` reported 16,241 here against **11,351 a Claude Code session actually read**, and each audience's roster is now read from the directory that audience loads it from, so the total is derived rather than asserted and deleting an entry moves it; what a Claude Code session here loads rose 11,351 -> 16,241, while a Codex session loads neither roster and stays at 11,351, the 4,890 the figure had been claiming. `check_project_roster` asks the generator for the expected bytes rather than recomputing them, on `_always_on`'s own recorded failure. The entry is a pointer rather than a copy -- one hop, exercised by a cold session that invoked `filing` from it, followed it, and applied the cell -- because copying bodies would put 78,931 characters in a second place -- every cell body at the landing tree, each file's decoded characters less its frontmatter block. Self-installing the plugin is rejected (sources cache per version, so a session here would read *published* descriptions, backwards for the sessions that edit them); relocating the cells is priced out at 120 `skills/` references inside frozen entries under mover-pays; a committed symlink is dead on the owner's machine; accepting the split was the third option at convergence and the owner picked the mechanism. No always-on artifact was edited, so no outflow was owed; no doctrine rule was added, per the admission order; `.claude` stays out of `REPO_ROOTS` with the reason narrowed rather than left standing |
| [D-222](D-222-2026-08-27-codex-runtime-compatibility.md) | Codex and Claude consume one shared plugin and one charter source, adopted explicitly through the repository's canonical `AGENTS.md` (with Claude's `CLAUDE.md` pointer); lifecycle-hook fallbacks are removed after failing in installed Codex sessions; native review launches record fresh context, root, sandbox, model and reasoning without a capability wrapper; bot and self-invoked external passes stay outside the staffed panel and are reported qualitatively in the report and index; and the Codex compatibility check runs in a temporary adopting repository outside the source tree |
| [D-225](D-225-2026-08-29-values-ranking-adopted.md) | The values ranking is **adopted and binding**, moving to `docs/values.md` out of a directory whose declared job is holding reference that binds nothing — a status line inside a file is missed exactly once, where the directory name is read every time the path is. `AGENTS.md` gains a `## Worth doing` section that **binds rather than points**: a worth-it judgment whose evidence has run out is argued against the ranking, by number. Admitted at the admission order's last-resort tier because a ranking function is the *must bind before any context loads* class that tier is reserved for; per-cell copies were **refused rather than foreclosed** — the zone wall reaches the citation form, not a copy, and the reason that does the work is that copies drift. The both-ways guard gains the recognition half #221's cold read found missing: its existing sentence reaches a **claim**, which carries its value because somebody is arguing worth as it is read, while an **open item** is read at rest by a sweep nobody is present to answer — and a values-funded item has no measurable payoff by construction, so it reads as weak and the guard never fires, which is the tilt operating on the board rather than in an argument. An item whose case rests on unmeasurable value names that value where its case is made, and **absence of a name means untagged, never *measurable***: a tag whose absence carries meaning must be applied reliably, and applied patchily a missing tag becomes a measurability claim that culls precisely the work the guard protects. The stronger shape — the obligation in the shipped `filing` cell — was declined on four grounds, decisively that the zone wall forbids a cell naming `docs/`, so an adopter who never loads `AGENTS.md` would receive an instruction to cite a numbered value against no ranking at all; it stays available on a sighting, which #221's cold read is not, the charter holding that a review finding about governing prose is no incident. The outflow took one compression, one refusal, and one attempted move the review reversed: removing the flow's fix-batch clause was recorded as a move on the ground that `experience-session`'s always-loaded description carried the trigger, and **[D-178] falsified it** — that entry bought the two-surface arrangement with its own experience session, recorded a consumer misreading the very sequence the removal reinstated, and recorded its own run as unable to speak to the description's surface; a Codex session loads no description at all [D-210], so for that runtime the removal left no carrier. The clause returns shorter but whole — condition, decline and citation — **now carrying the citation whose absence is how the audit missed D-178**, funded by compressing the Release line's mechanism to [D-81]; a first repair attempt dropped the condition and a cold probe caught it reading as unconditional. The Release line's CODEOWNERS contrast compresses to the `[D-81]` already beside it, whose title is that contrast; the CRLF clause is **refused**, because D-186 §5 records that placement as deliberate — the fact must be found mid-task, when a session notices CRLF and has no reason to open the decision log. Ranking adopted as written, amendment still the owner's alone. Charter export, allocation lanes, the worth-it check itself, and retro-tagging the open board all out of scope |
| [D-230](D-230-2026-08-29-review-economics.md) | The fix bar becomes **what acting costs in judgment, not scope**: needing neither the owner, a design call, nor a change to what the change is, a determined remedy lands here whatever it touches — scope and the *affirmed boundary* being different things, the latter the owner's by construction. `routed` and `priced_out` retire into fix / **record** / file, dissolving #171 rather than patching it; the index needed no change because [D-194] already forbids `dispositions` on new rows. A **lens retires** on either of two grounds — repeatedly not worth acting on, or **a mechanism now catches it** — the second written before any mechanism exists so #217's suites collect on it without amending this rule, and reaching *any standing instruction to look* after a cold probe found the narrower wording missed this change's own exemplar. That exemplar is performed, not merely offered: the post-fix look stops prosecuting the merged list, report, commit message and PR body, a class one rule manufactured while the relevance terminus existed to notice it working. **Fixing needs no permission; declining to fix does.** The admission bar #216 was filed for is **dropped on measurement** (~7% of 163 sustained findings, all already low or latent) and boundary triage with it, since keying on the affirmed criteria would have routed away the version collision that would have shipped two trees under one number. `external` narrows back to configured automation; a self-invoked run is a **commissioned pass**, keeping [D-222]'s not-a-seat ruling and discarding its bucket. Five cold probes ran before the first commit; the one that failed moved the record's read obligation out of the closing-the-review file, whose load condition guaranteed no dispatching session would reach it |
| [D-231](D-231-2026-08-29-roster-guard-compares-text.md) | **D-232 decided the roster comparison first and independently, and this defers to it** -- including on the half the two branches disagreed about, where this one had routed `write()` through the predicate too so `--write` would report rather than churn; re-litigating a landed call on one mechanism is deciding it twice. What is left is the mechanism and its population: a Claude Code **session** worktree comes up with ten files written in text mode by the harness -- the nine entries and `CLAUDE.md` -- while git checks out the rest LF in the same second, established by a creation-mtime trace of two writes 100ms apart whose CRLF group is not an index-order prefix. **`agent-*` subagent worktrees do not do this and are Claude Code worktrees too**; `CLAUDE.md` is the durable discriminator, at 14 of 15 session trees against 0 of 16 agent trees, because nothing rewrites it where `--write` erases the evidence under `.claude/skills/`. Scope is the load-bearing part: a first draft said *every* worktree and all five review seats, each in an agent tree, checked their own and reported the cause false. `core.autocrlf` is not the cause and the copied gitignored settings file is not evidence of the text-mode copy. The comparison's bound is one character wide: a lone carriage return defeats `text=auto`, but an entry carrying one still fires -- the exposed case is a **cell** carrying one *inside its frontmatter*, which `frontmatter()` copies into `want`, recorded on #234 after a review narrowed the claim twice. Pinning that bound takes two fixtures and neither half does it alone. **Check 20** bans a control character in a docstring's compiled value, admitted on measurement -- zero false positives across every tracked file, against a class that fired repeatedly inside the change that added it, including in the check's own registration sentence; the surfaces are disjoint for the carriage return alone, which the tokenizer folds before compiling, and #233 keeps the byte scan. The tree-level roster test asks that every cell has an entry rather than that nothing else is there, and stops asserting on-disk LF -- two reds a session could not clear while doing what the material says, the first found by a cold session asked to add a project skill. No always-on surface edited, so no outflow owed |
| [D-232](D-232-2026-08-29-subprocess-stdin-and-the-roster-comparison.md) | **A launch redirects nothing, or names all three of stdin, stdout and stderr.** On Windows `_get_handles` returns early when all three are None, so a launch redirecting *nothing* never asks `GetStdHandle` and cannot fail; redirect one and the others resolve through a std-handle table that can still name a handle something has closed, and `DuplicateHandle` raises `WinError 6` -- intermittently, because Windows recycles handle values, so the same call succeeds, hands the child an unrelated handle, or raises: [#229](https://github.com/Grimblaz-and-Friends/tradecraft/issues/229)'s different red set every run, all of it one error. **The rule shipped twice wrong before it held.** First as *name your stdin*, which manufactures the defect -- `run(cmd)` 0/20 against `run(cmd, stdin=DEVNULL)` 20/20 under real capture, the guard flagging the 0/20 shape and prescribing that edit. Then as one uniform keyword reading across five launchers, which **certified `check_output(cmd)`** -- `run(*popenargs, stdout=PIPE, ...)`, so it redirects before any keyword is read, measured 20/20 -- while reddening four `check_output` shapes measured 0/20. **A launcher is read against its own source, not against `run`'s**: `check_output` covers `stdout` by construction and pipes `input=None` (which `run` does not), `capture_output` exists only on `run`, and `input` only on `run` and `check_output`. `check_output` is deliberately **not** grouped with `getoutput`/`getstatusoutput`/`os.popen`, which have no compliant form at all: it has one, `stdin=` and `stderr=`, so naming it unconditionally would redden lawful work. **Four proposed remedies across three cycles diagnosed correctly and prescribed a fix that reddens lawful work**, every one by assuming a wrapper behaves as `run` does; each was caught only because a defense ran the counterfactual in both polarities unprompted -- recorded against the evidence standards, which bind that probe to a guard under review and say nothing about a finding's own remedy. **The guard's own bound was stated wrongly three times** -- a two-item list, then a not-a-list of two, then *not a literal* generalised from `capture_output` to every argument, false of all 17 launches here since every one is `stdin=subprocess.DEVNULL`. It now states what **is** read: the callee and its keyword arguments, with a stream keyword read as a redirect unless its value is the literal `None` -- so a name that is `None` at run time reads as covered and is not, a permanent bound of a call-site check. `.gitignore` filtering, root-level scripts, and `check_output` naming the stream it owns are **recorded** rather than fixed. Separately and on the owner's scope ruling, **`roster.verify()` stops comparing working-tree bytes**: a worktree whose entries a harness rewrote in text mode reported every cell out of step and took the lint red against a tree git called clean, which [D-186] ruling 5 declares expected -- the warrant being that inconsistency, not frequency, which is a **race rather than a boundary** (two worktrees 36 seconds apart, opposite results; 3 of 26 carry it). **`write()` is untouched and the asymmetry is recorded**, as is the fix's one-sidedness: a CRLF *cell* still reports, because `expected()` loses a byte to `frontmatter()`'s documented bound, and that repair is filed to [#234](https://github.com/Grimblaz-and-Friends/tradecraft/issues/234). The stale warrants moved with the fix in `roster.py` **and** in the shipped `skills/substrate/references/text-mode.md`, whose third rule told every adopter to compare as bytes and whose warrant claimed no defect had fired. The `substrate` description gains a launch trigger and the outflow is **refused in writing**, the search run over the whole always-on surface and returning nothing cheaper. A `conftest.py` repairing the table, disabling capture, fixing the harness, adding the no-stdin wrappers to the launcher list, flagging `check_output` unconditionally, restricting `input` crediting to `run`, and surfacing `--write` from `verify` all rejected |
| [D-246](D-246-2026-08-29-behaviour-pointers-and-the-erratum.md) | **A behaviour claim on a surface nobody goes back to correct names the probe, test, ruling, or file that shows it** rather than paraphrasing, because the paraphrase is a copy that can be wrong while the original stays right, and nothing routes anyone back to repair it; irreducible rationale stays prose, having no demonstrator and being superseded rather than falsified. **Scope is that property, deliberately not the figure rule's list** sitting above it: that rule reaches what nobody re-derives at read time, this one what nobody corrects, and the lists separate in **both** directions -- a comment beside code is in that rule and not this one (it can be edited, so a wrong sentence is an ordinary fix, and describing what the code does is often exactly what its reader needs), while an **append-only record** is in this one and not that, being the surface with no repair path at all. Unifying the two lists is the undo this entry exists to prevent. **The decision log gains a second lawful edit**: where a sentence is falsified by evidence the entry itself cites, a `[corrected — see Erratum]` marker sits at the error and a final `## Erratum` section carries the falsifying evidence -- **naming the demonstrator, never restating the behaviour**, since a correction written as a fresh paraphrase is a fresh frozen copy of the very class this repair ends. Lawful under the same claim-neutrality test the repoint passes [D-135], whose **rejection of errata-by-appendix and of an unbounded correction category this entry supersedes knowingly** -- answered by the bounds, D-135's own distinction being that an act can be bounded where a category cannot. Owner-ruled against footer-only (the reader who believes the sentence never reaches the correction) and strike-in-place (the body stops reading as what was decided then), and the supersession affirmed once shown. Bounded, each bound against a named failure: **the falsifier must be the entry's own**, established by the pin, or the clause becomes supersession-by-erratum; judgment is out of reach; it rides with other work; and **no sweep** is licensed. The index row moves in the same change where it summarises the corrected sentence. Outflow took one move -- `AGENTS.md` dropping "when in doubt, ask the cheap question", which the charter holds verbatim -- and **one refusal, the D-186 CRLF fact, declined on the same ground [D-225] declined it**, recorded so a third outflow does not spend the search again. A guard on the erratum's shape was rejected: the parts are checkable, whether the evidence truly falsifies the sentence is not. **The clause landed with no live customer**, so the warrant is the base rate [D-96], and absence of errata is not evidence the clause was unnecessary |
| [D-247](D-247-2026-08-30-the-lints-contract-and-the-two-prose-guards.md) | **Every check reports one at a time, so one that raises no longer discards the rest.** `run()` was a single `+` chain, so any check that raised threw away every finding computed before it and answered the flow's mandated pre-commit command with a traceback; it now walks a `CHECKS` tuple and reports a raising check as a finding naming the check, the exception and the frame *inside this repository*, without claiming the rest of the tree is clean. **`frontmatter()` takes the whole line ending after the terminator, not one byte** -- on a CRLF cell that byte was the carriage return, so the copied block lost a blank line and the repair the finding named committed a change no local diff could show while reddening Linux CI; the LF path is byte-identical to what it always was, and repaired at the slice rather than in `expected()` because that is where the bound is documented and what other readers call. **Two prose guards land, and the design call #233 deferred dissolved rather than being decided:** a hollow code span is content that is whitespace *and not empty*, because every false positive the obvious predicate produces has content that is exactly empty -- the doubled-backtick idiom's inner span -- so no exclusion list is needed and none can go stale. **The carriage-return guard reads the working tree and untracked files, not only the index**, because `AGENTS.md` runs the lint before staging and `persist.py` refuses a pre-loaded index, so reading the index alone answered a question about the previous commit; the pull request body asserted that choice changed cost and not answers, and it changed answers. **The two guards skip for two different reasons and so have two lists**: the frozen archive for both, the live append-only records for the prose guard alone, since a hollow span in a review row is intended content while a lone carriage return there is corruption of the row's own JSON. Three issues in one pull request because they are three defects in one consumer's contract. Unifying the three fence implementations is #249, which also records that on an indented fence marker the newer check is the correct one and a naive unification would regress it; validating `docs/recorded-findings.jsonl` is #251; four findings are held for evidence with their promotion conditions. |
| [D-252](D-252-2026-08-30-the-standard-stops-picking-a-language.md) | **The shipped substrate standard stops stating a language and `AGENTS.md` makes the pick on its own authority.** [D-192]'s two-site scoping narrowed the routing inversion rather than removing it -- the same pick stated twice, with two referents for *here* identical here and divergent in an adopter -- so bullet 1 becomes *one substrate, chosen once and hardened*, and the Audience clause that defused it comes out with it -- **not as the outflow, which the review caught the entry claiming**: a cell body is not on the measured surface, so that deletion moves it by zero and the outflow is the `AGENTS.md` edit itself. **"Stdlib-first" goes with "Python" as a judgment, not a trim**: that half is about admitting dependencies, and #192 cut the dependency rule as agent-proposed with no incident, so keeping it would re-admit the cut rule under a broader wording. **Two facts settled before deciding, and both narrowed the change.** The shipped `winio` helper is **unreachable by an adopter** -- both *shipped* importers resolve `lib/` as `parents[3]` from a file inside the shipped tree, and an installed plugin's copy sits at a version-stamped path [D-156] refuses -- so rule 2 named a helper its reader cannot reach while calling the obvious hand-roll insufficient, which reframed #241 from a missing clause to a missing discharge; the rule now states the two properties (UTF-8, *and* newline translation suppressed) **before** naming the helper, and requires it **imported by name rather than defined locally** and states what a guard reading an import binding does not establish -- `check_stdio_wired` refuses a local definition only where no import of the name exists at all, which no shipped prose stated and which #254 carries. And bullet 1 is the only one stating a **pick**, so #197's *do the other four rules share the shape* closes **negative for the language pick** and no restructure was needed -- but **not for substrate-dependence generally, which this entry claimed until its own experience session falsified it**: bullet 5, *a path resolved against the directory of the file naming it*, does not translate to a compiled substrate, a built binary having no source-file directory at run time. That residue, and the Python-saturation of both references, are live and recorded rather than filed. The guard-writing section stops restating the mechanism and points at the rule: #241 offered *move it up or accept the duplication*, and the answer is **neither**, because two imperatives to two readers are both owed while the fact beneath them may be owned once. **#235 closed without code** -- PR #232 merged two hours and thirteen minutes after it was filed (22:40:47Z to 00:54:19Z) and discharged both halves; the landed bound keys on *version control normalising on the way in* rather than the filing's *leaving the repository's control*, and is the more precise, since a rewrite version control does not normalise should still flag. **The general standard #241 floats** -- a shipped rule states its own strictness -- clears the admission bar on three incidents and is **deferred rather than refused**, because landing it while #237 is open ships it already violated. A fuller substrate standard, a one-clause fix, splitting the cell, and making `winio` importable were all rejected |
| [D-253](D-253-2026-08-30-guidance-on-its-readers-surface.md) | **Operative guidance lands on the surface its reader is already on** — four instances in one change, batched because they contend for two budgeted surfaces rather than because they share a class. The zone wall gains its second direction in `AGENTS.md` rather than a cell, because a shipped cell may not name `tools/` at all and could only have stated a principle about a wall it is forbidden to describe — **that ground, not an empty mechanism tier, is what carries the admission**, an earlier draft having claimed no guard could check the lawful direction when `check_stdio_wired` checks it for `lib/`'s one module. `Content routing, here` gains the binding `docs/` document as a fourth kind, behind a doctrine line that **binds rather than names**, which is the half [D-225] argues for and a first draft dropped; the bullet above it silently stopped being exhaustive and that narrowing is recorded. Both additions landed on the owner's ruling, funded by two outflow moves — the calling-contract bullet compressing to its guards' names and `[D-156]`, and the version-bump unit going to the guard that computes it, which now states the unit in both failure messages — with **D-225's three prior attempts on this file unavailable and not retried**, one of them reversed under review. The budget-as-trigger sentence is hoisted from a lint constant's comment to the `authoring` cell, the comment and the budget findings now pointing at it without restating it. **The change is not evidence that the three moves pay for what is added** — both surfaces end tighter than they began, and an earlier draft claimed the reverse as its own proof; what it demonstrates is the behaviour, a cold consumer at a near-full surface performing an outflow rather than declining. `check_body_strip_owner` refuses a hand-rolled cell-body strip — **the third implementation the filing predicted was already in the tree** — and turns on properties its own review had to add: marker and slice connected, receiver matched structurally, and — **on a slice of the tested text but not on a piece of a marker split** — an unbounded tail, the asymmetry that keeps the cheapest wrong expression caught while leaving most lawful field readers alone; the residue is reported and the message names the lawful spelling. Exemptions are `(path, qualified name)`; the owner file is exempt wholesale and says so; the suite pins the set's membership, which is **not** a ratchet; the test-file blind spot is sized by `--body-strip-scan` rather than by a number — a first draft's figure outlived its predicate by five times, and the scan itself then read low until it and the check were made to share one sweep. Left open: the `authoring` description still does not trigger for a session writing a measuring tool, routed to #241 with an experience session's evidence; the exhausted-surface route is recorded, not answered; no budget constant moved |
| [D-261](D-261-2026-08-30-visible-compliance.md) | A rule holds when its compliance is a **visible property of the artifact its reader is producing**, and fails when it asks for a judgment resolved against a document that is not in front of them — measured over every PR merged into `main` across twenty of this repository's own rules, with enforcement, age and surface all falsified as explanations on the issue that carries the derivations. `authoring`'s Writing standards state the preference plus the **conversion move** that is its actionable half — give the remembering form a carrier the writer already has open — and claim the separation rather than any rate, the sweep seeing merged work only so every rate is an upper bound. Two rules it caught are repaired. **The values ranking's citation, at zero**, moves onto the `record` entry shape as a `value` key, which is [D-225]'s declined `filing` mechanism admitted on the *actual sighting* that ruling reserved it for — measured non-compliance from real work, not a review finding about prose; path-free for the zone wall, omitted where a repository declares no ranking, its absence claiming nothing, and enforced by no guard because the record is one doctrine forbids repairing. **The outflow gains a fourth answer, deletion**, lawful only against evidence that the rule does not bind and recorded with that evidence — [D-184]'s never-drop clause having left the surface it governs rising on net with every local reduction paid for by growth in this practice's governing prose overall in the same commit, an instrument that can only route scoring as reduction locally and growth globally. The addition is paid by a **move rather than a smaller addition** per [D-253]: *What a cell carries* sheds whole to `skills/authoring/references/cell-structure.md`, freeing more than was needed because the section is one unit with one load condition. The standard landed in the body rather than routing depth, **revising the placement reason the affirmed artifact had argued** — routing depth loads when the home is the question or an always-on surface is edited, and a session revising a rule already sited in a cell fires neither, which is the gap the standard's own falsifier probes. No always-on surface is edited, so no outflow is owed; `AGENTS.md`'s pointer was considered and left byte-identical, a zero-net accuracy swap rejected because an always-on edit buys an audit to fund a repair that was not needed |
| [D-262](D-262-2026-08-30-batch-at-birth-and-the-failing-run.md) | **A filing carries the failing run where one exists, and defects one change would fix land as one number.** The evidence floor gains a *preference* rather than a requirement — the mandatory form is the plausible wrong implementation, since wants and ideas have nothing to reproduce by construction — and routes the breached-rule case to the cell's existing *the rule's own sentence is the observation* paragraph rather than restating it. A fourth lawful outcome files one issue carrying the batch, and **its test is the change, not the surface**: on #233/#234/#235 a same-file test splits a real batch (two different files, one change fixed both) and groups nothing, the third having closed without a change of its own; the false-admit direction is shown by #224/#233 instead, one `same-subject` pair that two changes fixed. #94/#95 move onto the new bullet, being evidence of the cost of not batching rather than of not tying, which is what keeps the addition from needing fresh evidence. **The fifth tie verb is declined** — the batch outcome removes the *one-change* cases rather than every case that could pass, D-128's anchor/slice relationship being one it does not reach; the residue is provenance whose home this entry leaves unsettled, and declining is the reversible direction where admitting strands vocabulary across filed bodies. Recorded here because the proposal will be made again and needs the test applied, not re-litigated. **No outflow owed**, as a fact about which surface was edited: only a cell's name and description are always-on and this change edits the body alone, with the description considered and deliberately left — its existing triggers already reach both new situations. Under-triggering is the failure mode that buys, and the experience session did **not** test it, so the condition on which the description changes is restated against any later observation rather than against a run already spent. |
| [D-263](D-263-2026-08-30-the-isolation-procedure-sheds-and-states-its-conditions.md) | **The `experience-session` cell's tree-building procedure leaves the always-on body for `skills/experience-session/references/isolation.md`, and the isolation rule gains a second direction.** Five issues in one pull request because four of them rewrite one paragraph -- shared scarcity in prose, not in a budget; this cell is unbudgeted on purpose, so the shed rests on the `authoring` disqualifier (a session loading prose it had no use for) rather than on a ceiling, the frequent firing being the *does this buy a session* consult that paid for a git fence to answer. **The dispatcher's check is re-aimed from the command to the extracted tree** -- `export-subst` stamps the commit subject into copied files and `export-ignore` drops a path the pathspec names, both reproduced on a throwaway repository, **neither set here and both live for an adopter**, which is exactly why this is shipped prose and not a guard in `tools/`; one inspection covers both and covers the build step nobody has thought of yet -- though **not** a path never named, which is what this change's own experience session then hit, nor `export-ignore` below a named directory, both fixed in the review. The fence quotes its paths and sets a `consumer` identity, because `git init` inherits none and the cell tells the consumer it may commit; `--force-local` is named beside the error text GNU tar actually prints. **The falsified roster sentence is replaced by naming the right paths, not by another route** -- since #210 an archive naming the loading surface carries the roster, the route it prescribed reds the lint with a remedy that deletes what it put there -- no count stated, the number moving with both tree and operation and the sentence having said *move* while the number that circulated belonged to *copy*, and because roster entries are *pointers*, naming the surface alone gives nine dead ends, so the cell now states its one case where the tree carries more than the job needs. **Reading the change's own record back in is forbidden**, the mechanical remedy rejected as unavailable in these lanes and fatal to network jobs, with the detection -- the end-of-job account naming what was consulted -- carrying the weight, since the one observed breach surfaced only because a consumer volunteered it; searching the board for the job's own sake stays lawful. **#227 is a wording repair and the rule is unchanged**: the clauses were already consistent and `and always will` is what made an absolute scan as overriding a conditional, so the precondition becomes the subject and picking a new default was rejected as a rule change no evidence asked for. **The cell gives on the unsatisfiable flow, not the doctrine** -- scoping `AGENTS.md` was the third option and the admission order puts that surface last -- and the leak question is answered rather than left: naming the doctrine is harmless unless the doctrine is what is under test, where the throwaway remote the cell already provides takes over. **Its own review falsified three of its claims before landing**: that no off-the-shelf mechanism gives a history-free copy ignoring both attributes (`git read-tree` plus `git checkout-index` does, and was rejected on failing *silently* where `git archive` is fatal), that the shed bought room (both the body and the cell total end larger than they began), and that the code-job remedy worked (the enumeration of publish steps missed convergence and missed a decline having nowhere to land, so the reference states the general form). Affirmed criteria 5, 6 and **7** are recorded **not met** and 2 not met as stated: 7's falsifier needed `--cell-budget` to run at all, and once it ran it showed the shed's payoff **inverted** -- the frequent consult ends up loading more, not less, because the review's own ordered remedies land on the always-on surface. The entry states the figures' command rather than the figures, having twice carried wrong ones. Left open: #183's fifth item, now with an observed mechanism -- a consumer inside a clean isolation read two files from outside it; no guard anywhere and the reason one is wrong here; `--force-local` as a per-tar remedy, bsdtar rejecting it outright; and two filings, whether `AGENTS.md` scopes its own flow ([#267](https://github.com/Grimblaz-and-Friends/tradecraft/issues/267)) and what a revealed read-back breach does to a run ([#266](https://github.com/Grimblaz-and-Friends/tradecraft/issues/266)) |
| [D-269](D-269-2026-08-30-description-names-stages-not-tests.md) | **A cell's `description` names which stages exist, never the tests they apply.** `adversarial-review`'s description advertised *"a terminal stage that weighs value against price"* after [D-230] replaced that axis ([#257](https://github.com/Grimblaz-and-Friends/tradecraft/issues/257)), and declared no trigger for the external pass `AGENTS.md`'s flow requires ([#203](https://github.com/Grimblaz-and-Friends/tradecraft/issues/203)) — one paragraph, one budget, one edit. Both characterising clauses go, the falsified one and the still-true *"that prices each remedy"*: keeping the true one preserves the habit that produced the defect. [D-107] and [D-230] each rewrote that test and each falsified the string with every guard green, and no cheap mechanism can see it — a phrase-presence check needs the description's wording verbatim in the body, which is the duplicated prose `authoring` forbids. Naming stages and not tests closes the class: a stage's test can be rewritten without touching an always-on surface. **This supersedes [D-107]'s rejection of amending this string**, whose ground was that a triggering string's regression surface is untestable — [#217](https://github.com/Grimblaz-and-Friends/tradecraft/issues/217) built the harness and measured it saturating *and* blind to this defect, so the untestability is now measured rather than assumed. The added trigger covers the commissioned pass in the same clause, `skills/adversarial-review/references/after-the-fix.md` governing both. Outflow performed and not sized to the addition: two moves empty, two candidates considered for the third and both blocked — `AGENTS.md`'s `docs/reviews.jsonl` line by the zone wall, its `lib/` line because shipping a repo-only layout rule would impose it on every adopter — so the discharge is the **refusal**, the deletion being part of the edit rather than its outflow answer. Substituting an accurate test and cutting the enumeration to triggers alone were the two options declined at convergence; a comparison guard and a routing eval on the new wording were rejected with their reasons |
| [D-270](D-270-2026-08-30-version-guard-two-bounds.md) | **The version guard's bound is the base ref's tip as well as its merge base, and the manifest's exemption narrows from the file to the `version` field.** The merge base alone answers *did this branch raise the version it started from*, a question about the branch rather than the version. #110 records six instances; four are that moved-tip shape and close here at the moment the version is picked, while two are concurrent open PRs off an unmoved base, which no bound reading only HEAD and the base ref can see - that residue is #279, and #206 is the same want filed and closed independently. The remedy sits in the script rather than CI because every instance is about the local run being green while the version is being picked, which #262's silent identical-edit auto-merge shows most sharply; the guard does not fetch, so every PASS that consulted the tip names the revision it read and a remote-tracking ref carries the freshness note - the first draft put that disclosure on the failing line, which is the path taken when the ref was fresh enough to catch the collision. The manifest's wholesale exemption was collateral from the circularity fix and reached `name` and `description`, so consumer copy could change at an unchanged version; a bump alone still counts for nothing, and an unreadable manifest is UNDETERMINED on the current side only, the base side having been an unclearable red where the manifest is the sole shipped change, a gate that reaches that case and not an adoption PR carrying skills too, whose red is inherited and recorded. The int cast in `_parse_semver` is pinned in both directions, `0.9.0 -> 0.10.0` having been the first bump any fixture could distinguish from a lexical comparison, and the `isdigit` gate in front of it is replaced by `isdecimal` so the gate and the cast agree |
| [D-278](D-278-2026-08-30-a-roster-surface-per-runtime.md) | **One roster surface per runtime, and the always-on total stops being one number.** D-210's remedy for #199 was one runtime wide: Codex discovers a project's own skills under `.agents/skills`, this repository had none, and every trigger routed to a description reached every adopter and reached no Codex session here **as this repository's own** -- on a machine with the published plugin installed those nine descriptions did load, stale, so what was missing was the current ones. `check_project_roster` carried the exclusion in its own docstring, true when written and never re-checked after Codex acquired the surface; how long it stood is left to `git log -S` on whatever history a reader has, no removal sha or interval being statable in advance of a squash merge that has not happened. Probed in both directions on one tree holding both directories: Codex returned the `.agents/skills` entry and not the `.claude/skills` one, Claude Code the second while reporting the first unavailable, so neither a shared directory nor a **committable** link was ever available -- a working-tree junction is makeable without privilege, which is a guard question rather than a loading one -- and a junction, which needs no privilege, defeated `inside_roster` in both branches until the guard began comparing the surface against where it was declared to be. `tools/roster.py` gains a `Surface` per runtime and loops; a finding **about a surface** is per surface and names its directory and its runtime, a finding about a **cell** is reported once and names neither. The frontmatter copied is identical on every surface, one trigger not being allowed to fire two ways, and each body names the runtime its copy exists for, keeping M10's discipline after the exclusion it was written about is gone. Ownership is checked on every surface rather than the first, on C1-F2/C1-F3's own record. **The always-on total is per runtime and every surface a reader meets prints the rows**: the two do not read the same doctrine files -- both `AGENTS.md`, only Claude Code `CLAUDE.md` -- and charging every row for both reported them as loading the same amount when they do not. `repo_total` survives as the scalar a delta could be taken against and is the smallest row, the largest having reported this change's own arrival as zero movement; nothing renders it alone, because the one renderer that did handed a session on the mandated pre-commit step a number that was another runtime's, and the delta is per runtime because growth confined to one surface moved no scalar. `is_roster_path` is per surface to bound **depth**, not to stop cross-counting, which the scoped listings already make impossible. Accepting the split in prose was the fork put to the owner and he picked the mirror; shipping `.agents/skills` to adopters is bounded by what was actually measured -- the cached plugin holds no `.agents/` at all, so the probe shows the loader ignoring a directory Codex never reads anywhere, and the open half is #282's; the specification's validator is declined; `.agents` stays out of `REPO_ROOTS` as a root one directory deep rather than for `.claude`'s untracked-content reason. A negative answer about a skill's *name* is no evidence a surface failed to load: with the plugin installed both copies surface under the qualified identifier, and only the description discriminates. Shipped zone untouched, so no version bump and no outflow owed -- on the narrow reading, the descriptions being unedited and their reach being what changed |
| [D-280](D-280-2026-08-31-the-brief-binds-and-a-cold-seat-settles-the-reading.md) | **The owner affirms a brief, not the artifact**: an affirmed brief is the term and the artifact under it is the session's reading -- conflict resolves to the brief, silence to the reading -- with authority keyed to **affirmation rather than placement**, so a review report's brief and a handback's stay answerable to what they describe. [D-114] had already ruled the posted artifact *"a record, not a review surface"*, and declined a shorter wording that would have dropped *"never where he first reads it"* and the `convergence gate` naming; `engagement` carried that entry's ordering and never absorbed the half, and the incident is the implementing session briefing the record while omitting the brief from the message that put the decision. The brief **locks once nothing still open could invalidate it** -- a question adding only detail is the artifact's business -- is posted as its own comment, and a **cold seat with none of the conversation's history settles the reading**, both halves carrying an artifact-visible carrier per [D-261]. A brief sits **immediately before what it briefs**, the prior top-of-surface wording putting it as far from an ask as the message is long. **#116's crux dissolves rather than resolving** -- record and contract both reach the append route, the practice having already written the reason on #159 -- and its body's central evidence was backwards, corrected on the issue. **#248 was decided by an eight-seat run** recorded on that issue: its own route 1 falsified at 0 of 2, seats matching the shape *not for ... pre-implementation artifact* without reading the qualifier; deleting the exclusion reached the standard 1 of 2; naming the artifact in the **positive trigger** reached it 2 of 2, and that wording ships unmodified. Route 3 is unlawful in form; #255 carries the general problem. Contingent criteria re-anchor: **an owner's fork goes in the brief**, found by the cold check rather than the drafting. Two temporary owner approvals -- the ceilings raised, no outflow discharged for four always-on edits -- are recorded on **#260**, whose landing is their expiry, departing knowingly from [D-253]'s *no budget constant moved*. Splitting this change was recommended twice and withdrawn on reading [#245]'s *a ceiling reached is a trigger, not a wall*. The routing does not reach Codex, which loads no roster [D-210] |
| [D-291](D-291-2026-09-01-repo-only-cells-and-a-budget-on-the-rows.md) | **This repository's own procedure moves into repo-only cells under `docs/cells/`, and one ceiling on the always-on rows replaces two on the files.** A rule only this repository needs had two homes -- the always-on doctrine, or a `docs/` file plus a binding line in it -- because a shipped cell ships to every adopter, so repo-specific procedure was always-on by construction and the outflow's move-to-the-cell-that-fires had no destination for any of it. `landing`, `records` and `siting` carry the flow, the records and decision log, and where content goes here; `tools/roster.py` gains a second source and generates them onto both runtime surfaces, so descriptions load and bodies do not. Three cells rather than one was the owner's, put as a fork the affirmed brief did not reach. `AGENTS.md` keeps the charter import, the ceremony moments, the one-way wall a session can breach in its first edit, and a routing line. **The ceiling moves to what is read**: a budget on `AGENTS.md` and another on the charter body could not see a move between them, which read as a saving in whichever file shrank while the surface a session loads had not moved -- D-184's own diagnosis one level up. Not on `repo_total`, the smallest row, where the freed room from an `AGENTS.md`-to-`CLAUDE.md` move admits an addition that then grows both rows. The headroom is **exactly one substantial rule** and the exactness is load-bearing in both directions: wider admits the move the budget exists to refuse, narrower refuses the admission it exists to allow; the unit is the headroom `AGENTS_BUDGET_CHARS` was set with at `81fb1d9`, and that constant's own later form -- a ceiling above a measured body -- was rejected as the blocking condition under a new name. The check reds when the figure cannot be derived, gated on this guard's own presence rather than the figure's so the deletion bypass #134 records stays closed. Both owner approvals on #260 are discharged, the callout's temporary-ceiling note going with them. **Six guards and the merge gate follow the material rather than the file it left**, `DOCTRINE_PATHS` by prefix so the owner's merge-time read does not narrow silently -- the one regression this could have shipped invisibly. The cell-reference widening is fenced along the wall's own direction: repo-only may name shipped, shipped naming repo-only is a finding since the name form is the one shape `check_zone_wall` cannot see, and repo-only naming repo-only stays the mesh the rule prevents. Stage 1 of three; stage 2 lowers the adopter total this leaves flat, stage 3 measures and closes #260 |
| [D-295](D-295-2026-09-01-the-trigger-and-its-fix-batch-reach-the-product-consumer.md) | **The trigger reaches the product consumer**: an adopter completed a change and never ran its app, its session correctly reading a firing clause that named only a skill's behaviour and a mechanism's surface. The bolded test widens with its example list, because the predicate is reused at the fix-batch paragraph as its own justification. **That fix-batch limiter widens too, superseding [D-178]'s measured bound knowingly** -- three cold seats read the widening through and each had to cross two paragraphs to do it; the owner ruled acceptance over narrowing, on the ground that D-178's census was taken entirely over prose fixes and is silent about code fixes in a product, so narrowing would extend an unmeasured bound into the class the affirmed brief widened. **The decline stops sealing itself** -- *nothing was bought* and *the trigger never named my case* were one silence -- and is owed by any change that is not *purely* mechanical, the owner's own word, dropped in drafting and restored; its reason clause moves to one fitting only the exempt case, seven of seven seats having routed on the category rather than the reason. **The software procedure sheds to `skills/experience-session/references/running-instance.md`**, one-trigger depth every prose firing had loaded, and **two false claims about the tree procedure are deleted**: it does not produce a consumer that reads the diff -- a tree built by its fence has no commits -- and the sentence saying so fenced off the only code-job guidance the cell has, three of three code-job seats declining to open it. **Half B is held, but not for the reason first given**: its description is bounded by the per-field ceiling alone and fits today, so the deferral buys one outflow argument rather than answering a ceiling. Nothing mechanical holds it -- a body contradicting its own description lints clean, and the doctrine callout reaches no cell but the charter |
| [D-300](D-300-2026-09-01-the-boundary-and-its-two-crossings.md) | The work has two halves with the affirmation as the boundary; nothing substitutes for it, one question stops the far side, and one restraint governs what may be performed there |
| [D-307](D-307-2026-09-01-a-commissioned-probe-stops-at-its-datum.md) | **A commissioned probe stops at the decision it was commissioned for, and every bound on it is the dispatcher's to impose.** Five cold seats sent to settle one yes-or-no question ran the practice's whole flow for four agent-hours, one dispatching its own sub-sessions, because a job that does not name the question is a real job and this repository's doctrine expands one recursively. Two standards, both landing in `spikes`: the stopping bound as a fifth cold-seat property -- ask for the plan before execution and stop there, so the stop leads nothing where naming the step would -- and a scope paragraph extending the cell's existing disposal bounds to the seats a spike dispatches, since a recipient that does not know it is in a spike infers none of them. **S2 was first placed in `dispatch.md` and the review moved it back**, which is the entry's most useful half: the change's own experience session showed a cold consumer commissioning six probes, executing the stopping bound unprompted after reaching `spikes` by description alone, and never opening `dispatch.md` by any route -- all four seats found the same independently, and two external reviewers convicted the same paragraph from the other side, its *seats that mutate the tree* gate never opening for a probe stopped before it executes. The plan-stop's premise was **spiked before the criteria were written** -- two matched arms, same routing datum at a fraction of the cost -- and its limit is written into the rule rather than assumed away: a plan is stated intention, not conduct, so a datum visible only mid-run keeps its run, and what this bounds is then what the probe leaves and what it spawns, **not what it costs**. A pointer between the two cells is available in the plain-word form and unlawful in both, which the relocation makes moot. Fork 1 ruled narrow, leaving [#289](https://github.com/Grimblaz-and-Friends/tradecraft/issues/289) open; Fork 2 ruled in scope. Left open: `dispatch.md`'s own isolation sentence keeps the weakness the incident exposed, a plain worktree's branch cut from the change under review; and whether `spikes`, which this change takes past the larger of the two capped cell bodies, earns a budget row -- appended to [#302](https://github.com/Grimblaz-and-Friends/tradecraft/issues/302) as a second instance rather than filed fresh. **Two superlatives were carried unchecked on the way there and both were false** -- *largest cell body in the tree* (it is fourth), and then, in the very commit correcting that, *the only cell body anything caps* (two are capped, and `spikes` had already passed one at base); the review's own standards say load hides in superlatives. No always-on surface edited, so no outflow owed |
| [D-304](D-304-2026-09-01-the-second-adverse-verdict-and-its-carrier.md) | **A second adverse verdict on the same point stops the rework, and the carrier that makes the second one visible is the load-bearing half.** Two convergences on 2026-09-01 kept patching the sentence a cold seat had just rejected -- #260's five verdicts on one budget paragraph, ended by a table; #286's seven on one clause, ended by the owner ruling a question that was never the session's -- and both were stopped by him rather than by anything in the practice. `engagement` said what an adverse verdict does not do and nothing about how to respond to one, so the default was local repair; `authoring`'s restate-rather-than-qualify governs a hypothetical misreader and reaches one of the three remedies. **The trigger is keyed to the point -- the claim, criterion or choice -- never the text**, because a point survives a full re-draft and a move between sections, which a location does not; #290's own deferred list asks what counts as the same passage and this is the answer to it rather than a rejection of it. Three remedies, and the session picks: false about the tree, which no wording repairs, so probe it; the wrong shape, so restate it whole; or successive drafts deciding the same thing differently, which is a fork being closed silently and goes to the owner argued. A restate-only rule sends #286 into a fifth shape. **The design was reversed by its own spike before the criteria were written**: four cold seats, two reconstructed incidents, treatment and control, and both controls -- today's cell, no new rule -- reached the right remedy unprompted, each naming a probe and one of them the owner fork besides. So naming the remedies buys close to nothing. Its bound is stated and severe: each prompt laid the two rounds out adjacently, testing whether the remedy is known rather than whether the repetition is noticed, and the incidents say the second is what fails -- #260's artifact records its five verdicts as one aggregate and its enumeration was written after settling. So **the draft carries every adverse verdict it receives, one line per point a verdict lands on, from the first verdict onward, collected directly under the draft's purpose header** -- [D-261]'s strongest band, compliance visible on the artifact its reader is producing. The first verdict earns a line because the second has to be adjacent to something. **No guard and no round cap**: no script decides *the same point*, a round counter fires on the exempt first verdict, and #290 attributes the waste to the response rather than the gate, #260's round 5 having produced that change's highest-value find. The second remedy is written out rather than pointed at `authoring`, `CHARTER_CELL` permitting a shipped cell one target at depth one. The owner **declined** a general rule spanning instruments, whose home would be the charter, and declined admitting nothing; #95 is the same shape at review with a per-incident remedy. **The carrier is an obligation he did not see** -- a standing cost on every future draft, wider than the case the brief describes -- reported as the session's reading rather than re-agreed, on the ground that it is a means to the affirmed trigger rather than a new end |
| [D-306](D-306-2026-09-01-the-charter-gains-the-caller-and-sheds-the-ladder.md) | **The charter carries the obligation to use what a change built, and sheds the admission ladder to pay for it.** Half B of the adopter incident [D-295] opens: Half A fixed the cell for a session that reaches it, and an adopter is routed there by nothing it is guaranteed to load. The **Release** bullet gains the obligation -- before a change merges, one that altered how a later session must work or what someone using the result can do owes a use of it, or one line saying why none happened -- **sited inside the bullet rather than beside it**, because a third would contradict the charter's own *exactly two moments* four lines above. It **carries the timing itself**, established by reading rather than by the obvious probe, which is hollow -- `grep -i merge` over the base charter returns nothing at all, *merge* not being a substring of *Merging*, so it cannot see the one line about merging the base charter did carry. The charter dated no obligation to before-merge; the cell's *before the review closes* is a different gate that nests inside merge rather than competing with it. It **routes rather than restates** -- though the trigger predicate is a copy of the cell's, a cost the entry states rather than claims to have avoided, the first draft having copied only one of its two limbs until the review caught it. The disjunction is also what stops it reading as the gate the cell says it is not. **The outflow is a deletion, not a relocation** -- the admission ladder leaves for `skills/authoring/references/routing.md`, a `references/` file loaded on demand, so the rungs leave the always-on surface rather than moving across it. What stays is the authority half, which `grep` finds in exactly one place in the shipped zone, making over-deletion the one wrong build no later edit recovers. **The deletion was contingent and probed before it shipped**: the settled artifact staked it on three cold seats reaching the ladder through the shortened sentence, a majority failing having refused it; 3 of 3 reached it and ran it in order. The recorded entry keyed `skills/authoring/references/routing.md -- where a discharge or a deletion's evidence is written` is met and unpromoted, its evidence in both an entry and the PR body. Rejected: a third bullet; restating what the cell carries; rewording the pointer instead of refusing; reaching past the cell to its reference, which one seat urged and which the body-plus-references structure forbids |
| [D-314](D-314-2026-09-01-dispatch-states-the-extent.md) | **The dispatch states the job's extent, because the box does not.** An experience-session dispatch pairing a time-box with a job whose extent the material sets -- *work out what this change owes and do it* -- states two things at once: this practice answers that job with a review and a session, each buying a further look, so the job is unbounded while the box caps only duration, and the consumer resolves the pair toward the job. [PR #263](https://github.com/Grimblaz-and-Friends/tradecraft/pull/263#issuecomment-5472614246)'s second run did exactly that and its note had to spend space separating the dispatcher's defect from the finding. **The spike reversed the reasoning before the criteria were written**: six cold seats, two arms differing in this paragraph alone, each stopped at the dispatch it would write. The premise the report recorded testing -- pairs a box with a deferred-extent job **and does not bound it** -- **held**, no control seat having stated an extent; what reversed is the justification, three of three having named the contradiction unprompted, so the rule cannot be argued from the clash being invisible. What none reached was the remedy: two declared a tiebreak and the third dropped the box from its dispatch entirely, and a tiebreak leaves the consumer holding an unbounded job and a clock, reproducing the #263 shape by construction. Three of three treatment seats stated an extent instead. So the rule **names two moves** rather than asserting only that the box does not bound the job -- the bare assertion is what the control arm already had. Criteria 1-3 are **pinned to this wording**, a cold seat having found that a falsifier naming an already-run A/B measures nothing unless the built text is the tested text. Rejected: routing up to the charter to cover every cold dispatch, `spikes`'s [D-307] sibling being a different standard for a seat that must *not* do the job, and the evidence being one incident; a rule prescribing the tiebreak, which the control arm invents unaided and which is the worse remedy; banning the deferred-extent job, which this repository's own flow makes routine and which produced [#274](https://github.com/Grimblaz-and-Friends/tradecraft/issues/274); and the Time-box bullet as its home, that bullet making no scope claim and sitting under a heading whose own text resists growth. #274's material-side half -- whether the fix-and-look cycle terminates outside a review at all -- is untouched, and the rule holds either way. **Its own review then found the rule half-built and the record drifted**: the extent was required and never required to be *performable* -- an owed step carries parties, not only time, and the change's own session capped sub-agents at one and lost the cold seat it had put in scope -- and the note's disclosure sentence, keyed to a failure of *authority*, did not reach a step the dispatch excluded. Both fixed outside the pinned paragraph, which is unchanged from the tested text. The entry's first draft also declared falsified a premise appearing in no pre-run document, and pinned its evidence to a branch commit no plain clone of the default branch resolves after landing; nine findings, most needing a reworded paragraph and a six-seat re-run to buy a low, are recorded rather than fixed |
| [D-315](D-315-2026-09-01-every-cell-body-is-sized-at-the-mandated-checkpoint.md) | **Every cell body is sized at the checkpoint the flow already mandates, and nothing is capped.** `check_doctrine` iterates `CELL_BODY_BUDGET_CHARS` rather than the cells, so a cell absent from that map was sized by nothing at either mandated command **save the charter**, whose body is a term in every always-on row and was already printed at that command -- and the absent cells had become the large ones, the three governed bodies standing fifth, sixth and seventh while the largest unbudgeted body was more than twice the larger body cap. **Reporting and capping are separable**: the map's sparseness is deliberate, a number for a cell nobody argued about being a ruling arriving as a constant, so this takes the reporting half only -- no entry added, no cell redding, the per-cell numbers left to #260's stage 2 which redraws these bodies. A block, one row per cell, largest body first, derived from `roster.SOURCES` so a third source is not silently unmeasured, hosted in `tools/figures.py` and loaded by path because the failure behaviour is testable only where a fixture can omit the module. Reports rather than reds and never goes silent -- *nothing to report* and *could not derive* each produce text. Nothing evaluative, guarded as a shape over every row rather than a word list. **The charter row is the finding four seats were needed for**: its body is a term in every always-on row and in the adopter total, so it is enforced today while absent from the map, and a report treating that map as the only budget prints a false `no budget` against the one capped cell that looks uncapped -- three seats passed over it and the criterion as written ratified the wrong row. Both constants named, marked shared, no per-cell headroom, since the row budget alone reads as ten thousand characters of room where a few hundred remain and the adopter total binds first. Rejected: a relative trigger firing when an unbudgeted body passes the largest budgeted one (reds several cells now and forces the deferred arguments; put to the owner and declined); #275, whose string is rendered in the shipped zone so the fix is adopter-visible or leaves adopters wrong, left open with that fork named; joining `build_figures`; and capping a cell total, which caps depth-shedding. Five verdicts, two NO. The ordering criterion was named by three seats, which under [D-304] is **wrong shape** rather than wording -- confounds chased into a fixture are an unclosed set, and it restated as a total property over the output. Eight pins, each mutation-verified with bytecode cleared |
| [D-317](D-317-2026-09-01-the-installed-version-check-is-declined.md) | **A check comparing the installed plugin version against the working tree is declined, because it has no consumer.** `check_version_bump` enforces that the number rises and says why -- *a consumer cannot tell installed from current* -- and nothing reads it after; this machine ran six releases behind for about seven hours -- `git rev-list --count ba49e68..e333441` is 6, `945fe6b` landed 2026-09-01 09:22:47 -0400 and the install's `lastUpdated` is 2026-09-01T20:26:22Z -- closed by hand. [#313](https://github.com/Grimblaz-and-Friends/tradecraft/issues/313) filed the cost as reviews judging changes against replaced doctrine, and **that attribution was withdrawn**: tool-free seats asked for the `Contents of <PATH>` lines in their own prompts returned the dispatching session's working directory under both plain and worktree-isolated dispatch, and no plugin-cache path at all, so the stale doctrine came from long-lived worktrees and the evidence went to [#284](https://github.com/Grimblaz-and-Friends/tradecraft/issues/284). Four reasons, strongest first: no consumer -- only this repository's sessions have a tree and the roster stub redirects them into it, three cold seats confirming, while an adopter has no tree at all; no lawful firing site, [D-222] having removed lifecycle hooks and CI having no install; a check a session must remember re-creates the failure, which was that nobody looked across six consecutive releases while the guard ran on every commit that produced them; and the version is a poor handle since comparing it presupposes the fetch. **The prose that ships is two sentences because the larger claim was probed and failed** -- four A-arm cold seats given the unchanged install section each knew a fetch of their own was required, one articulating it as *two gates between the merge and your working tree*, and none naming a restart or `claude plugin update`, so the brief was amended rather than the prose reworded [D-304]; what none of them supplied was the restart `--help` states and the `--scope` that defaults to `user` against a `--scope project` install. Left unsaid deliberately: no update order, the marketplace-refresh behaviour never established and the sequence never run end to end, against a file that two paragraphs above refuses unmeasured CLI claims; and no Codex command, `codex` not being on PATH. Rejected: a line in the `landing` flow telling sessions to update after a shipped-zone merge -- the remembering trigger in another costume, firing only for whoever merged |
| [D-320](D-320-2026-09-02-the-fences-are-measured-and-nearly-all-of-them-are-live.md) | **The roster's exclusion clauses are measured, nearly all of them are live, and the deletion rule keeps on a single citation.** Twenty-one fence clauses across the nine shipped descriptions, 1,176 characters of an adopter's 11,079 at `fbb6cc4` -- two sentence-initial and missed by the case-sensitive count that read nineteen. Nothing had established that any of them works: #217's 6-of-6 on near-misses was an **outcome** measure in an intact arm only, and #272 is the demonstration that outcome and warrant come apart. **So the measure is the warrant** -- whether a seat's reason for *declining* the excluded cell is the exclusion -- with one distinction the instrument had not stated and which decided the only deletion: **a decline reasoned from the absence of a positive trigger is not a citation**, where a seat attributing an exclusion to the entry is. **Deletion requires a per-clause run where no seat in either arm cited the fence clause; anything else keeps**, one citation by one seat being enough: keeping an inert clause costs characters, deleting a live one costs a session routed where the clause existed to stop it, which no guard catches. The rule's subject is a fence clause, not a positive trigger, where it would contradict #272. Every licensing run carries a planted near-miss fence, never landed, and **where it rides differs by run shape** -- intact-arm-only in the screen, whose ablation strips every fence including it; **unchanged in both arms of each confirm**, which is what makes those arms differ in exactly the clause under test, as `spikes` requires. A run whose control goes uncited in its intact arm is void; a run on an item no seat treated as live is *uninformative*, a rule `spikes` already carries in substance [D-173]. **Twenty-one tested, twenty kept, one deleted**: `engagement`'s *not for how a review is run.*, where all six seats declined the cell with cell-specific reasons naming its own positive trigger four clauses above, resolving on timing rather than topic, identically in both arms -- compliance with the mandated per-cell walk certifies nothing, since that walk cannot return the negative. A second warrant stands independently: the base description disclaimed review procedure while the body specified what a review's report carries, its fix bar's input, and what every stage inherits. **Twenty of twenty-one were cited by at least one seat, and five changed conduct rather than only stated warrant** -- one (`spikes` #0) on its own per-clause confirm, four on the screen, whose fence-free ablated arm attributes them to the cell's fence set rather than to the single clause -- so **#260's premise is falsified**: the fencing is load-bearing and the roster half is not where the slack is. Where the slack is instead is the charter body (5,851 = 52.81% of the adopter total at base, 52.97% at head; larger than all nine names and descriptions together; never ablated), and that measurement went to #303 rather than being acted on, the owner having set that edge. **Two ceilings a later session hits.** In five places an *ablated* seat reconstructed the removed clause's substance, so **a clause inferable from the cell name alone can never clear this instrument**. And the charter body rode unablated in every arm while naming four of the nine cells -- the second route #272 raised and this instrument did not carry across; a residual here, since the charter named `engagement` three times to `adversarial-review`'s once and no seat cited it, but **structural for #303, where the charter body is the ablation target**. **And a malformed docket item reads exactly like a dead clause**: `spikes`'s screen item echoed the substance of the cell's own positive trigger, every seat opened the cell in both arms, and rebuilt the clause returned 3/3 against 0/3; that contradiction is #321. The deleted clause's own item carries the mirror defect, opening with the *correct destination's* first trigger, which is why the warrant rests on the six declines and not on the routing outcome. The thirty seat outputs were not retained; the docket and elicitation are on the issue. Rejected: re-partitioning the roster (all nine triggers are moment-shaped already -- seven open on the phrase `Use when`, `charter` on `Use this before`, `persist-changes` on `Use whenever` -- the merges that would shed most fence text are zone-illegal, and the one legal merge loads 28,785 characters of body at that base, 29,956 at head where a session loads roughly ten thousand); ruling on the charter; any size limit; editing any description's summary half; admitting the instrument's four rules to `skills/spikes/SKILL.md`, a design call carrying standing obligations for every later cold-seat A/B (#325); and treating #217's 200-400 runs per clause as binding, that price being a behavioural readout where this is a warrant one that separated five runs out of five. Three artifact verdicts, two NO; then a five-seat review that found the arm placement written backwards, the near-miss certification stated in a form that cannot fail, and this entry's evidence resting on one off-tree link no guard can see (#324) |
| [D-322](D-322-2026-09-01-the-cold-seat-gets-a-procedure-and-a-third-verdict.md) | **The cold interpretation check gets a written procedure, and its verdict gains a third thing to say.** The stage is mandated by the charter and ordered by this repository's `landing` cell, and had no mechanism -- no `skills/engagement/references/`, no `commands/`, no `agents/` -- so every session invented the dispatch. Three filings are consequences of that one absence and land together, #297's own crux (whether the dispatch rather than the document carries what a later seat needs) being answerable only because #308 builds a dispatch to put it in. **The dispatch gives the seat the draft and the brief and nothing else that aims attention**, derived from the cell's own handoff test rather than chosen -- a seat given more settles the reading against material the implementer will not have; hence no issue thread. **The artifact travels inline with its sha256 and byte count**, because at that moment it is committed nowhere and posted nowhere, so a path names what the seat may not read and the later reader cannot pin, and a digest whose algorithm and unit are left to the session cannot be recomputed where line endings normalise on the way in. It does read the working root, which the dispatch names with the revision the seat confirms -- or, where the tree has no commit, says so, a tree built for a run of its own commonly having no history. One seat per verdict; the seat dispatches nothing of its own; **the vocabulary and the closed list travel in the dispatch too**, the seat not reading this file. **The third verdict is `not settleable`**, discharged by carrying the fork contingently -- **its own exception to the draft-again rule and not an instance of the reading amendment**, which the cell defines as a further comment and never an edit; the reason is that the seat found no reading settles the artifact, so a revision performing the remedy it named cannot fail a reading it never affirmed. Because the issue is withheld, the file states what the seat judges a closed fork against: a criterion turning on an answer *the brief* does not settle. That gap was found by a cold seat, not the session. **The bound on re-runs is not a count**, which licenses shipping unsettled work at the cap: it is [D-304]'s repeated-point rule -- pointed at rather than restated, the second statement having drifted to the unit D-304 rejected -- plus a carve-out for a revision that **neither changes nor adds** a claim the verdict rested on, adding being in the condition because an external pass found a new criterion slips a change-only test. **Five claims in this entry were false and are corrected in it**, four inherited or carried rather than derived: the mandate attributed to `AGENTS.md`, which carries neither *cold* nor *flow*; *the only operational definition of cold*, contradicted by two shipped cells; *both limbs met* on a disjunctive promotion clause whose second limb is about dispatching rather than resuming; the round that found the withholding gap; and the count of criteria defects among round one's three points. **The first fix batch shed three clauses and grew the file by about a fifth** -- a near-copy of a `spikes` sentence, an uncited seat-count claim, and the duplicated rule all leave, while two paragraphs arrive; an earlier draft of the entry called that net-shedding, which the post-fix look disproved and the entry corrects, the second pass then being held to no increase. Rejected: a round count; giving the seat the issue; resuming a finished seat; widening to #274; pinning wording that would pre-empt #288. Sent to the board rather than fixed: routing the shared cold definition to the charter, and shedding `SKILL.md:52` into the new file. Recorded: the `adverse verdict` extension, the pointer's narrower load condition, and two guard gaps. No always-on surface edited, so no outflow owed |
| [D-327](D-327-2026-09-02-board-transport.md) | **The board's two reads are different objects, and four GitHub behaviours are load-bearing rather than incidental.** The standing answer to *what to pick up next* gets a Projects v2 board and `tools/board.py` as its transport, so the maintaining agent spends context on judgment rather than plumbing. **Reconciling and settling are two jobs and only settling may halt** -- reconcile asks whether the board holds the open set, settle asks whether the ordered read has caught up, and they return opposite answers about the same newly added issue because reconcile's own writes are what make the read short; an earlier draft merged them into one comparison that a cold seat walked and found would halt after every filing, which the artifact itself called the normal steady state. Merging them back is the undo the entry exists to prevent. **`totalCount` goes stale in lockstep with the list it counts** -- twelve items added read back as ten items and a `totalCount` of ten -- so a short read is undetectable from the connection's own count and the target membership always comes from `gh issue list`; the same defect the hand-ranking hit from the other side, claiming 65 open placed where it placed 81. The unordered read was full at that moment, an observed run and not a deduction, `ProjectV2.items` declaring a `defaultValue` of `{field: POSITION, direction: ASC}` that says the two queries should be identical -- **but it is not fresh either, and the design does not rest on its being so**: a trial run's second pass read 78 of 86 items from it, and what makes acting on a stale read safe is that `addProjectV2ItemById` is idempotent and settling is the gate. Reading either board query as authoritative is the misreading; the issue list is the authority and the idempotence is the tolerance. **A single-select option re-sent without its id clears the field**, `updateProjectV2Field` overwriting wholesale and the input's own documentation saying so; the seed lost every `Bundle` value to it, silently and with a successful write, and options now carry ids and are added one write per run. That rejects option order as a second ordering primitive -- cheaper per move, but it puts the destructive write on the hot path and leaves two orders to reconcile. **`Band` is the ranking's stratum and `Status` is availability**, and collapsing them is the second undo: at seed, positions 1 and 2 are `In progress` and `Deferred`, so a consumer reading position 1 gets the wrong answer; the rule is the first item carrying none of the four unavailable states and not blank. **`In flight` is asserted rather than corroborated** -- the settled artifact said the built-in `Linked pull requests` field would maintain it and the transport never reads that field, corrected in the entry rather than left describing something built. Four cold seats settled the artifact, three returning NO across four points -- an unreachable seed source, no criterion falsifying a re-rolled first ordering, a run order that halted on the ordinary case, and a missing in-flight carrier. A five-seat panel, defense and judge then ran on the change: 30 fixes, 4 drops, 2 records, one lens retired, and two probes settled by run -- `statusUpdates(last:N)` returns the **oldest** notes, and `addProjectV2ItemById` appends at the **tail**, which disproved the panel's headline that a just-synced item could become the answer. **[D-128](D-128-2026-08-23-filing-cell.md)'s `Reopen on: #83 landing` has fired** and is named in the entry: *the board* now has two referents across always-loaded descriptions, with no consumer failure, and the reconciliation is left to be designed across `filing`, `engagement` and this cell together. Rejected: severity and cost labels and RICE/WSJF, which display a ranking rather than produce one; a ranked file in the repository, every re-rank becoming a bookkeeping pull request; and epic-level grouping, #218's trigger having fired at about ten groups but its subject -- the parent artifact and the emergent criteria no child owns -- untouched by a flat label. Nothing decides when the agent runs, excluded by the affirmed brief. The fourth cell's outflow is the doctrine's per-cell glosses, every cell description already loading in every session here |
| [D-336](D-336-2026-09-03-width-retires-on-local-evidence.md) | **The width claim retires on this repository's own record, and five becomes the written default for the modal change.** [D-185]'s trigger fired: 25 panel-lane reviews booking sustained highs ran in the eight days after the finder clause landed, and five extraction passes recovered 81 panel-originated sustained highs, **55 attributed, 50 naming a primary**. **The August reading reverses.** On 23 attributed highs it had concluded three seats retain 100% and *"the fourth and fifth seats add nothing"*; on 55 that is a small-sample artifact, and the two seats that sole-found nothing in August sole-find 5 and 2 now. The two narrower panels this practice floated are refuted by the same data -- #138's own executing-plus-claims pair retains 62% counting every credited finder and **30%** counting primaries alone, the weakest named option. **What ships is retention for the panels this roster permits, not the recomputation's hindsight-optimal subsets** -- a five-seat review found the first attempt printing the latter, whose measured best-four *excludes `wiring-falsifier`*, a standing seat, so every interior point described a panel this roster cannot staff and the sentence attributing a two-high marginal to *"the fifth"* inverted which seat the evidence supports: read in the file's own vocabulary the fifth is the second shape seat, and the two shape seats are the sole finders of **12 of the 20** sole-found highs. A seat also found the **primary-only curve's denominator is 50, not the 55 the sentence attached it to** -- an error inherited from the source, whose own table falsifies its own header -- and that the two curves are not over the same subsets. Both estimators ship with denominators explicit, and each panel carries its own bound: 26 of 81 highs name no finder, all but one because **six whole reviews published none**, so over all 81 the four seating `claims-vs-evidence` lands between 62% and 94% and the one seating `revision-diff` between 59% and 91%. **The owner's fork, settled at the recommendation:** the predecessor's figures retire rather than sit beside the local curve, since 12% is yield across every severity and the local curve is highs only; the single-pass 12% survives because the affirmed brief's spike comparison needs it named, the best-seat 46% is dropped as surplus. **Direction 3 resolved toward blessing five** rather than tightening the both-shapes clause, with its recorded con named and unpriced -- the modal round's recorded-finding rulings rise 68 to 85. A seat filed the change as exceeding its brief; defense and terminal stage both dropped it, the brief having named the disagreement, named the summary page as wrong, and told the owner most sessions read a number they will read differently after. **The brief's own *"nothing about how wide our panels actually are changes"* is true of practice and false for a `SKILL.md`-only reader**, recorded here so no later session re-derives the effect from it. Defense discovered `roster.md` is **unbudgeted** -- `CELL_BODY_BUDGET_CHARS` holds two `SKILL.md` keys -- re-pricing a docket every stage had treated as budget-blocked; the recorded finding requiring a remedy in that cell to be *net-neutral or shorter* is true of the body and false of `references/`. Recorded rather than fixed: the line-pin drift that broke two entries' `SKILL.md:41` citations, unlanded branches also at 0.70.0, and that `doctrine-callout` cannot fire on a change rewriting the practice's central width rule. Filed out: the recorded finding on per-seat rulings of carried entries, whose promoter fired at 17 entries x 5 seats = 85 rulings. Every dispatched stage of the review arrived on the default branch rather than the revision under review |
| [D-337](D-337-2026-09-03-the-instrument-rules-move-to-the-cell.md) | **The cold-seat A/B's instrument rules move to the cell that owns the method, split by what each rule is for.** D-320's fence run established four rules deciding whether a run's result can be trusted and wrote them down only in that entry; a decision entry informs and never binds, so a session opening `skills/spikes/SKILL.md` found none of them, and that same run had shown **a malformed docket item reads exactly like a dead clause** -- the failure they exist to separate. The owner ruled the one fork: **the planted control, the void rule and the forced per-item verdict bind any cold-seat A/B whose finding could be a null**; **keep-on-any-citation binds only a run whose output licenses a deletion**, being an asymmetry that means nothing where no text is removed. Rejected: all four scoped to deletion runs (leaves every other run reading nulls with nothing to say they are uninterpretable, and the motivating incident is not deletion-specific), and all four universal (makes keep-on-any-citation a rule about nothing on most runs, which trains its reader to skim). Placement follows the cut and is the session's: the general rules in the body, the deletion conditions behind a pointer in the cell's first `references/` file. Two rules landed the brief did not name, both reported as the session's -- **what counts as a citation**, without which the keep rule cannot be applied, and the **near-miss docket rule**, scoped to the general three because an item that is not a near-miss is what made a live clause read dead. **The forced walk runs after the unprompted question, never instead of it**, or it contradicts the sentence already above it. Five seats, a defence, a judge, a fix batch, a prosecution look, a second defence, a terminating ruling: **nine sustained highs**, four of which failed the purpose statement in its own words -- a whole-class run could license a deletion, a seat dispatched in-repo collapsed both arms while the plant still certified the run, a run that separated cleanly was discarded, and a void run was never *shown* void because nothing reported it. **The review turned on the owner's ruling itself**: the commit message stated the scope and the tree did not carry it, **the merge dropped that finding from both its lists**, and the defence recovered it only by reading the seat reports rather than trusting the merge -- recorded as an apparatus finding. Retracted here: the merge's claim that the cell *prescribes* the collapsing arrangement (it does not -- the detached-worktree sentence is scoped to a different spike, and the defect was a missing warning), and its presentation as fact that #303 is a whole-class ablation by construction, which is an inference the source entry does not state. Two experience sessions, and the second changed the outcome: it **evidenced acceptance criterion 5**, which the terminal stage had refused to record as met, by reaching the reference file **from the body** on a run licensing no deletion -- which the pre-fix load condition would have shut -- and it **falsified criterion 7's headline** while leaving its stated falsifier unmet, the cell's rules describing an ablation where its own trigger names a substitution first (#342). Also rejected: amending routing.md's deletion grounds (out of scope by the affirmed brief), fixing #321 here, setting a budget for `spikes`, pointing the cell at the isolation procedure (fires `sideways-dep` twice against a clean control -- probed, and routed to #341), reverting the near-miss widening (would reintroduce a defect with a recorded incident to close one with none), and spending the second review cycle on five word-level substitutions already written out verbatim. The keep-evidence question went to #340 as one question with three limbs. **Cost, stated because the next change inherits it**: the body 9,619 -> 12,857 (+33.7%), `spikes` fifth -> **second of thirteen**, no budget, plus a 2,455-character reference file; the fix batch came in at roughly **five and a half times** the accounting round one published, because the two additions the judge rated most serious were the two its estimate had not priced. **No always-on row moved** on four independent runs at both ends |
| [D-338](D-338-2026-09-03-the-routing-criterion-is-keyed-to-the-second-route.md) | **A routing criterion names which quantity it claims, and reach is falsifiable only where nothing else routes the situation, or routes it too weakly to mask the difference.** `skills/engagement/SKILL.md` carried [D-166]'s general rule against a criterion a plausible wrong implementation would also pass, and no instance of it -- so PR #269's criterion 1, *a session … reaches this cell*, was affirmed, run and marked met, and #272's spike then reached `substrate` in every one of its six seats, three per arm, including the three whose arm had the trigger deleted. One bullet is added beside that rule: a criterion claiming a piece of text routes a reader says whether it claims **reach** or the reader's **stated warrant**, and reach is falsifiable only where nothing else the reader loads routes that same situation to the same place, or where what does routes it too weakly to mask the difference — the destination's own summary sentence above the trigger, and an always-on surface naming the destination, both named because both have defeated a run. **Keyed to the second route rather than banning the reach form**, which was option 2 to the owner and not ruled: #255's port run separated on reach 5/6 against 0/6, so a ban would outlaw a measurement already run here. Its `brief` case saturating 5/5 in every arm on the charter's own sentence is the same mechanism seen as a control. **Amended by its own review**: the judge's rewrite struck an overreaching consequence clause and an evidence label, and named the ceiling [D-320] measures on warrant; the external pass then earned the saturation clause, since a route that does not saturate still leaves an ablation able to separate ([D-210]). Licenses no deletion -- the `substrate` positive trigger #272 priced at 39 characters stays, its contradiction with [D-320]'s keep-on-any-citation being #325's. Sustained and not fixed: the mirror polarity, to the board; `spikes`'s insufficient arm condition, to #325. |
| [D-345](D-345-2026-09-03-the-routing-criterion-covers-the-mirror-polarity.md) | **A routing criterion covers text that keeps a reader away, where reach is the quantity least likely to survive.** [D-338] landed the routing-criterion bullet positive-polarity throughout and recorded the mirror as sustained-and-not-fixed; a fence clause claims the mirror and neither the subject nor the enumeration transferred. **The mirror is the polarity this repository has measured** -- [D-320]'s run tested twenty-one exclusion clauses across thirty seats, and reached D-338's own thesis first in this polarity without generalising it. The bullet's subject widens to text that **moves** a reader, toward a destination or away from one, with both quantities restated to read in either. **A third route joins the enumeration, labelled reasoned rather than measured: the reader's own indifference**, reaching only text that keeps a reader away and there the ordinary case, since a reader with no pull toward the destination declines it whether or not the text is there and the situations testing such text are near misses by construction. **Least likely, not unavailable** -- the absolute would be falsified by D-320's record, of which the five conduct-changing clauses and the four-of-twenty screen rate are one result and its restatement rather than two — the entry first presented them as additive and a review corrected it, the conclusion surviving on `spikes` #0 at clause resolution alone, and that comparative is what a later session is most likely to flatten. **Route 1's wording widens with it** in two moves — head noun from *summary sentence* to *description*, qualifier from positional (*sitting above*) to functional (*settling the situation*), which is the load-bearing half — and the position clause *above the trigger under test* is dropped in review, because **D-320's one licensed deletion is route 1 measured in the mirror**: six seats declined the cell giving a cell-specific reason naming the cell's own positive trigger four clauses above the one under test, identically in both arms. Route 2 is present in the mirror as a residual, flagged structural for #303 and carried unresolved, so the shipped bullet says only the first route has been measured in that polarity rather than calling both measured. Route 1 ends wider than the warrant sentence beside it, deliberately and on D-320's evidence, recorded here rather than stated on the shipped surface. **`engagement` names no sibling and carries no copy of the citation disqualifier**, which ships in `skills/spikes/references/licensing-a-deletion.md` since [D-337] -- `skills/authoring/references/cell-structure.md` refuses it, both cells shipping so the one-way exception is empty, and `tools/lint.py` catches it too in `check_sideways_deps` — a first-draft claim that it did not, naming the wrong function, was probed and withdrawn in review; the standard's two remedies are declined with reasons, routing to the charter being the surface #303 exists to shrink and one-owning-cell being what already holds. Rejected: a second bullet (option C, duplicates the transferring warrant sentence at a cost first stated as double and withdrawn in review -- 246 characters against the 721 spent, nearer 1.2x); routing the criterion-writer to `spikes` (option B -- that file fires when a run is scored or text deleted, not when a criterion is written; a second ground first stated, that `engagement`'s body routes there, was withdrawn in review, the body naming the method and not the cell); generalising to any criterion about reader behaviour, D-338's ground still holding for other shapes; and a run to isolate the third route here, D-320 having isolated no decline attributable to indifference and the bullet saying so. Half the filing was discharged before pickup by D-337 landing hours after it, and the filer posted the correction. Three cold seats, each a fresh dispatch, two adverse: the sibling-naming breach, and an unconditional conclusion asserted against the contrary half of the entry the draft claimed to have read in full, then the claim that the two enumerated routes keep their positive-polarity scope. **One figure retracted** -- *four recorded findings decline fixes on that cell for size*, carried from D-338, which states it of the same cell and the same question; the query returns eight at every tree, so nobody ran it in either entry. Recorded rather than repaired: the one-clause-across-six comparison understates D-338's two-run base, and the artifact's *two edits* undercounts three. No always-on row moved |
| [D-346](D-346-2026-09-03-a-ceiling-admits-on-an-itemised-record.md) | **A ceiling admits a needed item on an itemised record, and the constant does not move.** None of the four ceiling findings in `tools/lint.py` offered a way to admit a needed item, which the owner ruled a size limit must never block: the always-on row finding named three answers -- *"retiring a cell, merging two, or raising this ceiling as a recorded decision"* -- `doctrine-budget` named two relocations and no raise, and the description and adopter-total findings named none at all. Three sources are on the record, six instances between them, each priced rather than judged: #303's four charter gaps; [D-184], where the description cap forced deleting examples [D-141] had placed; and #260, which needed a temporary owner approval to exceed the ceilings at all. **The fourth answer is an admission** -- one row on `docs/admissions.jsonl` carrying `date`, `issue`, `ceilings`, `chars`, `item` and `outflow`, with each site enforcing `constant + what is charged against it`. **The itemisation is the mechanism**, and it is what `ALWAYS_ON_ROW_BUDGET_CHARS`' own comment says no number can be: headroom and the largest tolerated relocation are one quantity, so a raise buys both and the next commit spends it, while an admission adds exactly the characters its row names and leaves the next addition with nothing. The constant stays put, so every surface reports the ceiling in force *and* the constant it is built from. **Two findings stop it becoming a waiver**: a ceiling still carrying admitted characters once its surface is back at or under the constant is banked by an **append** with negative `chars` -- a new fact, not a correction, so the append-only rule reaches it and the no-maintenance rule does not; and a row naming no issue, or one the reader cannot parse, admits nothing and says so, `read_admissions` failing closed and `check_admissions` reporting what it dropped. The route rides on the finding text, which is the artifact a session at a ceiling already has open. Rejected: **stop failing and speak only at merge** (option 2, not ruled -- spends value 3 on every exceedance, and nothing opposes the ratchet between merges); **a soft band under a hard cap** (option 3, not ruled -- a band is a ceiling, so it inherits every objection the ceiling had, and a needed item is blocked again at the top of it; the *numbers fill to just under and stay* reason first given is withdrawn, falsified by what `python tools/lint.py` prints on any run); **body budgets for the eleven uncapped cells**, raised by the owner in the same conversation and ruled out -- that is [#328](https://github.com/Grimblaz-and-Friends/tradecraft/issues/328), and doing it here would have seeded the new record with rows meaning *this was already like this*; any constant change; shipped prose, which already says a ceiling is a trigger and not a wall, so the shipped zone is untouched and no version bumps; and widening `DOCTRINE_PATHS`. **Costs stated rather than left**: zero slack is not enforced, so between the constant and the effective ceiling freed space is spendable, reported rather than policed; and the lint does not check `chars` against what the change grew -- the change's own probe hit that, a 150-character row for a 42-character overage leaving the next character fitting. **The tests found a live defect the design did not**: `str(None)` is `"None"`, so a JSON `null` `issue` had been admitting past the one field that separates an admission from a waiver -- and the five-seat review then found that fix had landed at one of its three sites, with `item`, `outflow` and `issue` itself still admitting `[]` and `{}`, so acceptance criterion 6 held on its literal falsifier and failed on the natural reading of *empty*. **All four of the review's sustained highs were perimeter, not arithmetic** -- the mechanism's own composition held under every probe across two cycles. Round one: the merge callout's adopter half stated the constant while the per-runtime half beside it composed correctly, printing an exceedance on a green tree at the owner's merge surface; and the tree criterion 1 says must land clean redded four required-check pins, three of them consumers of the redefined constant that were never re-founded and one -- `assert rows == []` -- added by this change, its cheapest remedy being to delete the lawful row. Cycle one added two more, each inside the repair for the class it belonged to: the pin written to close the second high carried that high's own defect on the fourth admission key, which the round-one ruling's binding check had never named because it said three key shapes where four exist; and the route sentence written to close the double-charge generalised a two-findings-one-ceiling incident to the two-ceiling case it does not fit, leaving 458 characters of unargued headroom on a green lint until an experience session's consumer met the shape and trimmed rather than banked. The record lands empty, and the suite pins its shape rather than its emptiness |
| [D-347](D-347-2026-09-03-whose-leaning-counts.md) | **The deletion instrument says whose leaning counts: an ablated-arm-only run is `unresolved`, a record hit keeps and stops the dispatch, and the void gate reads every arm whose silence is read.** [D-337]'s review raised three questions about what the cold-seat A/B counts as evidence and recorded two of the three (the record-hit limb has no row; #337 left it open in the shipped text), none settleable by the finder because every answer changes what the instrument licenses; each was reached independently by two parties, which is an independent arrival and **not** an incident, so all three waited on the owner's specific approval and this entry records that approval as much as the rules. **1B**: a citation from the arm *holding* the text is a keep, one seat being enough; where the only citations came from the arm it was *removed* from the run is `unresolved` -- the text stays on the same cost asymmetry, and it may not be recorded as tested-and-kept. The text stays either way, so what this buys is the record: a keep asserts something about the text, `unresolved` asserts the instrument was blind. Counting an ablated-arm citation *toward* deletion was rejected on width -- three seats reconstructing does not establish the ninetieth reader will. **2B**: a record hit keeps the text and stops the dispatch, on the instrument's own logic rather than a new principle, the rule already keeping on one cold seat working a docket somebody wrote for it. Two conditions are part of the rule because a record that discusses everything would otherwise keep everything, which would have killed [#303](https://github.com/Grimblaz-and-Friends/tradecraft/issues/303) on its first grep: **use, not mention**, and **the text as it now reads**. **3B**: which arms the void gate reaches follows the shipped plant-placement rule -- both in a confirm, the intact arm alone in a screen; [D-320] already performed this reading and said the ablated-arm control readings are what a both-arms null rests on, so the shipped rule was narrower than its only precedent. **The ceiling is narrowed for the measurement and left standing for the licence**: an `unresolved` run fails the deletion rule by construction and no further arm rescues it, so this change licenses no deletion **off an `unresolved` run or its further arm** — narrowed from a broader headline by the review, which found a fresh clean re-run still licensed off a null and a shipped flat ceiling struck, both now disclosed in the entry and the ceiling's return put to the owner; what a further arm removing **a candidate** reconstruction source buys is a measurement of whether that candidate was carrying the substance, never a licence. Two retractions from earlier drafts, both caught by cold seats: D-320's *third arm* sentence was cited as authority for the resolver when it closes a different paragraph about the charter as a second route, and the resolver's first outcome was written licensing a deletion the unamended rule forbids. #337's *say which arm a keep came from* is struck from the reference and **re-landed on the spike-report rule**, a first draft's claim that the disposition name carried the duty alone having been falsified by a seat -- a name discloses nothing unless a rule makes someone write it down; not restored, and a real loss, is whether a `keep` came from one arm or both. Five artifact dispatches, four `would not` before `would`, three of them landing on the ceiling passage -- read under [D-304] as *false about the tree*, so D-320 was opened rather than reworded. **The experience session found the defect this change created**: `separated on the text under test` reads as *behavioural* separation, which would flip the same run back to a keep, and the measure being the warrant rather than the route was stated in neither shipped file; disambiguated in the cell. Untouched: the deletion requirement, the three-seats width, the screen rule, the void-run sentence, the citation test. Left recorded: findings 143 and 147. [#342](https://github.com/Grimblaz-and-Friends/tradecraft/issues/342), which generalises the instrument to a substitution, follows rather than leads |
| [D-352](D-352-2026-09-03-the-instrument-covers-a-substitution.md) | **The cold-seat A/B names a substitution as a third run shape, and the plant is read seat by seat on every run.** Every rule of the instrument was written for an ablation; the shape `spikes`'s own description names first -- a candidate wording for a rule -- is a substitution, and [D-337]'s second experience session ran one, translated the keep rule, and said the translation was its own. `substitutes` joins `screen` and `confirm` in the bullet that already hangs plant placement off run shape, its plant riding unchanged in both arms and its arms differing in which wording the sentence carries; the disposition (adopt, or leave the current wording standing, the default being the wording already there), the citation test (substance one wording carries and the other does not) and the null's bound land behind the pointer, on [D-337]'s own cut. **A named shape rather than a generalisation** of *the text under test*, which would have made the plant rule, the void gate, the keep rule and the citation test vaguer at once to cover one case. **The one fork was the owner's: option A, the seat-by-seat plant reading admitted generally** -- rejected, B scoping it to a substitution (the confound is not substitution-specific and scoping would read as if an ablation were immune) and C recording without admitting (the one substitution run this repository has would have been scored wrong without it). The shipped sentence that argued for B was struck in review, so the entry is the record of the scope. **Four sustained highs, one gap from four angles: the change generalised what a scorer does after a run and left what a designer does before one written for a deletion** -- the record search bound a substitution and its remedy cancelled it, the near-miss property had two referents, a candidate that drops a clause could be deleted under a paragraph disclaiming a deletion licence, and the confound's own sentence admitted an unexecutable reading. **Acceptance criterion 1 ruled not met as written**: its falsifier is unscoped and this change's own consumer reported deriving a rule by analogy on the one paragraph the change did not touch. **Criterion 6 holds**, on the general defence the change failed to make -- no disposition anywhere keys on a citation count, so a clause firing only on a count difference can move no ablation licence and no ablation disposition. **Two figures retracted**: three recorded rows named the reference path where seven do, plus an unmentioned index site, nobody having run the count; and recorded finding 147 was credited with reserving the generosity calibration to the owner, which only 163 does -- so a new calibration ships with half the support claimed for leaving it uncalibrated. The session note's *reported no translation* is corrected against its own later paragraph, and its truncation reason against `grep -c "342" docs/recorded-findings.jsonl` returning 0. Rejected: renaming the file (seven append-only rows and four entry sites, and no guard resolves paths in the jsonl -- routed to the board with the one-directional pointer guard); a width or adoption threshold (a number with no evidence, to the board, carrying an external reviewer's argument that candidate-exclusive substance makes a clarity rewrite unmeasurable); a fourth shape for the further arm (recorded, and 164's first non-fix ground has eroded); a budget for `spikes`. **Cost**: body 14,593 -> 16,337, reference 3,916 -> 5,294, cell body 13,875 -> 15,607, still second of thirteen with no budget; **no always-on row moved** at either end |
| [D-353](D-353-2026-09-03-the-routing-condition-asks-what-routes-the-situation-anyway.md) | **The routing criterion's condition asks what routes the situation anyway, because the confound is independence rather than direction.** **Supersedes the wording [D-338](D-338-2026-09-03-the-routing-criterion-is-keyed-to-the-second-route.md) landed and that entry's own row still states** — D-338 created the bullet, [D-345] widened everything around the bolded condition to cover text that keeps a reader away, and the condition itself was left comparing a competing route against *where the text sends the reader*: meaningless for a bare exclusion, which names no destination, and destination-matched where the masking is not. `to the same place` becomes `anyway`. **The confound is independence, not direction**: an ablation separates unless something else decides the outcome *without the text under test*, and a competitor resolving the situation either way leaves both arms alike. **The verb is what the review turned on.** This change first shipped `settles that same situation on its own` at −5 characters, passing every acceptance criterion; a five-seat panel, a defense and a judge each verified on the tree that `settles` is telic and so empties the second limb — *what settles it on its own* yet *settles it too weakly to mask the difference* is an empty set — retiring the clause D-338 bought from an external pass on [D-210]; that `on its own` teaches **sufficiency**, its sense in every shipped use, while the intended gloss shipped only in this repo-only entry; and that keying the condition to route 1's verb created a route-2 asymmetry the base did not have, which `AGENTS.md`'s *"the names route on their own"* — doctrine rather than shipped material — closes in the practice's own terms. **D-338's own frozen row states the rule twice, and its bolded headline had already compressed it correctly** — *"nothing else routes the situation, or routes it too weakly"* — which is the shape this change supplies `anyway` to. Six candidates were measured on `python tools/lint.py`; the acceptance criteria passed five of them, which is recorded as a finding against this change's own criteria. **First change to shrink this bullet**, by 11 characters. Route 3 stays outside the condition and no wording of it repairs or breaks route 3; the positive-polarity widening is recorded rather than smuggled; density stays at [#328](https://github.com/Grimblaz-and-Friends/tradecraft/issues/328), where a third sighting is now attached. |
| [D-364](D-364-2026-09-04-convergence-runs-on-real-text-and-stops-at-two.md) | **Convergence runs on real text and stops at two: for a change whose deliverable is the truth of some text the draft precedes the artifact, the cold check's bar belongs to the instrument, and the third question is the owner's.** Three gaps in one procedure, all found in one four-sentence correction that ran five cold rounds and shipped a false sentence anyway. **The class is recognised by what *done* is** — whether the change succeeds or fails on a claim being true of what it describes — and three cheaper tests were rejected: *the fork is wording* is circular, *it corrects an existing sentence* under-reaches a new description, and *the issue says so* puts the routing where the session is not looking. A criterion on such a claim names the **run** that settles it. **The bar is `plausible`**, the word the criteria author was already held to and the checker was not, sited in `engagement` rather than the review's dispatch contract because most cold checks are dispatched outside a review, and carried as dispatch content because the seat never reads the file; a prohibition on the dispatcher setting any bar was rejected as leaving the seat with none. **The cap meets this file's own objection to counts** — that a count licenses shipping unsettled at round N — by routing to the owner rather than to a licence: the artifact stays unsettled, only `would not` counts, and the owner's ruling settles it under a disclosing label. A cold seat found the cap colliding with [D-304]'s repeated-point rule and with the amended-brief re-dispatch, and the cap now states its precedence over the repeated-point rule and yields to the amended-brief rule, an external reviewer having found that withholding that dispatch lets implementation begin from criteria judged against a superseded brief. **The change caught itself with its own rule**: its first artifact claimed nothing in the tree states an artifact-first ordering, and a sweep found the section opener does, so that sentence is qualified too. Two seats, `would` then `would`, the second on a revision the first's non-blocking observations forced. Cost stated before the affirmation: this cap would have interrupted PR #353 at its second verdict. **The experience session measured both arms** — one consumer took the drafted-text-first order unprompted on a truth-of-text job, one ruled itself out of the class on a design job — leaving only the mixed shape unmeasured. **A five-seat panel then found the entry misdescribing its own diff** (it claimed the `landing` flow chain was qualified when only the paragraph under it was), **a false empirical universal in the shipped cap paragraph** (this tree's log records two loops that ended without the owner), and **the cost disclosed to the owner understated in frequency** — two of the last four convergences interrupted, not one. All three repaired in the fix batch. |
| [D-365](D-365-2026-09-04-review-cost-and-target.md) | **What a review cost is a fact about the run, the surface each high hit rides on the high, and a positional exemption belongs to one record.** The lane heuristic leans expensive by design and its own rule promises an audit -- *"an unrecorded shape choice can never be audited later"* -- that nothing on the record could answer **quantitatively**, the reason each lane was chosen being one hop away in the report every row already links, because no row carries what a review cost. **`cost` is `{dispatches, subagent_tokens}`, scoped to the review** rather than the change, both read off what each dispatch returned; the premise that they are capturable without estimating was spiked before it was asserted, one throwaway dispatch returning `subagent_tokens: 42872`. `subagent_tokens` may abstain as `null` where a runtime does not report it; `dispatches` may not, a runtime that made dispatches being able to count them. Wall-clock rejected as measuring queueing, seat count as already in `staffing`. **[D-194]'s *"The row carries no arithmetic"* is narrowed rather than excepted** -- to arithmetic **over the findings**, which is what it always meant and the ground the guard's own comment already put `staffing` on. **`target` rides on each high** (`highs` entries become `{high, target}`) rather than the row gaining counts per bucket, because `facing` is that design's failure on this record: 8 rows of 74, none written since 2026-08-26, its own comment recording that the three reports which stated a split each counted a different population. **Three values, not the owner's stated binary** -- `record`, `shipped`, `repo` -- affirmed by him 2026-09-04 on the concrete case that this change's own deliverable is repo-only, so a binary files every high its review sustains against the guard as paperwork, in the first row the trial scores. **Ordered, first match governing**, `record` first because a decision entry and an index row sit inside the repo-only zone, `repo` residual so every site has a lawful label; both properties were adverse-verdict repairs. **`target` and consequence shape are different axes** and the guard rejects `artifact`/`apparatus` as `target` values. **Folds in [#268](https://github.com/Grimblaz-and-Friends/tradecraft/issues/268)'s first-row defect**: all four positional boundaries were facts about one file and were applied to any file of that name, so the first row of a tree whose index had not started was demanded in the shape the shipped material abolished -- two experience-session consumers hit it independently and both refused to clear the red rather than invent counts into an uncorrectable record. The file is now identified by the **sha256 of its first non-blank row's bytes, trailing whitespace stripped**; anything else gets zero for every boundary. Row 0's `artifact` was rejected as the sentinel: `pr-74` is a value no other repository is prevented from writing, so it would not separate this record from anyone else's, and `artifact` values are not unique in general (`pr-156` twice here, though `pr-74` itself is unique). Closes the defect's other direction too, where such a tree's rows 0-38 would have admitted the retired shape silently. A truncated copy reads as foreign, deliberately. **The test cost was predicted and paid**: prepending the real origin row was tried and rejected for shifting every synthetic position by one; `_write_index` instead declares its file to be this record, with an autouse fixture restoring the constant. **One admission**, the record's first row: the lane-heuristic clause puts the `adversarial-review` body 121 characters over, has no cheaper home (a reference opens after the lane is picked; the `records` cell loads at the close), and was cut from 165 to 138 characters before admitting. The constant does not move. **Three cold rounds**, each landing only new points; round 1 found the shipped sentence forbade the whole change and that the draft named no home for the semantics at all, round 2 that the not-a-target sentence sat where a review is closed rather than where a lane is chosen, round 3 returned `would`. The two-round cap was put to the owner, who directed the third. **Left open rather than closed**: a cited site outside this repository has no lawful `target`, `repo` being the residual of this tree |
| [D-366](D-366-2026-09-04-a-substitution-measures-and-licenses-nothing.md) | **A substitution is scored on two measures and licenses adoption by neither; three seats an arm is a floor; a plant may not be a second route to the behaviour under test.** Completes the shape [D-352](D-352-2026-09-03-the-instrument-covers-a-substitution.md) shipped without a threshold or a width. Presence-of-a-citation is calibrated by the deletion's *keeping is cheap*, which adoption reverses — so the exclusivity test hid the threshold question rather than avoiding it. Arm-attributed reliance on shared substance is added as a second measure, which is what makes a clarity rewrite measurable, and the adoption call is the proposer's except where the two wordings would have a reader do different things, which is the owner's. The plant's subject constraint binds every shape, **superseding this change's own reported call that it never reaches a screen**; its scoring-side reading is stated of a substitution alone, because on a confirm a redundant plant satisfies the deletion licence on live text and the remedy for that is out of the affirmed brief. |
| [D-371](D-371-2026-09-04-the-review-runs-once.md) | **A review runs once; the change's own record is held to honesty rather than precision; a finding enters the fix batch only where its consequence is one a consumer would live.** **Supersedes [D-96](D-96-2026-08-20-post-fix-terminus.md)** — nothing there was wrong about the cycle it governed, but the cycle itself goes, and with it the terminus, the budget and the escalation at budget. D-96's own Context is the argument: a post-fix cycle's third generator, the review's own conduct, *"does not deplete at all while the artifact's author runs the review."* The panel, defence and judge stand; the fix batch owes an executable floor; what reads the result after it is the experience session on the fixed tree and then friction under merged material. Direction and the three riders are the owner's, ruled on #360. Trial, scored on #360 at three weeks or fifteen reviews. |
| [D-372](D-372-2026-09-04-a-withheld-check-is-not-an-unobtainable-one.md) | **The truncation route says which checks it covers: a check somebody withheld is not a check nobody in reach can supply, and the excluded case takes the `draft` label that already exists.** [#367](https://github.com/Grimblaz-and-Friends/tradecraft/issues/367) recorded two cold consumers from [D-364]'s experience session answering the unbounded condition in opposite directions on the same facts -- one labelling its artifact *settled by a recorded truncation* where the check was withheld by its extent, one refusing on the ground that withheld and unobtainable are different things. **The owner ruled the second reading.** The route now covers only a check no party within the session's reach can supply; where a bound someone set removed it the artifact stays a draft and the withholding goes back to whoever set the bound. **No fifth label** -- `draft` already means an artifact no seat settled and already forbids building from it -- and **no definition of *unobtainable***, the change excluding one case rather than listing what qualifies. Rejected: the opposite reading, on the ground that this is the only one of four routes to *settled* needing no other party's act, so a dispatcher could otherwise settle every artifact by writing one clause; and extending the boundary to the cap, which needs the owner's act and which [D-300] already answers. **Three grounds carried the affirmation and the artifact wrongly said one of them did** -- the review found a real defect in that one, a one-shot session cannot ask mid-run, and the clause carrying it was cut from the shipped text; the other two are each sufficient. **Two figures retracted**: the commit message says #364 ran two cold consumers where it ran three, and the rulings comment's review-cost total is out by 40. **Row 199 answered** -- the convergence seat treated contrived implementations as observations and blocked on none, the row's first measurement. **Left standing, recorded**: both limbs of the condition satisfiable by the session's own reasoning; *reach* defined nowhere; the truncation's form behind a conditioned pointer; the installed plugin copy disagreeing with the tree. **The first experience session's truncation arm did not stage** and its *warrant both times* is corrected to one |
| [D-376](D-376-2026-09-04-the-charter-holds-what-loads-first.md) | **The charter holds what must hold before any cell loads, and a compressed copy of a cell's rule is worse than a pointer.** [#303](https://github.com/Grimblaz-and-Friends/tradecraft/issues/303) was filed on the reading that the charter is *silent* in four places and every comment argued for adding prose; the owner corrected the premise -- the success criterion is about an adopting repository with no doctrine of its own, and *"the charter should never be able to function without skills."* [D-184] already defines the adopter set as the charter **and the roster's descriptions**, so the population all four gaps argue from is empty. **The failure mode is compression, not size**: a compressed copy answers a session's own check that it knows the rule and so suppresses the read, while what compression drops is the limb that decides the case -- the argued ask's plural *"the live options"* omitting the one-live-option rule and pointing a reader at the defect it forbids, the fork test omitting *"Both halves are required"*. **Nothing is admitted as a new rule**: `routing.md:6` already made the always-on surface the last resort and defined residency as *what must hold before any context loads*, and `cell-structure.md:7` privileges the charter because a citation to it *"cannot drift as a second copy can"*. **The incident is the drafting session itself**, which wrote three briefs against the compressed form spec and broke it eight times across them -- the two rules the charter carried broken once between them, the four carried only in the cell broken seven times. **The test keeps as well as it cuts**: the positioning paragraph, Authority, the mechanical exemption whose only other carrier is repo-only, and the whole fix-bar paragraph stay, every sentence of the last firing outside a review where no cell is loaded. `exactly` leaves the two-moments sentence, which #364's cap had made false. **Rejected: record-search licensing**, run and abandoned -- it cleared 135 of 1,633 candidate characters and **cannot find text that was obeyed**, compliance leaving no citation, and the one passage it cleared was the form spec whose class was producing that session's defects as it read; **narrowing the deletion rule**, recommended then withdrawn as unnecessary once a route was seen not to be an ablation, its stated harm being a session routed where the text stopped it and a route sending them to *more* of the rule; and **two relocations asserted and falsified by the same error** -- checking that text exists somewhere rather than that the reader receives it -- to `README.md`, which lands in a consumer's cache inert; and to `adversarial-review` for the whole fix-bar paragraph, refused on a grep this change's own review then falsified — four of its five sentences are duplicated at `skills/adversarial-review/references/arbitration.md`, and which of them move goes to the board. **Both-polarities moves whole to `substrate`** with its negative control and #142, the incident being that requirement's reason; the figure rule stays with `authoring`; `cell-structure.md` stops making *routes up to the charter* coequal with single-owner. **Banking, not spending**: the move put `adversarial-review` under its ceiling with 121 characters still admitted, and the lint raised that itself as *room nobody argued for*. Charter body 5,851 -> 5,415. The always-on figures are in the pull request body, derived there with the command and the tree, because they moved twice under this change and a number here would carry neither. **Round 1 `would not`** on the change producing, in its own artifact, the duplication it exists to remove -- the fix bar specified as both kept and relocated, into a cell at its ceiling -- caught by the instrument; round 2 `would`. **Left open**: whether a route is lawful only where its destination reaches the reader the source served, which is the rule neither falsified route broke by name. |
| [D-376](D-376-2026-09-04-the-charter-holds-what-loads-first.md) | **The charter holds what must hold before any cell loads, and a compressed copy of a cell's rule is worse than a pointer.** [#303](https://github.com/Grimblaz-and-Friends/tradecraft/issues/303) was filed on the reading that the charter is *silent* in four places and every comment argued for adding prose; the owner corrected the premise -- the success criterion is about an adopting repository with no doctrine of its own, and *"the charter should never be able to function without skills."* [D-184] already defines the adopter set as the charter **and the roster's descriptions**, so the population all four gaps argue from is empty. **The failure mode is compression, not size**: a compressed copy answers a session's own check that it knows the rule and so suppresses the read, while what compression drops is the limb that decides the case -- the argued ask's plural *"the live options"* omitting the one-live-option rule and pointing a reader at the defect it forbids, the fork test omitting *"Both halves are required"*. **Nothing is admitted as a new rule**: `routing.md:6` already made the always-on surface the last resort and defined residency as *what must hold before any context loads*, and `cell-structure.md:7` privileges the charter because a citation to it *"cannot drift as a second copy can"*. **The incident is the drafting session itself**, which wrote three briefs against the compressed form spec and broke it eight times across them -- the two rules the charter carried broken once between them, the four carried only in the cell broken seven times. **The test keeps as well as it cuts**: the positioning paragraph, Authority, the mechanical exemption whose only other carrier is repo-only, and the whole fix-bar paragraph stay, every sentence of the last firing outside a review where no cell is loaded [corrected — see that entry's Erratum: four of the five had another home]. `exactly` leaves the two-moments sentence, which #364's cap had made false. **Rejected: record-search licensing**, run and abandoned -- it cleared 135 of 1,633 candidate characters and **cannot find text that was obeyed**, compliance leaving no citation, and the one passage it cleared was the form spec whose class was producing that session's defects as it read; **narrowing the deletion rule**, recommended then withdrawn as unnecessary once a route was seen not to be an ablation, its stated harm being a session routed where the text stopped it and a route sending them to *more* of the rule; and **two relocations asserted and falsified by the same error** -- checking that text exists somewhere rather than that the reader receives it -- to `README.md`, which lands in a consumer's cache inert; and to `adversarial-review` for the whole fix-bar paragraph, refused on a grep this change's own review then falsified — four of its five sentences are duplicated at `skills/adversarial-review/references/arbitration.md`, and which of them move goes to the board. **Both-polarities moves whole to `substrate`** with its negative control and #142, the incident being that requirement's reason; the figure rule stays with `authoring`; `cell-structure.md` stops making *routes up to the charter* coequal with single-owner. **Banking, not spending**: the move put `adversarial-review` under its ceiling with 121 characters still admitted, and the lint raised that itself as *room nobody argued for*. Charter body 5,851 -> 5,415. The always-on figures are in the pull request body, derived there with the command and the tree, because they moved twice under this change and a number here would carry neither. **Round 1 `would not`** on the change producing, in its own artifact, the duplication it exists to remove -- the fix bar specified as both kept and relocated, into a cell at its ceiling -- caught by the instrument; round 2 `would`. **Left open**: whether a route is lawful only where its destination reaches the reader the source served, which is the rule neither falsified route broke by name. |
| [D-381](D-381-2026-09-04-the-posted-brief-carries-the-ruling-and-the-artifact-waits.md) | **The brief that locks and gets posted is the agreement rather than the ask, and a session that cannot reach the owner may not draft the artifact.** [#373](https://github.com/Grimblaz-and-Friends/tradecraft/issues/373) — the brief is put as an ask and posted as the record of what was agreed, and no sentence joined the two, so an exclusion the owner set in conversation and never wrote down is silently demoted from his to the session's. [#374](https://github.com/Grimblaz-and-Friends/tradecraft/issues/374) — five of six cold consumers read past the wait rule's enumeration, which never named the artifact, three saying so as they did it. **Both forks were the owner's and he ruled both**, then required the brief be re-put carrying his rulings before anything continued — which is #373's own fix applied to itself before it was built. [D-300] had rejected all three licences for an unaffirmed brief, but its own next sentence put the unattended band's near end at *implementation start* while the map puts it at the affirmation, and the artifact falls in the gap the five walked through; that gap is now closed. **The owner's exclusion, and therefore his**: making the artifact available to a waiting session under any label, a draft included. **The verbatim rule shipped wrong and the external pass caught it** — requiring an exclusion *in their own words* is impossible where the answer is contextual, and this change's own convergence is the demonstration, the ruling being *"A for both"* and the affirmation *"affirm"*; it now reads *their own words where those carry it*, a restatement the owner affirms where they do not. **Rejected**: the artifact as a labelled draft, which is the second licence refused on #286; the ask-plus-answers posted form; a clause saying where the re-putting happens, against deleting the sentence that created the need, deletion being cheaper *and* more effective; conditioning the exclusion bullet, whose remedy writes the duplication a separate finding objects to; folding the withholding into the sentence above it, priced at a net reduction on arithmetic omitting the rewrite it forces; and amending the cell's Success clause, which is a comprehension criterion rather than a contents enumeration. **The panel's most serious finding was disproved by the defense** — its two sole highs assume disjoint readers, one reaching a sentence the other stops before, and that sentence's anaphor has no antecedent but the one it stops before; what survived was a false clause asserting a dispatcher set the job's terms, struck rather than qualified. **Three figure errors, all in supporting clauses beside a load-bearing figure that was checked**: the author's size-ceiling claim, the author's base body figure, then two seats reporting that inherited figure as their own measurement while the judge's first pass failed the same way through an unstated cp1252 decode. The standing correction — a seat naming the command *and the convention* — is in the report rather than a retired lens. **The assignment omitted two recorded findings about this cell's net growth** on a change whose largest finding was net growth; the external pass caught that too. **Body 24,745 → 25,843.** Left open: [#385](https://github.com/Grimblaz-and-Friends/tradecraft/issues/385) the charter and the cell disagreeing on when the affirmation happens, [#386](https://github.com/Grimblaz-and-Friends/tradecraft/issues/386) the doctrine callout not watching shipped cell bodies, [#387](https://github.com/Grimblaz-and-Friends/tradecraft/issues/387) the brief's form rule not describing the posted record, and four recorded findings. |
| [D-388](D-388-2026-09-04-a-move-must-reach-the-reader.md) | **A standard moves only to a cell that reaches the reader it is taken from, and a search for the source's own words is not that check.** [D-376] left the fix-bar paragraph unmoved, on a settled artifact's grep for the charter's own phrasings returning nothing in `adversarial-review` — a question about a rule answered by a search for text. `arbitration.md:15` and `:25` carry the four sentences, which that entry's own Rejected section concedes while its Decision section lists the paragraph among what *"has no other home"*: **corrected, not superseded** — the falsifier is that entry's own, so it takes a marker and an Erratum here, and the clause that is false is the lead-in rather than the *fires outside a review* clause, which is true of all five. **Four leave, the fifth stays reworded**, its *"no such stage"* losing its referent when the sentence before it moves; the owner's requirement is *"very little in the charter, and only those things that can't go somewhere else"*, against which the session's own first recommendation — keep three, scored on the reader test imported from [#378](https://github.com/Grimblaz-and-Friends/tradecraft/issues/378) rather than on the stated requirement — was wrong, and the owner caught it. **The check is a read, not a search**, and an obligation on the mover rather than a measured claim, because [#272](https://github.com/Grimblaz-and-Friends/tradecraft/issues/272) holds that *a session reaches this cell* is unfalsifiable. **Where no route holds the destination's trigger widens and the rule does not go back** — the owner's `substrate` ruling inside PR #376 generalised, that incident being what admits the rule at all. **Applied to itself, and it failed there first**: the rule's first draft was satisfiable without reading past a route's first hop, and the change had widened two of three excluding surfaces while its own rule required none of them — the review caught both, and the batch repaired the rule and the third surface, the cell's `Audience` line. **No guard**, either polarity: lint has no notion of a standard, so it could only test a proxy — the instrument class that caused this. **Rejected**: the two-readers ground; lifting the trichotomy into the cell body, which exceeds that body's headroom; and amending the cell's description, the owner's exclusion — unargued, because a description is always loaded and the charter already carries the pointer, so the reader test's teeth are on cell-to-cell moves. **Left open**: whether `record` is available outside a review at all ([D-269]'s recorded conflict, carried unchanged); whether any mechanism could hold the weaker one-home property; and what a description is for, filed as [#384](https://github.com/Grimblaz-and-Friends/tradecraft/issues/384). |
| [D-394](D-394-2026-09-05-the-callout-watches-descriptions-not-bodies.md) | **The doctrine callout watches what loads before a cell fires, and the Release bullet is corrected rather than the tool widened.** Three gaps in the one mechanism performing the owner's merge-time read, since `CODEOWNERS` structurally cannot [D-81]. [#277](https://github.com/Grimblaz-and-Friends/tradecraft/issues/277) — a cell's name and description load in every session and an edit to them fired nothing. [D-107] and [D-230] each left `adversarial-review`'s description asserting a retired test, falsified twice and merged twice with lint green — but by *body* edits, and #230 was flagged anyway for touching `AGENTS.md` and the charter; **PR #269 is the one unflagged description edit** behind the filing, and it repaired that very string while itself editing an always-on surface. [#293](https://github.com/Grimblaz-and-Friends/tradecraft/issues/293) — the diff read names only a rename's destination, so a file moved *out* of the doctrine matched neither set; the exact half had `lint.py` as a backstop and the prefix half had none. [#386](https://github.com/Grimblaz-and-Friends/tradecraft/issues/386) — PR #381 merged two binding rules into the largest cell body and bumped the plugin version with no flag, while the bullet asserted a wider reach as the guarantee. **The owner ruled both forks**: frontmatter in on 2026-08-31, bodies out on 2026-09-05, the second on measurement rather than argument — over the 40 most recently merged pull requests the flag fires on 15 today, 17 with frontmatter, and **35 with bodies**, and `docs/values.md` rank 3 decides it against rank 1 for the claim. So the guarantee comes down to the mechanism rather than the mechanism up to the guarantee. **No path match can express *the description changed***, so the new arm compares the block `roster.frontmatter` returns at two revisions; reusing it is what keeps a rival definition of a description from existing. **The charter is the one shipped cell whose body still fires** — watched entire because `AGENTS.md` imports it — and the bullet's first draft denied exactly that, saying *a shipped cell's body is not flagged* two clauses after naming it; a test now fails if the carve-out is dropped. **`--head` exists because a working-tree head made the arm's own falsifier unrunnable**, one checkout comparing every base against the same tree; CI passes none and is unaffected. **Line endings are normalised before comparing**, or this tree's LF-in-git against CRLF-on-disk flags every pull request — the false callout the module exists to prevent, arriving through the guard added to prevent a miss. **Verified by replay**: **all 40** of the last 40 merged pull requests, each judged against its own base and head, **zero disagreements** between the new bullet's rule applied by hand and the tool's union. The four whose objects the clone lacked are fetched with `git fetch origin refs/pull/N/head`, which any re-run needs and which the round-one panel found; an earlier draft recorded them as unreplayable and drew its headline from the other 36. **Two pull requests are newly flagged, not one — #269 and #320**, which is what 15→17 always meant; #320 is a description-only edit to `skills/engagement/SKILL.md` that merged with no label, a **fourth** instance of the incident class inside the measured window. **And the incident count behind #277 is one, not three**: D-107 and D-230 left the description stale by *body* edits and #230 was flagged anyway. Both errors were caught by the round-one review and repaired before this entry froze. **Rejected**: flagging bodies; a quieter surface for them, which spends the notification while appearing not to; the version bump as trigger, which flags the same 35; narrowing repo-only cells to their frontmatter, excluded by the affirmed brief as a shrink of the owner's read needing its own case; and calling the reach *the always-on surface*, which `figures.py` prices wider than anything here fires on. **Two consecutive `would not` verdicts**, settled by an owner ruling at the cap and by no seat — round 1 caught the charter contradiction, round 2 the unrunnable falsifier, the second buying a design change, so the cap arrived with the rounds still paying. |
