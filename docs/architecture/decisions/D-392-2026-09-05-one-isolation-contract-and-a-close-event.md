# D-392: One isolation contract for every dispatched recipient, a close event that is a condition on the tree, and the report's field list reaching the stage that must elicit it

**Status:** Accepted 2026-09-05 (PR #392)

## Context

Three defects in `skills/adversarial-review`, all found by use of the instruments [PR #382](https://github.com/Grimblaz-and-Friends/tradecraft/pull/382) landed, and all in the file that dispatches most of this practice's sessions.

**The isolation clause tested when isolation is needed rather than what it must be.** `dispatch.md` opened it *"Seats that mutate the tree get their own worktree, cut detached from a base the dispatch names, and their write boundary there."* Every obligation in the paragraph hung off that condition, which is false for most dispatched recipients — non-mutating seats, the defense and the judge are its majority population. PR #382 rewrote the sentence and left the gate byte-identical. `docs/recorded-findings.jsonl` carries the entry, promoted by *"a non-mutating dispatched seat leaving a branch or worktree"*, and every non-mutating seat on that review met it; the defense and the judge each reported it from inside.

**Nothing said when such a tree may be removed.** [PR #307](https://github.com/Grimblaz-and-Friends/tradecraft/pull/307) added a clause keying removal to resumability, which in this harness never goes false, so it licensed no cleanup at all and was withdrawn. The second recorded entry measured 61 worktrees at the time; this change measured 85 at drafting, 88 an hour later, with 33 `worktree-agent-*` branches outliving their trees. The motivating incident is that review's own dispatching session removing a cold seat's tree while the seat was still verifying.

**The stage that must ask for the report's fields was told not to read the list.** `the-record.md` enumerates what the final report carries; its load condition said *"Nothing before the terminal ruling needs it."* PR #382's experience-session consumer, given a dispatcher's job, read the file anyway, back-derived the seat output format, and reported plainly that *"that derivation is mine, not the skill's, and a dispatcher who read only `roster.md` and `dispatch.md` would not produce it."* The same consumer read `dispatch.md:5`'s *"Its output format and working location where it differs are the only other per-recipient content"* three times, reported both readings compliant, resolved it the way that produced byte-identical Output sections outside the byte-identical block, and called that odd.

## Decision

**Every dispatched recipient works in its own tree, cut detached at the revision the dispatch names and carrying no branch — and a recipient dispatched to write owes its write boundary there beyond that.** The gate now states what isolation *is*, which is the framing the recorded entry itself asked for. Reading and writing seats are deliberately **not** collapsed: the property is uniform and the write boundary is the one clause the writing minority owes on top. The two creation routes were already distinguished at `dispatch.md:13` — the dispatcher positions a root it creates, a recipient launched into a tool-cut root detaches itself — so the widened gate states the property and those sentences keep saying how each route reaches it.

**A dispatched recipient's tree becomes removable when the review that dispatched it closes — its final report posted — and not before.** Three properties were required of the event and are why this one was chosen. It **goes false and then true**, which is precisely what PR #307's version failed. It sits **on the safe side of the incident**: `arbitration.md:5` re-dispatches once a seat that returned unusable output, so any earlier event is true while the tree may still be needed. And **removal covers the branch the tree's creation made**, since a branch nothing removes is what outlives its tree.

**It is written as a condition on the tree rather than a duty owed by a live session, and that wording is the whole of whether the rule reaches anything.** Trees outlive the sessions that cut them: of the 85 measured at drafting, essentially none had a live dispatcher. A rule worded *the dispatching session removes its own trees* makes none of the standing population removable while reading as though it had addressed them. **The cold seat caught this**, finding the artifact's own Call 2 and Call 3 stating the clause in the two incompatible forms; the shipped sentence takes the condition form.

**The report's field list reaches the dispatcher by widening both excluding hops, and nothing is copied.** [D-388](D-388-2026-09-04-a-move-must-reach-the-reader.md) landed the ladder hours before this change began — *"what widens is the cheapest surface that closes it — a load condition, then a pointer, then a description; returning the rule to its source restores the second copy"* — checked by reading every hop rather than by finding the source's words in the destination. Read: `SKILL.md`'s pointer routed only the closer; `the-record.md`'s load condition disclaimed the dispatcher outright; `dispatch.md` and `roster.md` cite the file without instructing a load. **Both excluding hops widen**, and `dispatch.md` gains only the shared-block mention that makes output requirements assignment content. **PR #382's precedent — one report field written into `dispatch.md` — is the ladder's last rung**, and is treated here as the exception it was rather than the pattern to scale.

**Four report fields have no producer the dispatcher is told about, not one.** #389 filed the survey as unrun; run here, it returns a seat's raw count, the revision and tree each recipient reports, findings attributed well enough to name each high's finders, and what the review cost — the last being *"read off what each dispatch returned"* with `grep -rno "cost[a-z]*\|token[a-z]*" skills/adversarial-review/` returning 11 occurrences over 10 line references, none in `dispatch.md`. One clause naming the class closes all four, which is the argument for the load-condition rung over a per-field sentence.

**What every recipient's output must carry is assignment content; only the role's own output shape is per-recipient.** That is `dispatch.md:5`'s ambiguity closed in the second direction. The report reads the same things off every recipient, so writing them per recipient is how a dispatcher produces a byte-identical section outside the block whose definition is that it is byte-identical.

**Each dispatched recipient states whether its tree was its own or shared, and the report carries it.** This is the owner's ruled option, and it is the answer to a question PR #382's merge gate put and its merge did not rule — gate item 2's option (b), *"a report line, not a check, so inside your exclusion"*, recommended by that review's judge to land with #389. It answers isolation the way PR #382 answered the revision: with visibility rather than prevention.

## Rejected

- **Prose only.** Cheapest, and it lands almost entirely in the one file that owns these rules. Rejected by the owner: this is the same material that was obeyed and still failed — [#311](https://github.com/Grimblaz-and-Friends/tradecraft/issues/311)'s five probes each complied with the mutating-seat sentence as written — and nothing would observe whether the widened gate held any better than the narrow one.
- **Prose, the report line, and a built thing.** A script listing dispatched trees and saying which are removable is the only option that reduces the residue rather than describing it. Rejected: it needs the owner to rule that his standing exclusion's trigger fired, and he ruled it had not. Against it independently — a tool deleting on a close event stated for the first time is the direction that already removed a live seat's tree, and **most of the residue is not reachable by a dispatcher's rule at all**: 68 of the 85 trees sit under `.claude/worktrees/`, cut by the dispatching tool with no dispatcher choosing them.
- **A seat-output section written into `dispatch.md`**, following PR #382's precedent for one field. Rejected as D-388's last rung and the second copy that rule names.
- **An isolation field on `docs/reviews.jsonl`'s index row.** The owner ruled *one report clause*. Whether the index's *"facts about the run"* licence also admits isolation is a separate argument this change does not make, and `docs/seat-record.jsonl` — which carried an `isolated` boolean until 2026-08-19 — is frozen archive.
- **Shedding the mutating-seat sentence's `git worktree add` explanation**, expected when the gate widened on the theory that what was special-cased became general. Withdrawn on reading it: that text explains the two positional arguments and what each does, not the gate, and it is the only place the flag's behaviour is stated. **This change sheds nothing, and the pull request body says so rather than claiming an offset it did not make.**

## Left open

**Whether the harness's own trees are reachable by anything stronger than the close event.** The clause keys on the dispatch rather than on who ran `git worktree add`, which reaches them; nothing here changes *how* the tool cuts one, so they still arrive branch-attached and are still detached by the recipient. Round 2's own worktree was branch-attached, which the seat reported.

**Whether finder attributability needs its own clause.** The new sentence names it among the four recipient-produced fields, but nothing obliges a seat to structure its findings so the merge can attribute them; today it works by convention.

**Criterion 6's population is a moving number.** The artifact's falsifier says *"the 85 trees now on this machine"*; round 2 watched it reach 88 under its own dispatch and noted that whoever runs the check should re-count rather than reuse the figure. Recorded rather than fixed, because editing a judged criterion after its verdict returns the artifact to draft.

## Evidence

The affirmed brief is on [#383](https://github.com/Grimblaz-and-Friends/tradecraft/issues/383#issuecomment-5549282205), its affirmation [the comment naming it](https://github.com/Grimblaz-and-Friends/tradecraft/issues/383#issuecomment-5549287556), and the settled artifact [here](https://github.com/Grimblaz-and-Friends/tradecraft/issues/383#issuecomment-5549394825).

**The cold check ran two rounds, both `would`, no adverse verdict — so none of the two-round cap was spent.** Round 1 judged sha256 `b90419d3…` / 19,349 bytes at `8ed30af`; round 2 sha256 `756c78f2…` / 21,517 bytes at the same commit. The second round was bought not by an adverse verdict but by a revision that **added a claim** — a fifth call, promoted from a note — which `skills/engagement/references/cold-seat.md` puts outside the no-fresh-reading carve-out as surely as a change to one. Both seats were fresh dispatches in their own worktrees and both confirmed the revision unprompted.

**Round 1 returned four wrong figures in the artifact's supporting evidence, and one of them was inherited rather than derived.** `docs/reviews.jsonl` given at 82 rows when `8ed30af` had made it 83; `dispatch.md`'s post-#382 size given as 5,721 characters when the tree returns 6,114 bytes / 6,086 characters — **the same wrong 5,721 that PR #382's own body carries**; a sentence said to *end* a paragraph it is second-to-last in; and a grep characterised as three incidental uses where it returns eleven occurrences. All four were in supporting clauses beside load-bearing figures that had been checked, which is the class this repository keeps meeting. Each was re-derived from source rather than adjusted.

**Round 2 verified every citation against the tree and found none false.** Two of its observations are answered in the shipped text — the close event's grammatical form, and two of the three supports the artifact gave it, since the terminal stage's docket reaches every seat's *report* rather than its tree and the executable floor re-derives against the tree the fixes produce. The shipped sentence therefore rests on the re-dispatch clause and the incident alone.

The weight figures with the trees that derive them, both guard results, and the four-hop route table are in this pull request's body.
