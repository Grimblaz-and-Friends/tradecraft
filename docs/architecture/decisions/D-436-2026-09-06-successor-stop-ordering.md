# D-436: A successor's systemic-cause belief stops the work, and the amendment route says only how the disagreement is settled

**Status:** Accepted 2026-09-06 (PR #436)

## Context

[#432](https://github.com/Grimblaz-and-Friends/tradecraft/issues/432), worked alone. The [affirmed brief](https://github.com/Grimblaz-and-Friends/tradecraft/issues/432#issuecomment-5561408243), its [affirmation record](https://github.com/Grimblaz-and-Friends/tradecraft/issues/432#issuecomment-5561409288) and the [settled artifact](https://github.com/Grimblaz-and-Friends/tradecraft/issues/432#issuecomment-5561487541) are on that issue.

**The seam this sits on is the owner's split**, affirmed 2026-09-06: the session that settles a brief in conversation and the session that builds from it are different sessions, and [D-431] wrote down what has to survive the crossing. This is the first defect found on the arriving side of that seam by using it.

**The incident.** A cold consumer, given an issue whose brief another session had affirmed and told nothing was under test, came to believe the brief was aimed at the wrong cause, found two rules reaching it, and reported the fork unprompted rather than being asked about it. In its own words, from [the experience session on PR #431](https://github.com/Grimblaz-and-Friends/tradecraft/pull/431#issuecomment-5556649099): *"A successor amends rather than replaces, and amending a brief means re-agreeing it with the owner, who is not reachable. So I recorded it in the artifact's boundary statement as flagged-not-acted-on and moved on."* And: *"Someone else might have treated this as test 1 — 'does answering it change what the change is for?' — and stopped the work."* PR #431's [terminal review stage](https://github.com/Grimblaz-and-Friends/tradecraft/pull/431#issuecomment-5556974034) sustained it and dispositioned it a decision rather than a fix, which is what sent it to the board.

**What the tree held at the base.** At `07511c362979649c7711dd9a54d5de7c078d9fe6`, `skills/engagement/SKILL.md` carried both of these and nothing ordering them:

- `:119`, the first of the three tests a question inside the unattended stretch takes, first match governing — *"Does answering it change what the change is for?"* → *"The work **stops** — the one thing that stops it."*
- `:95`, in the handoff at the affirmation — *"A successor that believes the affirmed brief is wrong amends it rather than replacing it … That is what the amendment route above is for"*, pointing at `:66`, which re-agrees an amended brief with the owner and is silent on the work's state meanwhile.

`:95` is the passage a successor reaches, because the handoff section is what it is told to read on arrival.

## Decision

**1. The ordering is the owner's, ruled from three argued options.** The stop governs; the route that would have carried on comes second. Rejected: the amendment route governing, with the work proceeding and the disagreement carried to release; and the successor's case being neither of those and taking its own sentence — proceeding on the affirmed term, recording the belief as a disputed parentage, and stopping only where the affirmed term could not be built at all. **The session recommended the third and carried the strongest case against it**; he ruled the first, in one word — *"A"* — and affirmed the re-put brief carrying it. The case against the recommendation was that it adds a fourth rule to a stretch already carrying three tests plus an amendment route, and that the line it turns on is itself a judgment, so it may relocate the ambiguity rather than remove it. The ruling avoids that cost entirely.

**2. The ordering is stated at one site, `:95`, and test 1 is untouched.** The fork is reachable only from `:95`: a session that reaches test 1 reads *stops* and stops, so it was never in it. Giving test 1 a successor clause would put a second statement of the ordering in the file — **which is the shape that produced this defect**, two sites stating a successor's obligation differently. A fix that reproduces its own cause is not a fix. This was written as an acceptance criterion and checked on the diff, not left to care.

**3. The two passages are not two answers to one question, and the wording says so.** The amendment route answers *how* a disagreement about an affirmed brief is settled — by re-agreeing it with the owner, never by the successor settling it alone. Test 1 answers *what the work does* while that is unsettled. Read that way the ruling is not one rule overriding another but a statement of what each was already about, which is why the change is an ordering and not a new rule.

**4. The stop terminates in machinery the cell already carries, and no route was added.** The standing objection to this option, raised while it was argued, is that a stop has nowhere to end when the owner is unreachable — which is also the deferred question #432's filing called *"the one no route currently terminates"*. The file already ends it: the rule that a session which cannot reach the owner for an affirmation its work owes waits, the *waiting is not idling* clause with its closed list, and the hand-back form. The new text points at those rather than restating them. **A second seat observed that the closed list is worded for a session with no affirmed brief yet, while a successor here has one** — the pointer still yields a coherent instruction, and the mismatch is recorded rather than repaired, since repairing it is a change to the waiting rule and nobody asked for one.

**5. No site was added for an arriving session to run the why-chain, and that is the owner's own exclusion**, carried in the affirmed brief: *"This change is not adding a way for an arriving session to go looking for a bigger cause — only what it does with the belief once it has one."* Both existing why-chain sites remain scoped away from a successor — `grep -rn "systemic" skills/` returns two, one behind *"Before drafting"* and one behind the gate that no sitting opens where an affirmed brief is recorded. Whether an arriving session needs such a site at all is [#427](https://github.com/Grimblaz-and-Friends/tradecraft/issues/427)'s and stays open. `skills/engagement/references/design-sitting.md` is untouched.

**6. The why-chain ran and reached no systemic cause, which is recorded rather than assumed.** #432 is a problem, so the chain ran before drafting. It found one open issue of the same shape — [#170](https://github.com/Grimblaz-and-Friends/tradecraft/issues/170), a rule meeting a situation it does not route, so each session improvises and the same facts produce different outcomes — and **no cause whose single fix covers both**: #170's fix is a lawful partial discharge for the defence's re-run rule, and this one's is an ordering sentence. Failing the test that a cause's fix would have prevented the instance and its siblings, the chain stopped at this issue's own level. **The resemblance was recorded on the issue and deliberately not written as a tie**, a tie being what tells the next ranking session to plan one pull request for two unrelated mechanisms.

**7. One question is left open, and the reading is built so it does not load-bear.** Whether a systemic-cause belief a successor forms is the same object a drafting session's why-chain produces — #432's second deferred item — is unanswered. The ordering keys on the belief's **effect**, *an answer that changes what the change is for*, and not on its provenance, so it holds either way. That is the session's call and not the owner's, because nothing about the answer could have made the brief wrong.

**8. No mechanism, guard or script.** The obligation is discharged by a session's behaviour on arriving at an issue, and no guard in this tree reads that. The same weakness [D-431] recorded applies here and is not newly answered.

## What this change cost

**The figures are named by their command and their tree rather than stated**, per `skills/authoring/references/frozen-documents.md` — a decision entry freezes at its own commit, and a number written here cannot be re-derived by the reader who needs it most. Run them:

- `python tools/lint.py`, at `07511c362979649c7711dd9a54d5de7c078d9fe6` and at this entry's merge commit — the `engagement` cell-body row and the always-on rows. That cell reports *no body budget*, so no ceiling is in play, and the change touches the cell body alone: **the name and description are untouched, so the always-on row does not move and no outflow is owed.** `docs/admissions.jsonl` is untouched.
- `python tools/figures.py --base 07511c362979649c7711dd9a54d5de7c078d9fe6` — the always-on surface per runtime and for an adopter, and the prose delta across all governing prose, each with its basis inline.

**One paragraph changed and nothing else in the shipped zone.** `skills/engagement/SKILL.md:95` gains four sentences appended to the existing two, which are preserved verbatim; `.claude-plugin/plugin.json` takes the version bump a shipped-zone change owes. No prose was shed against it, so this change is net-positive on the cell body.

**Two cold seats, both `would`, and the second exists because of the first.** The first judged 9,838 bytes, sha256 `38dcede8460b699d1571d7bdc11e65af31642f40fcbf981e7e075a703340c679`, at `07511c3`, and returned `would` with two defects reported as observations rather than as points of failure: the artifact printed `grep -n "systemic" skills/`, which errors without `-r` — so the command as written was not runnable, though the claim about what it returns was true of the recursive form — and criterion 3 pinned no paragraph, so an implementation editing the amendment route at `:66` instead of the successor paragraph would have passed it. **Both were repaired, and because the repairs touched claims a fresh seat was dispatched rather than the carve-out invoked.** The second judged 10,156 bytes, sha256 `f4cb5ef6858cef952335782b60bd3299a5f68b57c01ce90aaa76db5605f22632`, at the same commit, and returned `would`. **No adverse verdict landed**, so the settled artifact carries no verdict lines, and neither round counts toward the two-round cap, which counts `would not` alone.

**A departure from `skills/engagement/references/cold-seat.md`, recorded rather than hidden.** Both dispatches pinned the artifact by absolute path plus sha256 and byte count instead of inlining it. That file requires inline because the artifact at that moment is committed and posted nowhere, so a path may name something the seat cannot read and a later reader cannot pin. Here the path was outside any repository and readable from the seat's own worktree; the dispatch stated the digest and the count and required the seat to compute both and refuse on any difference, which both seats did and reported. The reason the rule gives was met by a stronger carrier; the rule's letter was not.

**The stale-default hazard fired in the check and not in the run.** `skills/engagement/references/cold-seat.md` warns that a fresh dispatch comes up on the repository default rather than the branch the work is on. Local `main` was `bab184c68e20ec19b1ac7c04c9710920010799a9`, one commit behind this branch's base, so a seat judging on it would have read the pre-#429 cell. Both dispatches named `07511c3` and carried `git checkout --detach`; both seats reported arriving on it already, and reported the sha before and after as the dispatch required. The check is what establishes that, not the outcome.
