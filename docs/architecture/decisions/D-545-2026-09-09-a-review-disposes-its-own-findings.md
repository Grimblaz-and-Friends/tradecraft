# D-545 — A review disposes of its own findings, and only a decided cause leaves it as a number

**Landed by** [PR #545](https://github.com/Grimblaz-and-Friends/tradecraft/pull/545), closing [#542](https://github.com/Grimblaz-and-Friends/tradecraft/issues/542). Brief affirmed 2026-09-09 at [issue #542 comment](https://github.com/Grimblaz-and-Friends/tradecraft/issues/542#issuecomment-5609820464); artifact at [comment 5609956394](https://github.com/Grimblaz-and-Friends/tradecraft/issues/542#issuecomment-5609956394). Evidence pinned at `69ba49f` unless stated.

## What was decided

Every review here ended by creating issues, and it created them without anyone having decided the item was worth doing. Since the trial opened at 2026-09-04T22:34Z, 37 pull requests merged, 153 issues were created and 68 closed as completed. Three paragraph-sized changes landed overnight and their reviews filed seventeen issues, six rated lowest on both scales. Four rules move, on four shipped surfaces and one repo-only cell.

**A `record` ruling writes an entry and files nothing.** This reverts a term [#434](https://github.com/Grimblaz-and-Friends/tradecraft/issues/434)'s stage two landed at `ccacec2e` ([PR #441](https://github.com/Grimblaz-and-Friends/tradecraft/pull/441)) — that where the repository keeps a pool, a record is filed into it as well, *"which is what stops the record being a list nothing reads"*. **That premise was wrong when it was written.** `skills/adversarial-review/references/dispatch.md` already has every review fetch the findings record before its first dispatch — *"Findings an earlier review recorded against this artifact are fetched before the first dispatch"* — and a seat meeting an entry again is what promotes it. The record had a reader; the term added a second one and a number with it.

**A finding whose only consequence is that the text is wrong is fixed or dropped, and takes no entry and no number.** Clause (b) of `arbitration.md` routed exactly that class to `record`, and made it *"the precedence wherever fix and record both fit"*. A docstring, a comment, a wording is now fixed in the batch or dropped in the report with its reason. What stays `record` is the other limb alone: real, evidenced, and the evidence that acting is worth it not yet there.

**At most three causes leave one review, and no finding leaves the closing stage unplaced** — one it does not fix is a comment under a cause, this review's or one already open, an issue of its own where none could be named, an entry in the record, a matter handed to the owner or to a spike, or a drop with its reason. The once-per-cause rule was already there and carried no number, so three filings against one edit in one night passed it. **The first draft of this rule said *exactly one of three ends* and *there is no fourth*, which was false of four routes in the same file; the review's fix batch replaced it, and the external pass's second look caught this entry still describing the pre-fix version.**

**The one-seat lane is what a review runs by default.** `Choosing the shape` sent doubt to the panel — *"high blast radius — and when in doubt, this lane"* — and three of the last four reviews here were panels on paragraph-sized changes. Doubt now runs the one pass; the panel is bought by newly written foundational prose, new scripts or high blast radius, and declared risk buys width beyond either.

**Sites.** `skills/adversarial-review/references/arbitration.md` (clause (b), the pool-filing sentence, the once-per-cause paragraph, the refused-cause paragraph, the weight-argues-weight paragraph), `skills/adversarial-review/references/the-record.md` (the entry's fields, and the `issue` key it obliged), `skills/adversarial-review/SKILL.md` (both pointer lines and `Choosing the shape`), `skills/filing/SKILL.md` (the success line and the one exception to the prose bar), `docs/cells/records/SKILL.md` (where a `record` ruling goes).

## What was rejected

**Migrating the entries the term already wrote.** Records are exhaust; `docs/recorded-findings.jsonl` keeps every `issue` key it carries, because that filing did happen. The records cell now says so on the sentence that used to say the rulings themselves were not migrated.

**Making the refresh note's arrival-against-closure line obligatory.** The brief's measure — issues filed against issues closed, which has to fall below one for one — is read off a line `docs/cells/board/SKILL.md` already obliges: its watch-items bullet is *"Rate of arrival against rate of closure"*, and the drift-look section names the command supplying the arrival half. This change's own artifact first claimed nothing obliged it; that was false about the tree and the cold seat caught it. Nothing new is built to take the measure.

**Restating the doubt rule in both lane bullets.** It was written twice in the first draft. A restatement is what drifts, so it has one site: the bullet a chooser lands on.

## What this change did not settle

**Which issues the reverted term's closure covers is the owner's, and is open.** The brief's term is *"The handful of issues that term created are closed with a note naming their entry in the record."* It is not a handful. The set is the **distinct** issues named by entries appended to `docs/recorded-findings.jsonl` at or after the term landed — the file held 314 lines at `ccacec2e^` and 323 at `ccacec2e`, so line 315 on is the term's era. **Count the distinct numbers, not the entries:** 39 entries carry an `issue` key and they name 36 issues, `#449` appearing four times, and three more — `#536`, `#537`, `#538` — are named in `finding` prose with no key at all, so a derivation reading keys alone misses them. That is **39 issues, 37 still open** once `#488` and `#505` are subtracted, about a quarter of the pool. Twenty-seven of those were filed at the floor on both axes, which is the rating the term itself dictated; ten were rated above it by the filer. The recommendation put to the owner is to close the 27 and leave the 10, and **no issue is closed until he rules**: closing 37 live issues is an act no edit at review takes back and the map does not account for, and what he affirmed and the size of the class are not the same thing. The ask is at [comment 5609960202](https://github.com/Grimblaz-and-Friends/tradecraft/issues/542#issuecomment-5609960202).

**The cap of three is the session's term, not the owner's**, marked as such in the brief's affirmation record so it can be struck without touching the other three rules. Struck, the once-per-cause rule reverts to carrying no number and the three-ends sentence stands on its own.

## Cost

**Figures re-derived against the final tree, the fix batch and the external pass's fixes both having invalidated earlier ones.** Net **−586 characters** across the five surfaces: `skills/filing/SKILL.md` −413, `skills/adversarial-review/references/the-record.md` −279, `docs/cells/records/SKILL.md` −82, `skills/adversarial-review/SKILL.md` −21, and `skills/adversarial-review/references/arbitration.md` **+209** — the one surface that grew, and every character of that growth was bought by a review finding rather than written before the review: the two contradictions the internal stages found, and the destination gap the external pass found after them. **The unit is decoded UTF-8 characters, which is what `tools/lint.py` measures**, derived as `len()` over each file at `69ba49f` and at this branch. Byte deltas differ on the files carrying em dashes, and the external pass's second look reported its byte arithmetic as an error in this record; it is a different unit, not a different answer.

**No cell body here grew, and both that were over their constants came down — one of them under.** `python tools/lint.py` at `69ba49f` and at this branch: `adversarial-review` **9,864 → 9,843** against a constant of 9,855, from 9 over to **12 under**; `filing` 28,435 → 28,022 against 24,158, which is 413 less over and **still 3,864 over** — a gap this change does not close and [#476](https://github.com/Grimblaz-and-Friends/tradecraft/issues/476) is the work that would; `records` 8,873 → 8,791, from exactly at its constant to 82 under. The first draft of `Choosing the shape` grew the cell by 96 and three consolidation passes took it back past where it started; the fix batch took it under.

## What the review found, recorded because it changed the text

One `cold-read` pass and a defense that was also the terminal stage, each in its own worktree at `9197aea`. **Eight findings from the seat, one from the defense, nine disposed and nothing filed** — which is the change's own subject arriving as evidence about itself: the cap of three was never approached, and the session note reports the same thing from the other side.

**Two of the sustained findings were contradictions this change created between its new sentences and sentences it left standing**, and both are the class the `revision-diff` seat exists for — which the one-seat lane this change makes the default does not seat.

- **Clause (b)'s gate refused the class its own new limb admits.** *"The batch admits only a finding whose stated consequence is one a consumer would experience"* is a closed *only*, and the new text-only limb puts the excluded class into the batch. At the base there was no conflict, because text-only routed to `record`. The limb now comes first and the gate is residual: *"Every other finding the batch admits…"*.
- **"There is no fourth" was a false universal.** Four routes in the same file end a finding elsewhere — a lone filing, the owner, a spike, a two-tranche split — and a fifth sits in the cell body every review loads. Codex's external pass reached the same finding independently, by a route neither internal stage used: `the-record.md` still requires the report to name each handed-on decision's destination. Replaced with *"No finding leaves this stage unplaced"* and an enumeration that is true.
- **The panel-buyer sentence read as a closed list of three and omitted declared risk**, which this change had just made a panel buyer. The experience session had measured a consumer enumerating exactly three and not counting risk. Risk is now in the list, and the five-seat sentence no longer describes a population the buyer sentence does not admit.
- **The settling row claimed to be the first to carry `reversed`.** Eight prior rows carry it, as `null`. Fixed to *the first non-null value* — the only fix on the docket whose price becomes unpayable at merge, the record being append-only, and the one the terminal stage ordered first.
- **This entry's own derivation of the closure set could not be reproduced from what it stated.** It is restated above: distinct issues, not entries, and three of the thirty-nine are named in prose with no `issue` key at all, so a derivation reading keys alone misses them. The totals were right; the method as written was not.

**Two matters went to the owner rather than being fixed**, attached to the ask already open: whether *"newly written foundational prose"* and *"substantially new prose"* name one bar or two, and the cap of three, which — struck — takes one sustained finding with it.

One cold seat, one round, verdict `would`. Five corrections were made to the artifact after that verdict, each one the seat itself supplied the true version of, so the revision touched nothing the verdict turned on; the one that mattered was the false claim about the refresh note, above.
