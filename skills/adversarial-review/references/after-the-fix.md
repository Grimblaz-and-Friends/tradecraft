# The fix batch — what binds while writing it, what it owes, and the passes around it

**Loaded when** you are writing a review's fix batch, discharging what it owes once it has landed, a pull request is open and an automated reviewer may have posted on it, or you invoked a review tool yourself. The three standards in the first section bind every stage; **the rest of this file** a review's seats, defense and judge do not need.

The ruled batch goes to the resumed implementer, and every account the holder then writes goes through the **Source-verification rule** — both the `engagement` cell's, in its stretch depth.

## While the fixes are written

- **A new test pin must go red against the pre-fix revision** — a green suite proves nothing about the defect.
- **Derive a correction from its source; never adjust a wrong claim until it looks right.** Re-run the query or re-read the passage at fix time and write what it returns. A count over a corpus the change itself writes into is a query, not a number. Land the fix where the claim is *read* as well as where it is written.
- **A fix changes the state the finding was diagnosed in** — re-derive the diagnosis and its probe against the tree the fix produces; a probe re-run unchanged answers the pre-fix question. A fix that reaches *n* sites creates *n* more to check, including sites the fix itself introduces.

## The fix batch — what it owes, and what looks at the result

**The fix batch is the round's last act** [D-371]: no further prosecution pass is bought, ruled, or recorded, and no stage names a budget for one. What reads the result instead is use, below.

**The executable floor — mandatory, and never recorded or handed on in place of being run.** The resumed builder receiving the whole ruled batch runs it against the tree that batch produces: the suites, the lint, and each sustained finding's own probe re-derived against that tree. **Where a sustained finding carried no probe the floor owes nothing for it** — the floor is what executes, and a check invented at fix time is a fresh instrument answering a question no stage asked. **What the floor could not reach is named in the report**, so *nothing executable touched these fixes* and *the floor ran clean* stop being one silence.

**The implementer re-derives every figure the batch invalidates and re-reads every sentence turning on a term, contract or definition it moved — whose characters never changed — on every surface the batch changes**, and returns the changed evidence to the holder: a fix moves the tree the records describe, so each writer checks its own text before it is installed or published. **An implementer departing from an ordered remedy returns the ordered shape and its reason to the holder, who records the departure where the batch lands**: with no look after it the departure is stated in the batch or nowhere, and a ruling nobody can tell was departed from has stopped meaning anything.

**What looks at the result is use, and it is two things.** The experience session the batch buys — when a batch buys one is the `experience-session` cell's — reads the fixed tree before the review closes, and the holder returns its findings to the implementer for repairs whose batch owes this floor. A decline takes an independent ruling; where none is available inside the review, the holder puts the offer to an independent stage outside the review or pitches it under the filing standard. Then **friction met under merged material in ordinary later work is the standing intake**: it routes to a filing, which is where what a further round would have caught now arrives, and the review's residual risk is accepted against it.

## The external pass — reconciled every time

**An external pass is what automation configured on the repository posts on the pull request without being asked**, which is why publishing before the panel makes it concurrent rather than serial. It is collected whole, as a receipted bundle — `python ../scripts/external_pass.py collect --repo OWNER/REPO --pr PR_NUMBER --output ../../../external-pass-PR_NUMBER.json`, the paths resolving against this file's directory, following the anchoring sentence in the `filing` cell's `the-pool.md`, and the bundle kept outside `skills/` because it is review evidence rather than shipped material — and never committed: the report carries its receipt and a durable link, and a later reader retrieves the bundle to the path the receipt names and replays the receipt's `verify` command from the repository root. The bundle is the three REST collections the script names, whole, and not a discovery of every configured reviewer — one reporting only in a pull request body or check run is outside them. It enters every dispatch except a cold-read dispatch inside the shared block with its complete receipt (`../references/dispatch-template.md`); a proved-empty bundle is evidence too. **The holder reconciles after the terminal stage and before the fix batch, on the tree the rulings were made against, obtaining independent rulings for new offers — and again if a push triggered a re-review**, which reads the fixes and so checks the fix batch independently of the floor above. Each reconciliation runs the receipt's `verify` command; a match confirms both the recorded bundle bytes and source remain the source being dispositioned, while what the script reports as drift takes a new bundle at a new path and a disposition for every item that moved. The receipt's counts are cross-checks only: read every returned object's prose, because a finding need not have an inline-comment object of its own. **A comment arriving after the batch rides that same batch** — a review is one round, and that round does not end while an input it owes a disposition is still arriving — so the batch is not finished until reconciliation is, and the floor runs after it. Every external finding gets a disposition (`../references/report-template.md`), and lapsing one, including one arriving after the terminal ruling or a fix-batch push, needs a ruling independent of the builder. **Sustain on independent grounds or not at all** — an external reviewer's cited rule may not be one this repo runs; where the repo's own convention contradicts it, the convention wins and the lapse says so. Where GitHub stays unavailable after a retry, the error is an explicit gap and an independent ruling defers the review rather than lapsing its input.

**A review tool the session invokes itself is a commissioned pass, not an external one.** On the unattended stretch, the holder commissions it. It is asked for and no push triggers it, so the paragraph above does not govern it; it is not a seat either, since invoking a tool supplies neither the cold boundary, nor the shared block, nor a lens brief. Its findings **are a member of the terminal stage's docket** (`../references/arbitration.md`), whose enumeration is set by rule and reaches this one, and the report records what was run, what it returned, and each disposition. **It runs before that stage, where an external pass reconciles after it** — the inversion is the point: a pass you commissioned is evidence you chose to gather, and evidence arrives before the ruling it informs. It is reported in the report's prose alone, never under `seats` or the index's `external`.
