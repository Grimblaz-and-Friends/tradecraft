# After a fix batch — the executable floor, the external pass, and the commissioned one

**Loaded when** a fix batch has landed and you are discharging what it owes, a pull request is open and an automated reviewer may have posted on it, or you invoked a review tool yourself. A review's seats, defense and judge do not need it.

## The fix batch — what it owes, and what looks at the result

**A review is one round, and the fix batch is its last act.** The panel or the routine lane's cold pass, the defense, the terminal stage, the batch, and the floor below — once each, and the review closes. **No further prosecution pass is bought, ruled, or recorded**, and no stage names a budget for one: nothing after round one has been shown to pay on the text that ships, while [six cycles against a budget of two wrote a fresh false or imprecise claim in five of their six batches, every one in the change's own record](https://github.com/Grimblaz-and-Friends/tradecraft/issues/138#issuecomment-5521380432). What reads the result instead is use, below.

**The executable floor — mandatory, and never recorded or handed on in place of being run.** Against the tree the fixes produce: the suites, the lint, and each sustained finding's own probe re-derived against that tree. **Where a sustained finding carried no probe the floor owes nothing for it** — the floor is what executes, and a check invented at fix time is a fresh instrument answering a question no stage asked. **What the floor could not reach is named in the report**, so *nothing executable touched these fixes* and *the floor ran clean* stop being one silence.

**The batch re-derives every figure it invalidates, on every surface it wrote** — the commit message, the pull request body, a decision entry not yet merged. A fix moves the tree the figures were taken from, and nothing now re-reads the change's own record, so the duty sits on the writer alone. **A batch that departs from an ordered remedy says so where it lands**, naming the ordered shape and the reason: with no look after it the departure is stated in the batch or nowhere, and a ruling nobody can tell was departed from has stopped meaning anything. Post the report before writing the index row that cites it.

**What looks at the result is use, and it is two things.** The experience session the batch buys on the fixed tree — where it rewrote what the material instructs, or changed what a consumer can do — reads that tree before the review closes; what it finds is fixed by the session that reads it, whose own batch owes this floor and nothing further, and a decline needs a stage that is not the session that built the change. Then **friction met under merged material in ordinary later work is the standing intake**: it routes to the board as a filing, which is where what a further round would have caught now arrives, and the review's residual risk is accepted against it.

## The external pass — reconciled every time

**An external pass is what automation configured on the repository posts on the pull request without being asked**, which is why publishing before the panel makes it concurrent rather than serial. Reconcile after the terminal stage and before the fix batch; reconcile again if a push triggered a re-review, which reads the fixes and so checks the fix batch independently of the floor above. Every comment gets a disposition: duplicate of an internal finding (named as such), sustained and fixed, sustained and handed on with its destination, sustained and recorded, or dropped on the evidence with a one-line reason. **Sustain on independent grounds or not at all** — an external reviewer's cited rule may not be one this repo runs; where the repo's own convention contradicts it, the convention wins and the drop says so. Record what actually posted even when it produced nothing; configured reviewers stub, rate-limit, and skip. Where the repository keeps an index, book that qualitative outcome as a non-empty string under top-level `external`, never as arithmetic and never under `seats`.

**A review tool the session invokes itself is a commissioned pass, not an external one.** It is asked for, no push triggers it, and none of the availability states above can apply to it, so the paragraph above does not govern it. It is not a seat either: invoking a tool supplies neither the cold boundary, nor the shared block, nor a lens brief, and who invoked it is not what makes a seat. Its findings **are a member of the terminal stage's docket** (`../references/arbitration.md`), whose enumeration is set by rule and reaches this one, and the report records what was run, what it returned, and each disposition. **It runs before that stage, where an external pass reconciles after it** — the inversion is the point: a pass you commissioned is evidence you chose to gather, and evidence arrives before the ruling it informs. It is never booked under `seats` or in the index's `external`, both of which would misdescribe its provenance.
